---
phase: 01-monorepo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - tsconfig.json
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/database/package.json
  - packages/database/tsconfig.json
  - packages/database/src/index.ts
  - packages/utils/package.json
  - packages/utils/tsconfig.json
  - packages/utils/src/index.ts
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - apps/site/package.json
  - apps/site/tsconfig.json
  - themes/.gitkeep
  - docs/.gitkeep
autonomous: true
requirements:
  - CORE-01
  - CORE-02

must_haves:
  truths:
    - 'pnpm install 成功安装所有工作空间依赖'
    - 'pnpm -r exec tsc --noEmit 类型检查通过'
    - '本地包可以通过 @my-blog/* 导入'
  artifacts:
    - path: 'pnpm-workspace.yaml'
      provides: '工作空间定义，通配符发现 apps/*, packages/*, themes/*'
      contains: 'packages:'
    - path: 'tsconfig.base.json'
      provides: 'TypeScript 基础配置，适度严格模式'
      contains: 'noImplicitAny: true'
    - path: 'packages/core/package.json'
      provides: '@my-blog/core 包定义'
      contains: '@my-blog/core'
  key_links:
    - from: 'packages/*/package.json'
      to: 'pnpm-workspace.yaml'
      via: 'workspace 协议'
      pattern: "workspace:\\*"
    - from: 'packages/*/tsconfig.json'
      to: 'tsconfig.base.json'
      via: 'extends 继承'
      pattern: "extends.*tsconfig\\.base\\.json"
---

<objective>
创建 pnpm workspace 工作空间和 TypeScript 基础配置，建立 5 个初始包的骨架结构。

Purpose: 为 monorepo 架构奠定基础，确保工作空间依赖解析和类型系统正常运作。
Output: 可运行的工作空间配置和 5 个空包（core, database, utils, types, cli）
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo/01-CONTEXT.md
@.planning/phases/01-monorepo/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 pnpm workspace 配置和根 package.json</name>
  <files>pnpm-workspace.yaml, package.json</files>
  <action>
    1. 创建 `pnpm-workspace.yaml`:
       - packages: ['apps/*', 'packages/*', 'themes/*']
       - settings.sharedWorkspaceLockfile: true
       - settings.linkWorkspacePackages: true

    2. 创建/更新根 `package.json`:
       - name: "my-blog", private: true
       - scripts: dev, build, lint, lint:fix, format, format:check, type-check, prepare
       - devDependencies: typescript@^5.7.0, eslint@^9.0.0, prettier@^3.2.0, husky@^9.0.0, lint-staged@^15.0.0
       - engines: node>=18.0.0, pnpm>=10.0.0

    3. 创建空目录：apps/, packages/, themes/, docs/
    4. 创建占位文件：themes/.gitkeep, docs/.gitkeep

    使用 pnpm 10.x 版本，确保工作空间配置使用最新语法。

  </action>
  <verify>
    <automated>pnpm install --reporter=silent && echo "Workspace setup successful"</automated>
  </verify>
  <done>
    - pnpm-workspace.yaml 存在且包含正确的 packages 配置
    - package.json 存在且包含必需的 scripts
    - pnpm install 执行成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 TypeScript 基础配置</name>
  <files>tsconfig.base.json, tsconfig.json</files>
  <action>
    1. 创建 `tsconfig.base.json` (共享配置):
       - target: ES2022, module: ESNext, lib: [ES2022, DOM, DOM.Iterable]
       - moduleResolution: bundler
       - strict: false, noImplicitAny: true, strictNullChecks: true, noUnusedLocals: true
       - skipLibCheck: true, declaration: true, declarationMap: true, sourceMap: true
       - resolveJsonModule: true

    2. 创建根 `tsconfig.json`:
       - extends: ./tsconfig.base.json
       - files: []
       - references: 指向 packages/core, packages/database, packages/utils, packages/types, apps/site

    使用适度严格模式，不开启 strict: true。

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json && echo "TypeScript config valid"</automated>
  </verify>
  <done>
    - tsconfig.base.json 包含适度严格的配置
    - tsconfig.json 使用 project references
    - tsc --noEmit 不报错
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 5 个初始包骨架</name>
  <files>
    packages/core/package.json, packages/core/tsconfig.json, packages/core/src/index.ts,
    packages/database/package.json, packages/database/tsconfig.json, packages/database/src/index.ts,
    packages/utils/package.json, packages/utils/tsconfig.json, packages/utils/src/index.ts,
    packages/types/package.json, packages/types/tsconfig.json, packages/types/src/index.ts,
    packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/src/index.ts
  </files>
  <action>
    为每个包创建标准 npm 包结构：

    1. packages/core (@my-blog/core):
       - package.json: name, version: 0.0.0, main: dist/index.js, module: dist/index.mjs, types: dist/index.d.ts, scripts: build, dev
       - tsconfig.json: extends ../../tsconfig.base.json, outDir: dist, rootDir: src, paths: { "@/*": ["src/*"] }
       - src/index.ts: 导出 "Blog Core Framework" 注释

    2. packages/database (@my-blog/database):
       - 同上结构，注释 "Database Layer (Drizzle ORM + SQLite)"

    3. packages/utils (@my-blog/utils):
       - 同上结构，注释 "Shared Utilities"

    4. packages/types (@my-blog/types):
       - 同上结构，注释 "Shared TypeScript Types"

    5. packages/cli (@my-blog/cli):
       - 同上结构，注释 "CLI Tools"

    所有包使用 workspace:* 协议进行内部依赖（当前都为空）。
    所有包使用 src/ 目录存放源代码。

  </action>
  <verify>
    <automated>pnpm -r exec tsc --noEmit && echo "All packages type-check"</automated>
  </verify>
  <done>
    - 5 个包目录结构完整 (package.json, tsconfig.json, src/index.ts)
    - 所有包使用 @my-blog/* scoped 命名
    - 所有包 extends 基础配置
    - 所有包 type-check 通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建 apps/site 应用骨架</name>
  <files>apps/site/package.json, apps/site/tsconfig.json, apps/site/src/index.ts</files>
  <action>
    创建主应用包 @my-blog/site:

    1. apps/site/package.json:
       - name: "@my-blog/site"
       - version: 0.0.0
       - private: true
       - dependencies: @my-blog/core: workspace:*, @my-blog/database: workspace:*, @my-blog/utils: workspace:*, @my-blog/types: workspace:*
       - devDependencies: typescript: ^5.7.0
       - scripts: dev, build, type-check

    2. apps/site/tsconfig.json:
       - extends: ../../tsconfig.base.json
       - compilerOptions.outDir: .nuxt/dist
       - compilerOptions.rootDir: .
       - compilerOptions.paths: { "@/*": ["src/*"], "@my-blog/*": ["../../packages/*/src"] }
       - include: ["**/*.ts", "**/*.vue"]
       - exclude: ["node_modules", ".nuxt", "dist"]

    3. apps/site/src/index.ts: 空导出或注释 "Nuxt 3 Application Entry"

  </action>
  <verify>
    <automated>cd apps/site && npx tsc --noEmit && echo "Site app type-check passed"</automated>
  </verify>
  <done>
    - apps/site/package.json 存在且使用 workspace:* 依赖
    - apps/site/tsconfig.json 配置正确
    - type-check 通过
  </done>
</task>

</tasks>

<verification>
Phase 1 Plan 01 验证:
1. pnpm install 成功
2. pnpm -r exec tsc --noEmit 所有包类型检查通过
3. 工作空间依赖可以正常解析
</verification>

<success_criteria>

- pnpm-workspace.yaml 配置正确
- 5 个包 + 1 个应用骨架创建完成
- TypeScript 配置使用适度严格模式
- 所有包 type-check 通过
  </success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo/01-01-SUMMARY.md`
</output>
