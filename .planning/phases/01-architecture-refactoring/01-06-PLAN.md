---
phase: 01-architecture-refactoring
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/pages/admin/comments.vue
  - apps/site/pages/admin/posts.vue
  - apps/site/pages/admin/general.vue
  - apps/site/pages/admin/display.vue
  - apps/site/pages/admin/features.vue
  - apps/site/pages/admin/analytics.vue
  - apps/site/pages/admin/drafts.vue
  - apps/site/pages/admin/index.vue
autonomous: false
requirements: [ARCH-05, ARCH-06]

must_haves:
  truths:
    - "No admin page uses scoped `<style>` blocks with `@apply` directives — all styles are inline UnoCSS utility classes"
    - "All toggle switches render identically to before the change (visual regression: toggle switches work)"
    - "All 8 child admin pages inherit layout from parent admin.vue — no double scrollbar"
  artifacts:
    - path: "apps/site/pages/admin/comments.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/posts.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/general.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/display.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/features.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/analytics.vue"
      provides: "Layout-fixed, @apply-free admin page"
      changed: ["remove layout from definePageMeta", "remove scoped style block", "inline toggle-switch classes"]
    - path: "apps/site/pages/admin/drafts.vue"
      provides: "Layout-fixed admin page"
      changed: ["remove layout from definePageMeta only"]
    - path: "apps/site/pages/admin/index.vue"
      provides: "Layout-fixed admin dashboard page"
      changed: ["remove layout from definePageMeta only"]
  key_links: []
---

<objective>
Complete the remaining layout chain fixes (8 child admin pages) AND replace all scoped `<style>` blocks using `@apply` with inline UnoCSS utility classes on the 6 pages that have them. Per decisions D-11 through D-15.
Purpose: Eliminate the remaining redundant layout declarations and the scoped `@apply` styles. All admin pages use inline UnoCSS only.
Output: 8 pages with layouts fixed; 6 of those also have scoped `@apply` styles replaced with inline classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
**Before implementing, read these files in full (each once):**

1. `apps/site/pages/admin/general.vue` — 502 lines. Example of a page needing BOTH layout removal AND @apply cleanup. Focus on:
   - Lines 2-4: `definePageMeta({ layout: 'admin-default' })` — the layout to remove
   - Lines 494-501: `<style scoped>` block with `.toggle-switch` and `.toggle-knob` `@apply` directives
   - Template: search for `class="toggle-switch"` and `class="toggle-knob"` usages to understand how to inline them
2. `apps/site/pages/admin/comments.vue` — Read to confirm same @apply pattern
3. `apps/site/pages/admin/drafts.vue` — Read to confirm it has layout but NO @apply
4. `apps/site/pages/admin/index.vue` — Read to confirm it has layout but NO @apply

The toggle-switch pattern is IDENTICAL across all 6 @apply pages:
```css
.toggle-switch { @apply relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer; }
.toggle-knob { @apply inline-block h-4 w-4 transform rounded-full bg-white transition-transform; }
```

The template usage follows this pattern (found by grepping each page):
```html
<button class="toggle-switch" :class="condition ? 'bg-primary' : 'bg-surface-2'" @click="handler">
  <span class="toggle-knob" :class="condition ? 'translate-x-6' : 'translate-x-1'" />
</button>
```

Do NOT re-read the same file twice. One pass per file.
</read_first>

<interfaces>
**UnoCSS class mapping for toggle-switch (inline replacement):**

| @apply class | UnoCSS inline equivalent |
|---|---|
| `.toggle-switch { @apply relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer; }` | `class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer"` |
| `.toggle-knob { @apply inline-block h-4 w-4 transform rounded-full bg-white transition-transform; }` | `class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform"` |

The toggle component becomes (before state class):
```html
<button
  class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer"
  :class="condition ? 'bg-primary' : 'bg-surface-2'"
  @click="toggle"
>
  <span
    class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform"
    :class="condition ? 'translate-x-6' : 'translate-x-1'"
  />
</button>
```

Note: When inlining, the static classes go in `class=""`, and the dynamic boolean classes go in `:class=""`. Never put static classes inside `:class=""` and never put dynamic expressions inside `class=""`.

**Layout removal patterns** — same as plan 01-05:
- If `definePageMeta` has ONLY `layout: 'admin-default'` → remove entire block
- If `definePageMeta` has layout + other keys → remove only the `layout: 'admin-default',` line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove layout from 8 pages + replace @apply styles on 6 pages</name>
  <files>
    <file>apps/site/pages/admin/comments.vue</file>
    <file>apps/site/pages/admin/posts.vue</file>
    <file>apps/site/pages/admin/general.vue</file>
    <file>apps/site/pages/admin/display.vue</file>
    <file>apps/site/pages/admin/features.vue</file>
    <file>apps/site/pages/admin/analytics.vue</file>
    <file>apps/site/pages/admin/drafts.vue</file>
    <file>apps/site/pages/admin/index.vue</file>
  </files>
  <action>
    For each of the 8 files above, perform the following edits:

    **Part A — Layout removal (ALL 8 pages):**
    Read the `<script setup>` block. Find `definePageMeta({ ... })`. Remove the `layout: 'admin-default'` declaration using the same patterns as plan 01-05:
    - If `definePageMeta` has ONLY layout → remove the entire `definePageMeta({ layout: 'admin-default' })` block
    - If `definePageMeta` has layout + other keys → remove only `layout: 'admin-default',` line

    Expected per-file layout patterns (verify before editing):
    | File | Expected pattern |
    |------|-----------------|
    | comments.vue | layout only → remove entire block |
    | posts.vue | layout only → remove entire block |
    | general.vue | layout only → remove entire block |
    | display.vue | layout only → remove entire block |
    | features.vue | layout only → remove entire block |
    | analytics.vue | layout only → remove entire block |
    | drafts.vue | layout only → remove entire block |
    | index.vue | layout only → remove entire block (may also have middleware — check!) |

    **Part B — @apply cleanup (6 pages: comments, posts, general, display, features, analytics):**
    For each of these 6 files:

    1. **Delete the `<style scoped>` block entirely.** Find the block at the bottom of the file:
       ```css
       <style scoped>
       .toggle-switch { @apply relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer; }
       .toggle-knob { @apply inline-block h-4 w-4 transform rounded-full bg-white transition-transform; }
       </style>
       ```
       Remove all three lines (the `<style scoped>` opening, the two @apply rules, and the `</style>` closing).

    2. **Replace `class="toggle-switch"` in templates with inline UnoCSS classes.** Search each file's `<template>` for `toggle-switch`. For each occurrence:
       - Find: `<button class="toggle-switch" :class="condition ? 'bg-primary' : 'bg-surface-2'" @click="handler">`
       - Replace with: `<button class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer" :class="condition ? 'bg-primary' : 'bg-surface-2'" @click="handler">`
       - The `class="toggle-switch"` becomes `class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer"`

    3. **Replace `class="toggle-knob"` in templates with inline UnoCSS classes.** Search for `toggle-knob`. For each occurrence:
       - Find: `<span class="toggle-knob" :class="condition ? 'translate-x-6' : 'translate-x-1'" />`
       - Replace with: `<span class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform" :class="condition ? 'translate-x-6' : 'translate-x-1'" />`
       - The `class="toggle-knob"` becomes `class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform"`

    4. **Merge adjacent class attributes if needed.** Never create two `class=""` attributes on the same element. If an element already has `class="toggle-switch smth-else"`, merge: `class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors cursor-pointer smth-else"`.

    **IMPORTANT rules per D-14/D-15:**
    - `class="..."` contains ONLY static classes (no Vue expressions)
    - `:class="..."` contains ONLY dynamic bindings (Vue expressions like `condition ? 'a' : 'b'`)
    - Never put static UnoCSS classes inside `:class=""`
    - Do not add any `<style>` block to replace what was removed
    - Do not create UnoCSS shortcuts or rules — pure inline utility classes
    - Do not change any other part of the template or script

    **Part C — drafts.vue and index.vue (layout-only, no @apply):**
    These two files only need Part A (layout removal). Skip Part B.
  </action>
  <verify>
    <automated>cd apps/site && npx tsx --eval "
      import fs from 'fs';
      const stylePages = ['comments.vue','posts.vue','general.vue','display.vue','features.vue','analytics.vue'];
      const allPages = [...stylePages, 'drafts.vue', 'index.vue'];
      let pass = true;

      for (const p of allPages) {
        const content = fs.readFileSync('./pages/admin/' + p, 'utf-8');
        if (content.includes(\"layout: 'admin-default'\")) {
          console.error('FAIL: ' + p + ' still has admin-default layout');
          pass = false;
        }
      }

      for (const p of stylePages) {
        const content = fs.readFileSync('./pages/admin/' + p, 'utf-8');
        if (content.includes('@apply')) {
          console.error('FAIL: ' + p + ' still has @apply');
          pass = false;
        }
        if (content.includes('toggle-switch')) {
          console.error('FAIL: ' + p + ' still references toggle-switch class');
          pass = false;
        }
        if (content.includes('toggle-knob')) {
          console.error('FAIL: ' + p + ' still references toggle-knob class');
          pass = false;
        }
        if (content.includes('<style scoped>') && content.includes('toggle')) {
          console.error('FAIL: ' + p + ' still has scoped toggle style block');
          pass = false;
        }
      }

      if (pass) console.log('PASS: all 8 pages layout-free, all 6 pages @apply-free');
    "</automated>
  </verify>
  <done>All 8 pages removed layout: 'admin-default'; 6 @apply pages have scoped style blocks deleted and toggle classes inlined as UnoCSS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Layout chain fixes on 8 remaining admin pages + scoped @apply styles replaced with inline UnoCSS on 6 pages.
    
    - 8 pages: `layout: 'admin-default'` removed from `definePageMeta`
    - 6 pages: `<style scoped>` blocks deleted, `.toggle-switch` and `.toggle-knob` classes inlined as UnoCSS utility classes directly in template `class=""` attributes
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` in the project root
    2. Login to admin panel at `/admin/login`
    3. Navigate to each of the 6 @apply-cleanup pages:
       - `/admin/posts` — verify toggle switches (cover, copyright, reward, related posts, notice outdate) render and toggle correctly
       - `/admin/general` — verify dark mode toggle, footer toggles, social bar toggle, subtitle toggles work
       - `/admin/comments` — verify comment-related toggle switches work
       - `/admin/display` — verify code block highlight toggles work
       - `/admin/features` — verify feature toggles work
       - `/admin/analytics` — verify analytics toggles work
    4. Verify there is only ONE scrollbar on each page (not a nested frontend-default scrollbar wrapped around admin-default)
    5. Navigate to `/admin/drafts` and `/admin` (dashboard) — verify they load correctly (no layout issues, no @apply errors)
  </how-to-verify>
  <resume-signal>Type "approved" if all toggle switches work and layout is correct, or describe specific issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None specific to this plan | Layout and style changes only — no data flow, no auth changes, no new API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | CSS class inlining | accept | CSS is presentation-only — cannot cause data tampering. Toggle state is governed by Vue refs, not CSS classes |
| T-01-12 | Elevation of Privilege | admin.vue layout inheritance | accept | Auth middleware still applied at admin.vue level and inherited by children. Layout change does not bypass auth |
</threat_model>

<verification>
  <automated>cd apps/site && npx tsx --eval "
    import fs from 'fs';
    const stylePages = ['comments.vue','posts.vue','general.vue','display.vue','features.vue','analytics.vue'];
    const allPages = [...stylePages, 'drafts.vue', 'index.vue'];
    let errors = 0;
    
    for (const p of allPages) {
      const content = fs.readFileSync('./pages/admin/' + p, 'utf-8');
      if (content.includes(\"layout: 'admin-default'\")) { errors++; console.log('Layout fail:', p); }
    }
    for (const p of stylePages) {
      const content = fs.readFileSync('./pages/admin/' + p, 'utf-8');
      if (content.includes('@apply')) { errors++; console.log('@apply fail:', p); }
      if (content.includes('class=\"toggle-switch\"') || content.includes('class=\"toggle-knob\"')) {
        errors++; console.log('toggle class fail:', p);
      }
    }
    console.log(errors === 0 ? 'PASS' : errors + ' errors found');
  "</automated>
</verification>

<success_criteria>
1. All 8 pages no longer have `layout: 'admin-default'` in `definePageMeta`
2. The 6 @apply pages no longer contain `@apply` directives, `.toggle-switch`, or `.toggle-knob` CSS class references
3. The 6 @apply pages no longer have `<style scoped>` blocks containing toggle-switch/toggle-knob rules
4. Toggle switches on all 6 pages are functional and visually identical to before (visual verification)
5. No double scrollbar on any admin page (visual verification)
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-refactoring/01-06-SUMMARY.md`
</output>
