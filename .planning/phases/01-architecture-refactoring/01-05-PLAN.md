---
phase: 01-architecture-refactoring
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/pages/admin.vue
  - apps/site/pages/admin/themes.vue
  - apps/site/pages/admin/homepage.vue
  - apps/site/pages/admin/plugins.vue
  - apps/site/pages/admin/categories.vue
  - apps/site/pages/admin/tags.vue
  - apps/site/pages/admin/seo.vue
  - apps/site/pages/admin/media.vue
  - apps/site/pages/admin/sidebar.vue
  - apps/site/pages/admin/articles/new.vue
  - apps/site/pages/admin/articles/index.vue
  - apps/site/pages/admin/articles/[id].vue
  - apps/site/pages/admin/pages/new.vue
  - apps/site/pages/admin/pages/index.vue
  - apps/site/pages/admin/pages/[id].vue
autonomous: true
requirements: [ARCH-05]

must_haves:
  truths:
    - "Navigating to any admin page does not produce a double scrollbar (no frontend-default layout wrapping admin-default)"
    - "All child admin pages (articles, pages, media, settings pages) render inside the admin sidebar layout without declaring their own layout"
    - "The admin login page (login.vue) continues to use its own 'admin-auth' layout — not affected by this change"
  artifacts:
    - path: "apps/site/pages/admin.vue"
      provides: "Parent admin page with layout declaration"
      contains: ["definePageMeta", "layout: 'admin-default'", "middleware: 'admin-auth'"]
    - path: "apps/site/pages/admin/*.vue (14 files)"
      provides: "Child admin pages without redundant layout declarations"
      changed: "remove layout: 'admin-default' from definePageMeta"
  key_links:
    - from: "pages/admin.vue"
      to: "layouts/admin/default.vue"
      via: "definePageMeta({ layout: 'admin-default' })"
      pattern: "layout.*admin-default"
    - from: "child admin pages"
      to: "parent admin.vue layout"
      via: "Nuxt layout inheritance (children inherit parent's layout when no layout declared)"
      pattern: "no definePageMeta layout"
---

<objective>
Fix the admin layout chain: add `definePageMeta({ layout: 'admin-default' })` to `pages/admin.vue` (the parent admin page), then remove the redundant `layout: 'admin-default'` from 14 child admin pages. Per decisions D-11, D-12, D-13.
Purpose: Eliminate the double-scrollbar issue (frontend default layout wrapping admin-default layout). Child pages inherit the parent's layout via Nuxt's nested route system.
Output: Clean layout chain — one layout declaration at the top, children inherit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
**Before implementing, read these files in full (each once):**

1. `apps/site/pages/admin.vue` — 9 lines. Current admin.vue has `definePageMeta({ middleware: ['admin-auth'] })` WITHOUT a layout. You will ADD `layout: 'admin-default'`.
2. `apps/site/layouts/admin/default.vue` — 48 lines. The admin layout already has correct structure (flex h-screen + Sidebar + main). This is the layout you're wiring.
3. One example child page with layout — `apps/site/pages/admin/homepage.vue` — lines 1-5. Shows `definePageMeta({ layout: 'admin-default' })` that needs removal.
4. One example child page without layout but with auth — `apps/site/pages/admin/plugins.vue` — to see how pages with `definePageMeta` but no layout should look after the change.

That is sufficient. Do NOT read all 14 child pages — the pattern is identical in each. For each file, you either:
- Remove the `layout: 'admin-default',` line from `definePageMeta({ ... })`
- If `definePageMeta` has ONLY `layout: 'admin-default'` with nothing else, remove the entire `definePageMeta({ layout: 'admin-default' })` block
- If `definePageMeta` has `layout: 'admin-default'` AND other keys like `middleware`, remove only the layout line, keeping the rest
</read_first>

<interfaces>
**Pattern A — definePageMeta has ONLY layout (remove entire block):**
```typescript
// BEFORE:
definePageMeta({
  layout: 'admin-default',
})
// AFTER:
// (entire block removed)
```

**Pattern B — definePageMeta has layout + other keys (remove only layout line):**
```typescript
// BEFORE:
definePageMeta({
  layout: 'admin-default',
  middleware: ['admin-auth'],
})
// AFTER:
definePageMeta({
  middleware: ['admin-auth'],
})
```

**Pattern C — definePageMeta has multiple keys with layout in middle (remove only layout line, preserve formatting):**
```typescript
// BEFORE:
definePageMeta({
  middleware: ['admin-auth'],
  layout: 'admin-default',
  someOtherKey: true,
})
// AFTER:
definePageMeta({
  middleware: ['admin-auth'],
  someOtherKey: true,
})
```

**admin.vue — ADD layout (Pattern D):**
```typescript
// BEFORE:
definePageMeta({
  middleware: ['admin-auth'],
})
// AFTER:
definePageMeta({
  layout: 'admin-default',
  middleware: ['admin-auth'],
})
```

**DO NOT TOUCH:**
- `apps/site/pages/admin/login.vue` — uses `layout: 'admin-auth'` (its own layout), not `admin-default`. Leave unchanged.
- Any page that doesn't have `layout: 'admin-default'` at all
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layout: 'admin-default' to pages/admin.vue</name>
  <files>apps/site/pages/admin.vue</files>
  <action>
    Edit `apps/site/pages/admin.vue`. The current `<script setup>` block:

    ```typescript
    definePageMeta({
      middleware: ['admin-auth'],
    })
    ```

    Change to:

    ```typescript
    definePageMeta({
      layout: 'admin-default',
      middleware: ['admin-auth'],
    })
    ```

    That's the ONLY change to this file. The `<template>` block (`<NuxtPage />`) stays unchanged.

    This enables Nuxt's layout inheritance: the parent `admin.vue` declares `admin-default` layout, and all child admin pages rendered via `<NuxtPage />` inherit it. Per D-11.
  </action>
  <verify>
    <automated>cd apps/site && npx tsx --eval "
      import fs from 'fs';
      const content = fs.readFileSync('./pages/admin.vue', 'utf-8');
      console.assert(content.includes(\"layout: 'admin-default'\"), 'has admin-default layout');
      console.assert(content.includes(\"middleware: ['admin-auth']\"), 'has admin-auth middleware');
      console.log('PASS: admin.vue layout added');
    "</automated>
  </verify>
  <done>admin.vue has definePageMeta with layout: 'admin-default' and middleware: ['admin-auth']</done>
</task>

<task type="auto">
  <name>Task 2: Remove layout: 'admin-default' from 14 child admin pages (non-@apply pages)</name>
  <files>
    <file>apps/site/pages/admin/themes.vue</file>
    <file>apps/site/pages/admin/homepage.vue</file>
    <file>apps/site/pages/admin/plugins.vue</file>
    <file>apps/site/pages/admin/categories.vue</file>
    <file>apps/site/pages/admin/tags.vue</file>
    <file>apps/site/pages/admin/seo.vue</file>
    <file>apps/site/pages/admin/media.vue</file>
    <file>apps/site/pages/admin/sidebar.vue</file>
    <file>apps/site/pages/admin/articles/new.vue</file>
    <file>apps/site/pages/admin/articles/index.vue</file>
    <file>apps/site/pages/admin/articles/[id].vue</file>
    <file>apps/site/pages/admin/pages/new.vue</file>
    <file>apps/site/pages/admin/pages/index.vue</file>
    <file>apps/site/pages/admin/pages/[id].vue</file>
  </files>
  <action>
    Edit each of the 14 files above. In each file:

    1. Read the content of the `<script setup>` block (top of file)
    2. Find the `definePageMeta({ ... })` call
    3. Identify which pattern the page uses (A, B, or C from the interfaces section above)
    4. Apply the corresponding removal

    **Specific per-file instructions:**

    | File | Pattern | Action |
    |------|---------|--------|
    | themes.vue | A (layout only) | Remove entire `definePageMeta({ layout: 'admin-default' })` block |
    | homepage.vue | A (layout only, single-line style: `definePageMeta({ layout: 'admin-default' })`) | Remove entire `definePageMeta(...)` call |
    | plugins.vue | A (multi-line, layout only) | Remove entire `definePageMeta` block |
    | categories.vue | A (may have middleware too — check) | Remove only the `layout: 'admin-default',` line. Keep `middleware: ['admin-auth']` if present |
    | tags.vue | A (layout only) | Remove entire block |
    | seo.vue | A (layout only) | Remove entire block |
    | media.vue | A (layout only) | Remove entire block |
    | sidebar.vue | A (layout only) | Remove entire block |
    | articles/new.vue | A (layout only, may have middleware) | Remove only layout line |
    | articles/index.vue | B/C (layout + middleware) | Remove `layout: 'admin-default',` line only |
    | articles/[id].vue | B/C (layout + middleware) | Remove `layout: 'admin-default',` line only |
    | pages/new.vue | A (layout only) | Remove entire block |
    | pages/index.vue | A (layout only) | Remove entire block |
    | pages/[id].vue | A (layout only) | Remove entire block |

    **IMPORTANT: For each file, verify the actual content before editing.** The pattern column above is based on a pre-scan but the executor must verify. Use the Grep tool to find the exact `definePageMeta` block content in each file, then apply the appropriate edit.

    **Edge case:** If `definePageMeta` becomes empty `definePageMeta({})` after removing the layout line, remove the entire `definePageMeta` block (empty definePageMeta is noise).

    **Do NOT modify:**
    - Any other code in the `<script setup>` block
    - The `<template>` block
    - Any `<style>` block
    - The `login.vue` page (uses `admin-auth` layout)
    - Pages not in this list

    Per D-12: "All child admin pages remove their definePageMeta({ layout: 'admin-default' }) declarations — they inherit from the parent."
  </action>
  <verify>
    <automated>cd apps/site && npx tsx --eval "
      import fs from 'fs';
      const pages = [
        'themes.vue','homepage.vue','plugins.vue','categories.vue','tags.vue',
        'seo.vue','media.vue','sidebar.vue',
        'articles/new.vue','articles/index.vue','articles/[id].vue',
        'pages/new.vue','pages/index.vue','pages/[id].vue'
      ];
      let pass = true;
      for (const p of pages) {
        const content = fs.readFileSync('./pages/admin/' + p, 'utf-8');
        if (content.includes(\"layout: 'admin-default'\")) {
          console.error('FAIL: ' + p + ' still has admin-default layout');
          pass = false;
        }
      }
      if (pass) console.log('PASS: all 14 pages have layout removed');
    "</automated>
  </verify>
  <done>All 14 child pages no longer declare layout: 'admin-default'; parent admin.vue inherits to them</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nuxt route resolution | Layout inheritance chain: admin.vue → admin-default layout → child pages. No user input involved — this is a framework configuration change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | layout chain | accept | The admin-auth middleware still guards all admin pages (added to admin.vue, inherited by children). Layout change doesn't affect auth. No privilege escalation possible |
| T-01-10 | Information Disclosure | layout chain | accept | No information disclosure risk — this is a layout nesting change, not a data exposure change |
</threat_model>

<verification>
**Manual visual verification (after execution):**
1. Start dev server: `pnpm dev`
2. Navigate to `/admin` → verify admin login page renders with admin-auth layout (no sidebar)
3. Login, then navigate to `/admin/homepage` → verify page renders inside admin sidebar layout
4. Check for double scrollbar: there should be ONE scrollbar (the main content area), not a nested frontend-default scrollbar wrapping admin-default
5. Navigate to `/admin/articles`, `/admin/pages`, `/admin/media` → verify all render correctly

**Automated check:**
  <automated>cd apps/site && npx tsx --eval "
    import fs from 'fs';
    import path from 'path';
    const adminDir = './pages/admin';
    // Recursively find all .vue files
    function findVue(dir) {
      const results = [];
      for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) { results.push(...findVue(full)); }
        else if (entry.name.endsWith('.vue')) { results.push(full); }
      }
      return results;
    }
    const files = findVue(adminDir);
    let withLayout = [];
    let withoutLayout = [];
    for (const f of files) {
      const content = fs.readFileSync(f, 'utf-8');
      if (content.includes(\"layout: 'admin-default'\")) {
        withLayout.push(f);
      }
    }
    // Only admin.vue should have layout: 'admin-default'
    const adminVue = withLayout.filter(f => f.endsWith('admin.vue'));
    const childPages = withLayout.filter(f => !f.endsWith('admin.vue'));
    console.log('admin.vue has layout:', adminVue.length === 1 ? 'YES' : 'NO');
    console.log('Child pages with layout (should be 0, but excluding login.vue admin-auth):', childPages.length);
    // Also count that login.vue still has its layout
    const loginContent = fs.readFileSync('./pages/admin/login.vue', 'utf-8');
    console.log('login.vue has admin-auth layout:', loginContent.includes(\"layout: 'admin-auth'\") ? 'YES' : 'NO');
  "</automated>
</verification>

<success_criteria>
1. `pages/admin.vue` has `definePageMeta({ layout: 'admin-default', middleware: ['admin-auth'] })`
2. All 14 listed child pages NO LONGER contain `layout: 'admin-default'` in their `definePageMeta`
3. `login.vue` still has its own `layout: 'admin-auth'` (verified untouched)
4. Running `pnpm dev` and navigating to admin pages shows admin sidebar layout (visual check)
5. No double scrollbar (visual check)
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-refactoring/01-05-SUMMARY.md`
</output>
