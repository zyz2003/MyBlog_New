---
plan: 02
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - packages/core/src/theme/**
  - packages/core/src/styles/**
  - themes/**
autonomous: true
---

# Plan 02: 主题系统核心架构

**Phase:** 2
**Goal:** 实现主题系统核心架构，包括 CSS Variables 主题变量、主题管理器、JSON 主题配置

**Requirements covered:**

- THEME-01: 主题系统核心架构
- THEME-02: CSS Variables 主题变量
- THEME-03: JSON 主题配置文件

---

## Must Haves (Goal-Backward Verification)

- [ ] ThemeManager 类实现（切换主题、获取变量）
- [ ] CSS Variables 基础框架（[data-theme] 选择器）
- [ ] JSON 主题配置文件格式
- [ ] 2 个示例主题骨架（Classic + Minimal）
- [ ] 主题切换 API
- [ ] 单元测试覆盖核心逻辑

---

## Tasks

<tasks>

### Task 02-02-01: 创建主题目录结构

**Type:** implementation
**Requirements:** THEME-01

创建主题系统目录结构：

```
themes/
├── classic/
│   ├── index.ts          # 主题入口
│   ├── variables.css     # CSS Variables 定义
│   └── theme.json        # 主题元数据
└── minimal/
    ├── index.ts
    ├── variables.css
    └── theme.json

packages/core/
├── src/
│   └── theme/
│       ├── index.ts          # 主题包入口
│       ├── ThemeManager.ts   # 主题管理器
│       ├── types.ts          # 主题类型定义
│       └── __tests__/
│           └── theme-manager.test.ts
└── package.json

packages/core/src/styles/
├── variables.css         # 全局 CSS Variables
└── base.css              # 基础样式
```

**Verification:**

- 目录结构创建完成
- 所有文件都是空骨架（后续任务填充）

---

### Task 02-02-02: 实现主题类型定义

**Type:** implementation
**Requirements:** THEME-01

```typescript
// packages/core/src/theme/types.ts

/**
 * 主题元数据配置
 */
export interface ThemeMetadata {
  name: string
  displayName: string
  version: string
  author?: string
  description?: string
}

/**
 * 主题颜色配置
 */
export interface ThemeColors {
  primary: string
  secondary?: string
  accent?: string
  background: string
  surface?: string
  text: string
  textSecondary?: string
  border?: string
  error?: string
  success?: string
  warning?: string
}

/**
 * 主题排版配置
 */
export interface ThemeTypography {
  fontFamily: string
  fontFamilyMono?: string
  fontSize: string
  lineHeight: string | number
  fontWeight?: string
  letterSpacing?: string
}

/**
 * 主题间距配置
 */
export interface ThemeSpacing {
  unit: string
  scale?: number
  xs?: string
  sm?: string
  md?: string
  lg?: string
  xl?: string
}

/**
 * 主题圆角配置
 */
export interface ThemeRadius {
  none?: string
  sm?: string
  md?: string
  lg?: string
  xl?: string
  full?: string
}

/**
 * 完整主题配置
 */
export interface ThemeConfig {
  metadata: ThemeMetadata
  colors: ThemeColors
  typography: ThemeTypography
  spacing: ThemeSpacing
  radius?: ThemeRadius
  shadows?: Record<string, string>
}

/**
 * 主题切换选项
 */
export interface ThemeSwitchOptions {
  persist?: boolean // 是否持久化到 localStorage
  transition?: boolean // 是否添加过渡动画
}
```

**Verification:**

- `pnpm type-check` 通过
- 所有类型导出正确

---

### Task 02-02-03: 实现 ThemeManager（主题管理器）

**Type:** implementation
**Requirements:** THEME-01, THEME-02, THEME-03

```typescript
// packages/core/src/theme/ThemeManager.ts
import type { ThemeConfig, ThemeSwitchOptions } from './types'

export class ThemeManager {
  private currentTheme: string
  private themes: Map<string, ThemeConfig> = new Map()
  private hooks: {
    onChange: ((theme: string) => void)[]
  } = { onChange: [] }

  constructor(defaultTheme: string = 'classic') {
    this.currentTheme = defaultTheme

    // 从 localStorage 恢复主题
    const savedTheme = this.getSavedTheme()
    if (savedTheme && this.themes.has(savedTheme)) {
      this.currentTheme = savedTheme
    }

    // 应用初始主题
    this.apply(this.currentTheme, { transition: false })
  }

  /**
   * 注册主题
   */
  register(name: string, config: ThemeConfig): void {
    this.themes.set(name, config)

    // 如果这是当前主题，立即应用
    if (name === this.currentTheme) {
      this.apply(name)
    }
  }

  /**
   * 应用主题
   */
  apply(themeName: string, options: ThemeSwitchOptions = {}): void {
    const { persist = true, transition = true } = options

    if (!this.themes.has(themeName)) {
      console.warn(`Theme "${themeName}" not found, falling back to current`)
      return
    }

    // 添加过渡效果
    if (transition) {
      document.documentElement.style.transition = 'background-color 0.3s, color 0.3s'
      setTimeout(() => {
        document.documentElement.style.transition = ''
      }, 300)
    }

    // 设置 data-theme 属性
    document.documentElement.setAttribute('data-theme', themeName)
    this.currentTheme = themeName

    // 持久化
    if (persist) {
      this.saveTheme(themeName)
    }

    // 触发变更钩子
    this.hooks.onChange.forEach((fn) => fn(themeName))

    console.log(`[ThemeManager] Applied theme: ${themeName}`)
  }

  /**
   * 获取当前主题
   */
  getCurrent(): string {
    return this.currentTheme
  }

  /**
   * 获取主题配置
   */
  getTheme(name: string): ThemeConfig | undefined {
    return this.themes.get(name)
  }

  /**
   * 获取所有已注册主题
   */
  getThemes(): string[] {
    return Array.from(this.themes.keys())
  }

  /**
   * 获取 CSS 变量值
   */
  getCSSVariable(name: string): string {
    return getComputedStyle(document.documentElement).getPropertyValue(name).trim()
  }

  /**
   * 设置 CSS 变量（运行时覆盖）
   */
  setCSSVariable(name: string, value: string): void {
    document.documentElement.style.setProperty(name, value)
  }

  /**
   * 注册主题变更回调
   */
  onChange(callback: (theme: string) => void): () => void {
    this.hooks.onChange.push(callback)

    // 返回取消订阅函数
    return () => {
      const index = this.hooks.onChange.indexOf(callback)
      if (index > -1) {
        this.hooks.onChange.splice(index, 1)
      }
    }
  }

  /**
   * 持久化主题到 localStorage
   */
  private saveTheme(themeName: string): void {
    try {
      localStorage.setItem('blog-theme', themeName)
    } catch (e) {
      console.warn('[ThemeManager] Failed to save theme:', e)
    }
  }

  /**
   * 从 localStorage 恢复主题
   */
  private getSavedTheme(): string | null {
    try {
      return localStorage.getItem('blog-theme')
    } catch (e) {
      return null
    }
  }
}
```

**Verification:**

- 创建 `packages/core/src/__tests__/theme-manager.test.ts`
- 测试用例：
  - 注册主题
  - 切换主题
  - 持久化主题
  - 获取 CSS 变量
  - 变更回调

---

### Task 02-02-04: 创建 CSS Variables 基础框架

**Type:** implementation
**Requirements:** THEME-02

```css
/* packages/core/src/styles/variables.css */

/* =============================================
   基础变量（所有主题共享）
   ============================================= */

:root {
  /* 字体系列 */
  --font-sans:
    'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial,
    sans-serif;
  --font-mono: 'Fira Code', 'Consolas', 'Monaco', 'Andale Mono', monospace;

  /* 过渡 */
  --transition-fast: 150ms;
  --transition-normal: 300ms;
  --transition-slow: 500ms;
}

/* =============================================
   Classic 主题（默认）
   ============================================= */

[data-theme='classic'] {
  /* 颜色 */
  --color-primary: #2c5282;
  --color-secondary: #4a5568;
  --color-accent: #3182ce;
  --color-background: #ffffff;
  --color-surface: #f7fafc;
  --color-text: #1a202c;
  --color-text-secondary: #4a5568;
  --color-border: #e2e8f0;
  --color-error: #e53e3e;
  --color-success: #38a169;
  --color-warning: #dd6b20;

  /* 排版 */
  --font-size-base: 16px;
  --line-height-base: 1.5;
  --font-weight-base: 400;
  --letter-spacing-base: normal;

  /* 间距 */
  --spacing-unit: 8px;
  --spacing-xs: calc(var(--spacing-unit) * 0.5);
  --spacing-sm: calc(var(--spacing-unit) * 1);
  --spacing-md: calc(var(--spacing-unit) * 2);
  --spacing-lg: calc(var(--spacing-unit) * 3);
  --spacing-xl: calc(var(--spacing-unit) * 4);

  /* 圆角 */
  --radius-none: 0;
  --radius-sm: 2px;
  --radius-md: 4px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-full: 9999px;

  /* 阴影 */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
}

/* =============================================
   Minimal 主题
   ============================================= */

[data-theme='minimal'] {
  /* 颜色 - 极简黑白 */
  --color-primary: #000000;
  --color-secondary: #333333;
  --color-accent: #666666;
  --color-background: #f5f5f5;
  --color-surface: #ffffff;
  --color-text: #1a1a1a;
  --color-text-secondary: #666666;
  --color-border: #e0e0e0;
  --color-error: #d32f2f;
  --color-success: #388e3c;
  --color-warning: #f57c00;

  /* 排版 - 更紧凑 */
  --font-size-base: 15px;
  --line-height-base: 1.6;
  --font-weight-base: 400;
  --letter-spacing-base: 0.02em;

  /* 间距 - 更小的单位 */
  --spacing-unit: 4px;
  --spacing-xs: calc(var(--spacing-unit) * 0.5);
  --spacing-sm: calc(var(--spacing-unit) * 1);
  --spacing-md: calc(var(--spacing-unit) * 2);
  --spacing-lg: calc(var(--spacing-unit) * 3);
  --spacing-xl: calc(var(--spacing-unit) * 4);

  /* 圆角 - 无圆角 */
  --radius-none: 0;
  --radius-sm: 0;
  --radius-md: 0;
  --radius-lg: 0;
  --radius-xl: 0;
  --radius-full: 9999px;

  /* 阴影 - 更淡 */
  --shadow-sm: 0 1px 1px 0 rgba(0, 0, 0, 0.08);
  --shadow-md: 0 2px 4px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 4px 8px -2px rgba(0, 0, 0, 0.1);
}
```

**Verification:**

- CSS 文件语法正确
- 可以在浏览器中正常应用

---

### Task 02-02-05: 创建 Classic 主题骨架

**Type:** implementation
**Requirements:** THEME-03

```json
/* themes/classic/theme.json */
{
  "metadata": {
    "name": "classic",
    "displayName": "经典博客",
    "version": "0.1.0",
    "author": "My Blog Team",
    "description": "经典博客风格 - 温暖、专业、易读"
  },
  "colors": {
    "primary": "#2c5282",
    "secondary": "#4a5568",
    "accent": "#3182ce",
    "background": "#ffffff",
    "surface": "#f7fafc",
    "text": "#1a202c",
    "textSecondary": "#4a5568",
    "border": "#e2e8f0",
    "error": "#e53e3e",
    "success": "#38a169",
    "warning": "#dd6b20"
  },
  "typography": {
    "fontFamily": "Inter, system-ui, sans-serif",
    "fontSize": "16px",
    "lineHeight": 1.5,
    "fontWeight": "400"
  },
  "spacing": {
    "unit": "8px",
    "scale": 1.25
  },
  "radius": {
    "sm": "2px",
    "md": "4px",
    "lg": "8px",
    "xl": "12px",
    "full": "9999px"
  }
}
```

```typescript
/* themes/classic/index.ts */
import type { ThemeConfig } from '@my-blog/core/theme'
import themeJson from './theme.json'

export const classicTheme: ThemeConfig = themeJson

export default classicTheme
```

**Verification:**

- JSON 格式正确
- TypeScript 类型检查通过

---

### Task 02-02-06: 创建 Minimal 主题骨架

**Type:** implementation
**Requirements:** THEME-03

```json
/* themes/minimal/theme.json */
{
  "metadata": {
    "name": "minimal",
    "displayName": "极简主义",
    "version": "0.1.0",
    "author": "My Blog Team",
    "description": "极简黑白风格 - 专注内容、无干扰"
  },
  "colors": {
    "primary": "#000000",
    "secondary": "#333333",
    "accent": "#666666",
    "background": "#f5f5f5",
    "surface": "#ffffff",
    "text": "#1a1a1a",
    "textSecondary": "#666666",
    "border": "#e0e0e0",
    "error": "#d32f2f",
    "success": "#388e3c",
    "warning": "#f57c00"
  },
  "typography": {
    "fontFamily": "Inter, system-ui, sans-serif",
    "fontSize": "15px",
    "lineHeight": 1.6,
    "fontWeight": "400"
  },
  "spacing": {
    "unit": "4px",
    "scale": 1.2
  },
  "radius": {
    "sm": "0",
    "md": "0",
    "lg": "0",
    "xl": "0",
    "full": "9999px"
  }
}
```

```typescript
/* themes/minimal/index.ts */
import type { ThemeConfig } from '@my-blog/core/theme'
import themeJson from './theme.json'

export const minimalTheme: ThemeConfig = themeJson

export default minimalTheme
```

**Verification:**

- JSON 格式正确
- TypeScript 类型检查通过

---

### Task 02-02-07: 创建主题包入口文件

**Type:** implementation
**Requirements:** THEME-01

```typescript
// packages/core/src/theme/index.ts
export { ThemeManager } from './ThemeManager'

export type {
  ThemeConfig,
  ThemeMetadata,
  ThemeColors,
  ThemeTypography,
  ThemeSpacing,
  ThemeRadius,
  ThemeSwitchOptions,
} from './types'
```

```typescript
// packages/core/src/index.ts
// 导出主题模块
export * from './theme'
```

**Verification:**

- 所有导出类型正确
- 可以从外部导入

---

### Task 02-02-08: 集成到 Nuxt 应用

**Type:** implementation
**Requirements:** THEME-01, THEME-02

```typescript
// apps/site/plugins/theme.ts
import { ThemeManager } from '@my-blog/core/theme'
import classicTheme from '../../../../themes/classic/theme.json'
import minimalTheme from '../../../../themes/minimal/theme.json'

export default defineNuxtPlugin(() => {
  const themeManager = new ThemeManager('classic')

  // 注册主题
  themeManager.register('classic', classicTheme)
  themeManager.register('minimal', minimalTheme)

  // 提供到 Nuxt 应用
  return {
    provide: {
      theme: themeManager,
    },
  }
})
```

```typescript
// apps/site/composables/useTheme.ts
export const useTheme = () => {
  const nuxtApp = useNuxtApp()
  return nuxtApp.$theme as ThemeManager
}
```

**Verification:**

- 在组件中可以调用 `useTheme()`
- 主题切换功能正常

---

</tasks>

---

## Verification Criteria

| Task     | Verification Command         | Expected Result |
| -------- | ---------------------------- | --------------- |
| 02-02-01 | 手动检查目录                 | 目录结构完整    |
| 02-02-02 | `pnpm type-check`            | 类型检查通过    |
| 02-02-03 | `pnpm test -- theme-manager` | 测试通过        |
| 02-02-04 | 手动检查                     | CSS 应用正常    |
| 02-02-05 | `pnpm type-check`            | 类型检查通过    |
| 02-02-06 | `pnpm type-check`            | 类型检查通过    |
| 02-02-07 | `pnpm type-check`            | 导出正确        |
| 02-02-08 | 启动应用测试                 | 主题切换正常    |

---

## Wave Summary

**Wave 2:** Depends on Plan 01 completion.

```
Task 02-02-01 ─┬─> Task 02-02-02 ─> Task 02-02-03 ─> Task 02-02-07 ─> Task 02-02-08
               ├─> Task 02-02-04 ──────────────────────────────┘
               ├─> Task 02-02-05 (independent)
               └─> Task 02-02-06 (independent)
```

---

_Plan created: 2026-03-17_
_Phase 2 Plan 02 of 02_
