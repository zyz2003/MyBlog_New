# Phase 5: 后台管理 - Plan 02/08

**Wave:** 1 - 认证基础设施
**Dependencies:** 05-01-PLAN.md
**Files Modified:** apps/site/stores/, apps/site/middleware/, apps/site/pages/admin/login.vue
**Autonomous:** true

---

## Objective

实现后台认证系统，包括登录页面、Pinia Auth Store、路由中间件、以及 API 集成。

---

## UI/UX Design Flow

**本 Plan 包含登录页面 UI，需使用 Pencil + Skills 组合工作流程：**

| 步骤 | 工具 | 用途 |
|------|------|------|
| 1. 整体布局 | `mcp__pencil__batch_design` | 创建登录页线框图（居中卡片布局） |
| 2. 设计灵感 | `ui-ux-pro-max` | 获取登录页设计灵感、交互模式参考 |
| 3. 代码实现 | `frontend-design` | 生成高质量 Vue 3 + Tailwind 登录页代码 |
| 4. 截图验证 | `mcp__pencil__get_screenshot` | 对比设计稿和实现效果 |

**设计文件位置：** `docs/design/phase-05/` 目录存放 .pen 设计文件

---

## Tasks

### Task 1: 创建 Auth Store

**文件：** `apps/site/stores/auth.ts`

```typescript
import { defineStore } from 'pinia'
import type { UserObject } from '~/server/services/auth.service'

export interface AuthState {
  token: string | null
  user: UserObject | null
  isLoggedIn: boolean
}

export const useAuthStore = defineStore('auth', {
  state: (): AuthState => ({
    token: null,
    user: null,
    isLoggedIn: false,
  }),

  getters: {
    currentUser: (state) => state.user,
    authToken: (state) => state.token,
    isAuthenticated: (state) => state.isLoggedIn,
  },

  actions: {
    setAuth(token: string, user: UserObject) {
      this.token = token
      this.user = user
      this.isLoggedIn = true
    },

    logout() {
      this.token = null
      this.user = null
      this.isLoggedIn = false
      return $fetch('/api/v1/auth/logout', { method: 'POST' })
    },
  },

  persist: {
    storage: persistedState.localStorage,
    paths: ['token', 'user'],
  },
})
```

**测试：** `apps/site/tests/stores/auth.test.ts`
- Store 初始化
- setAuth 动作
- logout 动作
- persist 持久化

### Task 2: 创建认证中间件

**文件：** `apps/site/middleware/auth.global.ts`

```typescript
export default defineNuxtRouteMiddleware(async (to) => {
  // 只保护 /admin 路由
  if (!to.path.startsWith('/admin')) {
    return
  }

  // 跳过登录页
  if (to.path === '/admin/login') {
    return
  }

  const authStore = useAuthStore()

  if (!authStore.token) {
    return navigateTo(`/admin/login?redirect=${encodeURIComponent(to.path)}`)
  }

  // 验证 token 有效性
  try {
    await $fetch('/api/v1/auth/me', {
      headers: {
        Authorization: `Bearer ${authStore.token}`,
      },
    })
  } catch (error) {
    authStore.logout()
    return navigateTo(`/admin/login?redirect=${encodeURIComponent(to.path)}`)
  }
})
```

**测试：** `apps/site/tests/middleware/auth.test.ts`
- 未登录重定向
- Token 有效时通过
- Token 失效时重定向

### Task 3: 创建 useAuth Composable

**文件：** `apps/site/composables/useAuth.ts`

```typescript
export function useAuth() {
  const authStore = useAuthStore()
  const toast = useToast()

  const getAuthHeaders = () => ({
    headers: {
      Authorization: authStore.token ? `Bearer ${authStore.token}` : '',
    },
  })

  const login = async (username: string, password: string) => {
    try {
      const response = await $fetch('/api/v1/auth/login', {
        method: 'POST',
        body: { username, password },
      })

      if (response.success) {
        authStore.setAuth(response.data.token, response.data.user)
        toast({
          title: '登录成功',
          description: `欢迎回来，${response.data.user.username}`,
        })
        return response.data
      }
    } catch (error) {
      toast({
        title: '登录失败',
        description: error instanceof Error ? error.message : '用户名或密码错误',
        variant: 'destructive',
      })
      throw error
    }
  }

  const logout = async () => {
    await authStore.logout()
    toast({
      title: '已退出登录',
    })
  }

  return {
    login,
    logout,
    getAuthHeaders,
  }
}
```

### Task 4: 创建登录页面

**文件：** `apps/site/pages/admin/login.vue`

使用 shadcn-vue 组件：
- Card 容器
- Input 输入框
- Button 按钮
- Form 表单验证 (VeeValidate)

**功能：**
- 用户名/密码表单
- Zod schema 验证
- 登录成功跳转到原页面
- 登录失败 Toast 提示

**测试：** `apps/site/tests/pages/admin/login.test.ts`

### Task 5: 创建 API 工具函数

**文件：** `apps/site/utils/api.ts`

```typescript
export function createApiFetch() {
  const authStore = useAuthStore()

  const getAuthHeaders = () => ({
    headers: {
      Authorization: authStore.token ? `Bearer ${authStore.token}` : '',
    },
  })

  const fetchApi = async <T>(
    url: string,
    options?: RequestInit
  ): Promise<T | null> => {
    try {
      return await $fetch(url, {
        ...options,
        ...getAuthHeaders(),
      })
    } catch (error) {
      throw error
    }
  }

  return {
    fetchApi,
    getAuthHeaders,
  }
}
```

### Task 6: 登出功能测试

**文件：** `apps/site/tests/auth/logout-flow.test.ts`

测试场景：
- 登录后存储 token
- 页面刷新 token 保持
- 登出后清空存储
- 登出后访问保护路由重定向

---

## Must Haves

- [ ] Auth Store 带持久化
- [ ] 全局认证中间件保护 /admin/* 路由
- [ ] useAuth composable 封装登录登出
- [ ] 登录页面表单验证完整
- [ ] API 工具函数可用
- [ ] 登出流程测试通过

---

## Verification

```bash
# 运行认证相关测试
pnpm -C apps/site test:run tests/stores/auth.test.ts
pnpm -C apps/site test:run tests/middleware/auth.test.ts
pnpm -C apps/site test:run tests/pages/admin/login.test.ts
```

---

## Output

**Created:**
- `apps/site/stores/auth.ts` - 认证 Store
- `apps/site/middleware/auth.global.ts` - 认证中间件
- `apps/site/composables/useAuth.ts` - 认证 Composable
- `apps/site/pages/admin/login.vue` - 登录页
- `apps/site/utils/api.ts` - API 工具
- `apps/site/tests/stores/auth.test.ts` - Store 测试
- `apps/site/tests/middleware/auth.test.ts` - 中间件测试
- `apps/site/tests/pages/admin/login.test.ts` - 页面测试
- `apps/site/tests/auth/logout-flow.test.ts` - 登出流程测试

---

*Phase: 05-admin, Plan: 02/08*
*Created: 2026-03-23*
