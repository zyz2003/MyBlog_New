# Phase 5: 后台管理 - Plan 06/08

**Wave:** 2 - 分类与标签管理
**Dependencies:** 05-CONTEXT.md, 05-01-PLAN.md (后台布局), 05-02-PLAN.md (登录认证), 05-03-PLAN.md (文章管理), 05-04-PLAN.md (文章编辑器), 05-05-PLAN.md (媒体库)
**Files Modified:** `apps/site/components/admin/categories/`, `apps/site/components/admin/tags/`, `apps/site/pages/admin/categories/`, `apps/site/pages/admin/tags/`
**Autonomous:** true

---

## Objective

实现分类和标签管理功能，包括分类树形结构展示、拖拽排序、嵌套分类管理、标签云可视化、颜色选择器等功能，为文章提供完整的分类和标签管理体系。

---

## UI/UX Design Flow

**本 Plan 包含复杂 UI 组件，必须使用 Pencil + Skills 组合工作流程：**

| 步骤 | 工具 | 用途 |
|------|------|------|
| 1. 整体布局 | `mcp__pencil__batch_design` | 创建分类树、标签列表线框图 |
| 2. 设计灵感 | `ui-ux-pro-max` | 获取树形组件、拖拽交互、标签云设计灵感 |
| 3. 代码实现 | `frontend-design` | 生成高质量 Vue 3 + Tailwind 组件代码 |
| 4. 主题配色 | `theme-factory` | 定义树形节点、标签配色方案 |
| 5. 截图验证 | `mcp__pencil__get_screenshot` | 对比设计稿和实现效果 |
| 6. 代码审查 | `simplify` | 审查组件代码质量、优化复用 |

**设计文件位置：** `docs/design/phase-05/` 目录存放 .pen 设计文件

---

## Tasks

### Task 1: 创建分类树形表格组件

**文件：** `apps/site/components/admin/categories/CategoryTreeTable.vue`

**使用组件：**
- shadcn-vue Table, Button, DropdownMenu, Input, Badge
- lucide-vue-next 图标（ChevronRight, ChevronDown, Folder, FolderOpen, MoreVertical, Pencil, Trash2, GripVertical）
- @dnd-kit/core, @dnd-kit/sortable (拖拽排序)

**功能：**
- 树形结构展示分类（支持无限层级嵌套）
- 展开/收起子分类
- 拖拽排序（支持同级排序和跨层级移动）
- 行内操作按钮（编辑、删除、添加子分类）
- 空状态显示（无分类时显示插画和创建引导）
- 批量操作（批量删除）
- 显示每个分类的文章数量

**设计决策：**
- 根节点缩进 0px，每层级增加 24px 缩进
- 拖拽时显示拖拽线和目标位置预览
- 有子分类的分类项无法直接删除（需先删除子分类）
- 使用 @dnd-kit 实现平滑拖拽体验

**API 集成：**
- GET /api/v1/categories?tree=true (获取树形结构)
- POST /api/v1/categories/reorder (拖拽排序后更新顺序)

**代码片段：**
```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useDroppable } from '@dnd-kit/core'
import { arrayMove, useSortable } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
import type { Category } from '@my-blog/database'

interface CategoryTreeNode extends Category {
  children: CategoryTreeNode[]
  depth: number
  articleCount: number
}

const props = defineProps<{
  categories: CategoryTreeNode[]
  loading?: boolean
}>()

const emit = defineEmits<{
  'edit': [category: CategoryTreeNode]
  'delete': [category: CategoryTreeNode]
  'addSubCategory': [parent: CategoryTreeNode]
  'reorder': [fromId: string, toId: string, dropPosition: 'before' | 'after' | 'inside']
}>()

// 展开状态管理
const expandedIds = ref<Set<string>>(new Set())

const toggleExpand = (id: string) => {
  if (expandedIds.value.has(id)) {
    expandedIds.value.delete(id)
  } else {
    expandedIds.value.add(id)
  }
}

// 拖拽处理
const handleDragEnd = (event: DragEndEvent) => {
  const { active, over } = event
  if (!over || active.id === over.id) return

  // 计算拖拽目标位置
  const dropPosition = calculateDropPosition(active.id, over.id)
  emit('reorder', active.id as string, over.id as string, dropPosition)
}
</script>

<template>
  <DndContext @dragEnd="handleDragEnd">
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead class="w-[40px]"></TableHead>
          <TableHead>名称</TableHead>
          <TableHead>别名</TableHead>
          <TableHead>描述</TableHead>
          <TableHead class="w-[100px]">文章数</TableHead>
          <TableHead class="w-[120px]">操作</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <draggable
          v-model="categories"
          item-key="id"
          handle=".drag-handle"
        >
          <template #item="{ element }">
            <CategoryTreeRow
              :category="element"
              :expanded="expandedIds.has(element.id)"
              @toggle-expand="toggleExpand(element.id)"
              @edit="emit('edit', element)"
              @delete="emit('delete', element)"
              @add-sub="emit('addSubCategory', element)"
            />
          </template>
        </draggable>
      </TableBody>
    </Table>
  </DndContext>
</template>
```

**测试：** `apps/site/tests/components/admin/categories/CategoryTreeTable.test.ts`

---

### Task 2: 创建分类行组件（递归）

**文件：** `apps/site/components/admin/categories/CategoryTreeRow.vue`

**使用组件：**
- shadcn-vue TableRow, TableCell, Button, Badge
- lucide-vue-next 图标（ChevronRight, ChevronDown, Folder, GripVertical）

**功能：**
- 递归渲染分类行（包含子分类）
- 展开/收起按钮（有子分类时显示）
- 拖拽手柄
- 分类名称和颜色指示器
- 文章数量显示
- 悬停显示操作按钮

**设计决策：**
- 使用递归组件实现无限层级
- 每层级缩进通过 :style 动态计算
- 分类名称前显示颜色圆点（如有颜色）
- 拖拽手柄仅在悬停时显示

**代码片段：**
```vue
<script setup lang="ts">
import type { CategoryTreeNode } from './CategoryTreeTable.vue'

defineProps<{
  category: CategoryTreeNode
  expanded?: boolean
  depth?: number
}>()

const emit = defineEmits<{
  'toggleExpand': []
  'edit': []
  'delete': []
  'addSub': []
}>()

const indent = computed(() => props.depth ? props.depth * 24 : 0)
</script>

<template>
  <TableRow class="group hover:bg-muted/50">
    <TableCell :style="{ paddingLeft: `${indent + 16}px` }">
      <div class="flex items-center gap-2">
        <button
          v-if="category.children?.length > 0"
          @click="emit('toggleExpand')"
          class="p-1 hover:bg-muted rounded"
        >
          <ChevronRight
            v-if="!expanded"
            class="w-4 h-4"
          />
          <ChevronDown
            v-else
            class="w-4 h-4"
          />
        </button>
        <span
          v-else
          class="w-6"
        ></span>
        <GripVertical class="w-4 h-4 text-muted-foreground drag-handle cursor-grab opacity-0 group-hover:opacity-100" />
        <span
          v-if="category.color"
          class="w-3 h-3 rounded-full"
          :style="{ backgroundColor: category.color }"
        ></span>
        <Folder
          v-if="category.children?.length > 0"
          class="w-4 h-4 text-primary"
        />
        <span class="text-sm font-medium">{{ category.name }}</span>
      </div>
    </TableCell>
    <TableCell>
      <code class="text-xs text-muted-foreground">{{ category.slug }}</code>
    </TableCell>
    <TableCell class="max-w-[200px] truncate text-muted-foreground">
      {{ category.description || '-' }}
    </TableCell>
    <TableCell>
      <Badge variant="secondary">{{ category.articleCount || 0 }}</Badge>
    </TableCell>
    <TableCell>
      <div class="flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity">
        <Button variant="ghost" size="icon" @click="emit('addSub')">
          <Plus class="w-4 h-4" />
        </Button>
        <Button variant="ghost" size="icon" @click="emit('edit')">
          <Pencil class="w-4 h-4" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          @click="emit('delete')"
          :disabled="category.children?.length > 0"
        >
          <Trash2 class="w-4 h-4" />
        </Button>
      </div>
    </TableCell>
  </TableRow>
  <CategoryTreeRow
    v-if="expanded && category.children?.length > 0"
    v-for="child in category.children"
    :key="child.id"
    :category="child"
    :depth="(depth || 0) + 1"
    @toggle-expand="toggleExpand(child.id)"
    @edit="emit('edit')"
    @delete="emit('delete')"
    @add-sub="emit('addSub')"
  />
</template>
```

**测试：** `apps/site/tests/components/admin/categories/CategoryTreeRow.test.ts`

---

### Task 3: 创建分类表单组件

**文件：** `apps/site/components/admin/categories/CategoryForm.vue`

**使用组件：**
- shadcn-vue Dialog, DialogContent, Form, FormControl, FormField, FormItem, FormLabel, FormMessage, Input, Textarea, Select, Button
- lucide-vue-next 图标（X）
- vue-color-kit (颜色选择器)

**功能：**
- 新增/编辑分类表单
- 字段：名称、别名、父分类、描述、颜色
- 别名自动生成（从名称转换，可手动编辑）
- 实时验证（名称必填、别名唯一性检查）
- 父分类选择（树形下拉选择器，排除当前分类和子分类）
- 颜色选择器（预设色板 + 自定义颜色）
- 表单提交处理

**设计决策：**
- 名称输入时自动转换拼音生成别名
- 别名实时检查是否重复（防抖 API 请求）
- 父分类选择器显示树形结构
- 颜色选择提供预设色板和自定义输入

**API 集成：**
- POST /api/v1/categories (新增)
- PUT /api/v1/categories/:id (编辑)
- GET /api/v1/categories?tree=true (获取父分类选项)
- GET /api/v1/categories/check-slug?slug=xxx (检查别名唯一性)

**代码片段：**
```vue
<script setup lang="ts">
import { ref, computed, watch } from 'vue'
import { useForm } from 'vee-validate'
import { toTypedSchema } from '@vee-validate/zod'
import * as z from 'zod'
import { useDebounceFn } from '@vueuse/core'
import type { Category } from '@my-blog/database'

const props = defineProps<{
  open: boolean
  category?: Category | null
  categories: Category[]
}>()

const emit = defineEmits<{
  'update:open': [value: boolean]
  'submit': [data: FormData]
}>()

const formSchema = toTypedSchema(z.object({
  name: z.string().min(1, '分类名称不能为空').max(50, '分类名称最多 50 个字符'),
  slug: z.string().min(1, '别名不能为空').regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, '别名只能包含小写字母、数字和连字符'),
  parentId: z.string().nullable().optional(),
  description: z.string().max(200, '描述最多 200 个字符').optional(),
  color: z.string().regex(/^#[0-9A-Fa-f]{6}$/, '请选择有效的颜色').optional(),
}))

const { handleSubmit, setFieldValue, values, errors } = useForm({
  validationSchema: formSchema,
  initialValues: {
    name: props.category?.name || '',
    slug: props.category?.slug || '',
    parentId: props.category?.parentId || null,
    description: props.category?.description || '',
    color: props.category?.color || '#3B82F6',
  },
})

// 别名自动生成
watch(() => values.name, (newName) => {
  if (!props.category || newName !== props.category.name) {
    setFieldValue('slug', generateSlug(newName))
  }
})

// 别名唯一性检查
const slugError = ref('')
const checkSlugAvailability = useDebounceFn(async (slug: string) => {
  if (!slug) return
  try {
    await $fetch(`/api/v1/categories/check-slug?slug=${slug}`)
    slugError.value = ''
  } catch {
    slugError.value = '该别名已被使用'
  }
}, 300)

const onSubmit = handleSubmit((data) => {
  emit('submit', data)
})

// 中文转拼音别名
const generateSlug = (name: string): string => {
  // 简单实现：移除特殊字符，替换空格为连字符
  return name
    .toLowerCase()
    .replace(/[^a-z0-9\u4e00-\u9fa5]/g, '')
    .replace(/\s+/g, '-')
}
</script>

<template>
  <Dialog :open="props.open" @update:open="emit('update:open', $event)">
    <DialogContent class="sm:max-w-[500px]">
      <DialogHeader>
        <DialogTitle>{{ category ? '编辑分类' : '新增分类' }}</DialogTitle>
      </DialogHeader>
      <form @submit="onSubmit" class="space-y-4">
        <FormField v-slot="{ componentField }" name="name">
          <FormItem>
            <FormLabel>分类名称</FormLabel>
            <FormControl>
              <Input
                v-bind="componentField"
                placeholder="请输入分类名称"
                @input="checkSlugAvailability(values.slug)"
              />
            </FormControl>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="slug">
          <FormItem>
            <FormLabel>别名</FormLabel>
            <FormControl>
              <div class="relative">
                <Input
                  v-bind="componentField"
                  placeholder="category-name"
                  :class="{ 'border-red-500': slugError }"
                />
                <span
                  v-if="slugError"
                  class="absolute -bottom-5 right-0 text-xs text-red-500"
                >
                  {{ slugError }}
                </span>
              </div>
            </FormControl>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="parentId">
          <FormItem>
            <FormLabel>父分类</FormLabel>
            <Select v-bind="componentField">
              <FormControl>
                <select class="w-full rounded-md border p-2">
                  <option :value="null">无（作为顶级分类）</option>
                  <option
                    v-for="cat in categories"
                    :key="cat.id"
                    :value="cat.id"
                    :disabled="cat.id === category?.id"
                  >
                    {{ '  '.repeat(cat.depth || 0) }}{{ cat.name }}
                  </option>
                </select>
              </FormControl>
            </Select>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="description">
          <FormItem>
            <FormLabel>描述</FormLabel>
            <FormControl>
              <Textarea
                v-bind="componentField"
                placeholder="分类描述（可选）"
                rows="3"
              />
            </FormControl>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="color">
          <FormItem>
            <FormLabel>颜色</FormLabel>
            <div class="flex items-center gap-4">
              <FormControl>
                <input
                  type="color"
                  v-bind="componentField"
                  class="w-12 h-10 rounded cursor-pointer"
                />
              </FormControl>
              <Input
                v-model="values.color"
                placeholder="#3B82F6"
                class="w-32"
              />
            </div>
            <!-- 预设色板 -->
            <div class="flex gap-2 mt-2">
              <span
                v-for="color in presetColors"
                :key="color"
                class="w-6 h-6 rounded cursor-pointer border border-gray-200"
                :style="{ backgroundColor: color }"
                @click="setFieldValue('color', color)"
              ></span>
            </div>
            <FormMessage />
          </FormItem>
        </FormField>

        <div class="flex justify-end gap-2">
          <Button type="button" variant="outline" @click="emit('update:open', false)">
            取消
          </Button>
          <Button type="submit">
            {{ category ? '保存' : '创建' }}
          </Button>
        </div>
      </form>
    </DialogContent>
  </Dialog>
</template>
```

**测试：** `apps/site/tests/components/admin/categories/CategoryForm.test.ts`

---

### Task 4: 创建分类管理页面

**文件：** `apps/site/pages/admin/categories/index.vue`

**使用组件：**
- AdminLayout
- CategoryTreeTable, CategoryForm
- shadcn-vue Button, Dialog, Badge, Separator, Breadcrumb
- lucide-vue-next 图标（Plus, FolderPlus)

**功能：**
- 页面标题和面包屑导航
- 创建分类按钮
- 分类树形表格展示
- 新增/编辑分类 Dialog
- 删除确认 Dialog
- 空状态显示
- Toast 提示（操作成功/失败）

**Pinia Store 设计：**
```typescript
// stores/categoryAdmin.ts
import { defineStore } from 'pinia'
import type { Category } from '@my-blog/database'

interface CategoryAdminState {
  categories: Category[]
  loading: boolean
  dialogOpen: boolean
  editingCategory: Category | null
}

export const useCategoryAdminStore = defineStore('categoryAdmin', {
  state: (): CategoryAdminState => ({
    categories: [],
    loading: false,
    dialogOpen: false,
    editingCategory: null,
  }),
  actions: {
    async fetchCategories() {
      this.loading = true
      try {
        const data = await $fetch('/api/v1/categories?tree=true')
        this.categories = data
      } finally {
        this.loading = false
      }
    },
    openCreateDialog() {
      this.editingCategory = null
      this.dialogOpen = true
    },
    openEditDialog(category: Category) {
      this.editingCategory = category
      this.dialogOpen = true
    },
  },
})
```

**API 集成：**
- GET /api/v1/categories?tree=true
- POST /api/v1/categories
- PUT /api/v1/categories/:id
- DELETE /api/v1/categories/:id
- POST /api/v1/categories/reorder

**测试：** `apps/site/tests/pages/admin/categories/index.test.ts`

---

### Task 5: 创建标签列表组件

**文件：** `apps/site/components/admin/tags/TagList.vue`

**使用组件：**
- shadcn-vue Table, Button, Badge, DropdownMenu, Input
- lucide-vue-next 图标（Pencil, Trash2, MoreVertical, Search, Tag)

**功能：**
- 标签表格展示
- 颜色徽章显示（每个标签显示对应颜色的 Badge）
- 使用频次统计显示
- 搜索框（按名称/别名搜索）
- 行内操作（编辑、删除）
- 分页支持
- 空状态显示

**设计决策：**
- 标签名称使用彩色 Badge 显示
- 频次徽章使用 secondary variant
- 搜索框带防抖
- 每页显示 20 条记录

**API 集成：**
- GET /api/v1/tags?search=&limit=&offset=
- DELETE /api/v1/tags/:id

**代码片段：**
```vue
<script setup lang="ts">
import { computed } from 'vue'
import { useDebounceFn } from '@vueuse/core'
import type { Tag } from '@my-blog/database'

interface TagWithCount extends Tag {
  usageCount: number
}

const props = defineProps<{
  tags: TagWithCount[]
  loading?: boolean
  pagination: {
    total: number
    current: number
    pageSize: number
  }
}>()

const emit = defineEmits<{
  'edit': [tag: TagWithCount]
  'delete': [tag: TagWithCount]
  'pageChange': [page: number]
  'search': [query: string]
}>()

const debouncedSearch = useDebounceFn((query: string) => {
  emit('search', query)
}, 300)
</script>

<template>
  <div class="space-y-4">
    <!-- 搜索栏 -->
    <div class="flex items-center gap-2">
      <div class="relative flex-1">
        <Search class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" />
        <Input
          placeholder="搜索标签..."
          class="pl-10"
          @input="debouncedSearch(($event.target as HTMLInputElement).value)"
        />
      </div>
    </div>

    <!-- 表格 -->
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>标签</TableHead>
          <TableHead>别名</TableHead>
          <TableHead>描述</TableHead>
          <TableHead class="w-[100px]">使用次数</TableHead>
          <TableHead class="w-[120px]">操作</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow
          v-for="tag in tags"
          :key="tag.id"
          class="group"
        >
          <TableCell>
            <Badge
              :style="{ backgroundColor: tag.color || '#64748b', color: 'white' }"
              class="text-sm px-3 py-1"
            >
              <Tag class="w-3 h-3 mr-1" />
              {{ tag.name }}
            </Badge>
          </TableCell>
          <TableCell>
            <code class="text-xs text-muted-foreground">{{ tag.slug }}</code>
          </TableCell>
          <TableCell class="max-w-[200px] truncate text-muted-foreground">
            {{ tag.description || '-' }}
          </TableCell>
          <TableCell>
            <Badge variant="outline">{{ tag.usageCount || 0 }}</Badge>
          </TableCell>
          <TableCell>
            <div class="flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity">
              <Button variant="ghost" size="icon" @click="emit('edit', tag)">
                <Pencil class="w-4 h-4" />
              </Button>
              <Button
                variant="ghost"
                size="icon"
                @click="emit('delete', tag)"
                :disabled="tag.usageCount > 0"
              >
                <Trash2 class="w-4 h-4" />
              </Button>
            </div>
          </TableCell>
        </TableRow>
        <TableRow v-if="tags.length === 0">
          <TableCell :colspan="5" class="h-[200px] text-center">
            <div class="flex flex-col items-center justify-center text-muted-foreground">
              <Tag class="w-12 h-12 mb-4 opacity-20" />
              <p>暂无标签</p>
              <p class="text-sm">点击右上角按钮创建第一个标签</p>
            </div>
          </TableCell>
        </TableRow>
      </TableBody>
    </Table>

    <!-- 分页 -->
    <div class="flex justify-between items-center">
      <p class="text-sm text-muted-foreground">
        共 {{ pagination.total }} 条
      </p>
      <Pagination
        :current="pagination.current"
        :total="pagination.total"
        :pageSize="pagination.pageSize"
        @change="emit('pageChange', $event)"
      />
    </div>
  </div>
</template>
```

**测试：** `apps/site/tests/components/admin/tags/TagList.test.ts`

---

### Task 6: 创建标签表单组件

**文件：** `apps/site/components/admin/tags/TagForm.vue`

**使用组件：**
- shadcn-vue Dialog, DialogContent, Form, FormControl, FormField, FormItem, FormLabel, FormMessage, Input, Textarea, Button
- lucide-vue-next 图标（X）

**功能：**
- 新增/编辑标签表单
- 字段：名称、别名、描述、颜色
- 别名自动生成（从名称转换，可手动编辑）
- 实时验证（名称必填、别名唯一性检查）
- 颜色选择器（预设色板 + 自定义颜色）
- 表单提交处理

**设计决策：**
- 与分类表单类似，但无父级选择
- 名称输入时自动转换拼音生成别名
- 别名实时检查是否重复
- 颜色选择提供预设色板和自定义输入

**API 集成：**
- POST /api/v1/tags (新增)
- PUT /api/v1/tags/:id (编辑)
- GET /api/v1/tags/check-slug?slug=xxx (检查别名唯一性)

**代码片段：**
```vue
<script setup lang="ts">
import { ref, watch } from 'vue'
import { useForm } from 'vee-validate'
import { toTypedSchema } from '@vee-validate/zod'
import * as z from 'zod'
import { useDebounceFn } from '@vueuse/core'
import type { Tag } from '@my-blog/database'

const props = defineProps<{
  open: boolean
  tag?: Tag | null
}>()

const emit = defineEmits<{
  'update:open': [value: boolean]
  'submit': [data: FormData]
}>()

const presetColors = [
  '#EF4444', '#F97316', '#F59E0B', '#84CC16',
  '#10B981', '#06B6D4', '#3B82F6', '#6366F1',
  '#8B5CF6', '#EC4899', '#F43F5E', '#64748b',
]

const formSchema = toTypedSchema(z.object({
  name: z.string().min(1, '标签名称不能为空').max(30, '标签名称最多 30 个字符'),
  slug: z.string().min(1, '别名不能为空').regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, '别名只能包含小写字母、数字和连字符'),
  description: z.string().max(100, '描述最多 100 个字符').optional(),
  color: z.string().regex(/^#[0-9A-Fa-f]{6}$/, '请选择有效的颜色').default('#3B82F6'),
}))

const { handleSubmit, setFieldValue, values } = useForm({
  validationSchema: formSchema,
  initialValues: {
    name: props.tag?.name || '',
    slug: props.tag?.slug || '',
    description: props.tag?.description || '',
    color: props.tag?.color || '#3B82F6',
  },
})

const slugError = ref('')
const checkSlugAvailability = useDebounceFn(async (slug: string) => {
  if (!slug) return
  try {
    await $fetch(`/api/v1/tags/check-slug?slug=${slug}`)
    slugError.value = ''
  } catch {
    slugError.value = '该别名已被使用'
  }
}, 300)

watch(() => values.name, (newName) => {
  if (!props.tag || newName !== props.tag.name) {
    setFieldValue('slug', generateSlug(newName))
  }
})

const onSubmit = handleSubmit((data) => {
  emit('submit', data)
})

const generateSlug = (name: string): string => {
  return name
    .toLowerCase()
    .replace(/[^a-z0-9\u4e00-\u9fa5]/g, '')
    .replace(/\s+/g, '-')
}
</script>

<template>
  <Dialog :open="props.open" @update:open="emit('update:open', $event)">
    <DialogContent class="sm:max-w-[450px]">
      <DialogHeader>
        <DialogTitle>{{ tag ? '编辑标签' : '新增标签' }}</DialogTitle>
      </DialogHeader>
      <form @submit="onSubmit" class="space-y-4">
        <FormField v-slot="{ componentField }" name="name">
          <FormItem>
            <FormLabel>标签名称</FormLabel>
            <FormControl>
              <Input v-bind="componentField" placeholder="请输入标签名称" />
            </FormControl>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="slug">
          <FormItem>
            <FormLabel>别名</FormLabel>
            <FormControl>
              <Input
                v-bind="componentField"
                placeholder="tag-name"
                :class="{ 'border-red-500': slugError }"
              />
            </FormControl>
            <span v-if="slugError" class="text-xs text-red-500">{{ slugError }}</span>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="description">
          <FormItem>
            <FormLabel>描述</FormLabel>
            <FormControl>
              <Textarea v-bind="componentField" placeholder="标签描述（可选）" rows="2" />
            </FormControl>
            <FormMessage />
          </FormItem>
        </FormField>

        <FormField v-slot="{ componentField }" name="color">
          <FormItem>
            <FormLabel>颜色</FormLabel>
            <div class="flex items-center gap-4">
              <FormControl>
                <input
                  type="color"
                  v-bind="componentField"
                  class="w-12 h-10 rounded cursor-pointer"
                />
              </FormControl>
              <Input v-model="values.color" placeholder="#3B82F6" class="w-32" />
            </div>
            <div class="flex gap-2 mt-2 flex-wrap">
              <span
                v-for="color in presetColors"
                :key="color"
                class="w-6 h-6 rounded cursor-pointer border border-gray-200"
                :style="{ backgroundColor: color }"
                @click="setFieldValue('color', color)"
              ></span>
            </div>
            <FormMessage />
          </FormItem>
        </FormField>

        <div class="flex justify-end gap-2">
          <Button type="button" variant="outline" @click="emit('update:open', false)">
            取消
          </Button>
          <Button type="submit">
            {{ tag ? '保存' : '创建' }}
          </Button>
        </div>
      </form>
    </DialogContent>
  </Dialog>
</template>
```

**测试：** `apps/site/tests/components/admin/tags/TagForm.test.ts`

---

### Task 7: 创建标签云组件

**文件：** `apps/site/components/admin/tags/TagCloud.vue`

**使用组件：**
- shadcn-vue Badge, Button
- lucide-vue-next 图标（Tag, TrendingUp)

**功能：**
- 可视化标签云展示
- 标签大小根据使用频率动态调整
- 颜色显示（使用标签自定义颜色）
- 点击标签可筛选/查看使用该标签的文章
- 显示 TOP 50 热门标签
- 支持按频次排序或按名称排序

**设计决策：**
- 字体大小范围：12px - 32px（根据频次计算）
- 颜色使用标签自定义颜色，无颜色则使用默认灰色
- 标签云使用弹性布局，标签随机旋转角度（-10° 到 10°）增加视觉效果
- 鼠标悬停显示频次提示

**API 集成：**
- GET /api/v1/tags/cloud?limit=50 (获取标签云数据)

**代码片段：**
```vue
<script setup lang="ts">
import { computed, ref } from 'vue'
import type { Tag } from '@my-blog/database'

interface TagCloudItem extends Tag {
  usageCount: number
}

const props = defineProps<{
  tags: TagCloudItem[]
  loading?: boolean
}>()

const emit = defineEmits<{
  'tagClick': [tag: TagCloudItem]
}>()

const sortBy = ref<'frequency' | 'name'>('frequency')

// 计算标签大小
const getFontSize = (count: number, maxCount: number, minCount: number): string => {
  const minSize = 12
  const maxSize = 32
  const range = maxCount - minCount || 1
  const ratio = (count - minCount) / range
  const size = minSize + (ratio * (maxSize - minSize))
  return `${Math.round(size)}px`
}

// 随机旋转角度
const getRandomRotation = (): number => {
  return Math.floor(Math.random() * 21) - 10 // -10 to 10
}

const sortedTags = computed(() => {
  const tags = [...props.tags]
  if (sortBy.value === 'frequency') {
    return tags.sort((a, b) => b.usageCount - a.usageCount)
  }
  return tags.sort((a, b) => a.name.localeCompare(b.name))
})

const stats = computed(() => {
  const counts = props.tags.map(t => t.usageCount)
  return {
    max: Math.max(...counts, 0),
    min: Math.min(...counts, 0),
    total: props.tags.reduce((sum, t) => sum + t.usageCount, 0),
  }
})
</script>

<template>
  <div class="space-y-4">
    <!-- 头部统计和排序 -->
    <div class="flex justify-between items-center">
      <div class="flex gap-4 text-sm text-muted-foreground">
        <span>标签总数：<strong>{{ tags.length }}</strong></span>
        <span>总使用次数：<strong>{{ stats.total }}</strong></span>
      </div>
      <div class="flex items-center gap-2">
        <span class="text-sm text-muted-foreground">排序：</span>
        <Button
          variant="outline"
          size="sm"
          :class="{ 'bg-primary text-primary-foreground': sortBy === 'frequency' }"
          @click="sortBy = 'frequency'"
        >
          <TrendingUp class="w-3 h-3 mr-1" />
          频次
        </Button>
        <Button
          variant="outline"
          size="sm"
          :class="{ 'bg-primary text-primary-foreground': sortBy === 'name' }"
          @click="sortBy = 'name'"
        >
          名称
        </Button>
      </div>
    </div>

    <!-- 标签云 -->
    <div
      v-if="tags.length > 0"
      class="flex flex-wrap gap-3 p-4 bg-muted/30 rounded-lg"
    >
      <span
        v-for="tag in sortedTags"
        :key="tag.id"
        class="inline-flex items-center cursor-pointer transition-transform hover:scale-110"
        :style="{
          fontSize: getFontSize(tag.usageCount, stats.max, stats.min),
          color: tag.color || '#64748b',
          transform: `rotate(${getRandomRotation()}deg)`,
        }"
        @click="emit('tagClick', tag)"
      >
        {{ tag.name }}
        <span
          class="ml-1 text-xs opacity-60"
          :style="{ fontSize: '0.5em' }"
        >
          ({{ tag.usageCount }})
        </span>
      </span>
    </div>

    <!-- 空状态 -->
    <div v-else class="flex flex-col items-center justify-center py-12 text-muted-foreground">
      <Tag class="w-12 h-12 mb-4 opacity-20" />
      <p>暂无标签数据</p>
      <p class="text-sm">创建文章并添加标签后，这里会显示标签云</p>
    </div>
  </div>
</template>
```

**测试：** `apps/site/tests/components/admin/tags/TagCloud.test.ts`

---

### Task 8: 创建标签管理页面

**文件：** `apps/site/pages/admin/tags/index.vue`

**使用组件：**
- AdminLayout
- TagList, TagForm, TagCloud
- shadcn-vue Button, Dialog, Tabs, TabsContent, TabsList, TabsTrigger
- lucide-vue-next 图标（Plus, Cloud, List)

**功能：**
- 页面标题和面包屑导航
- 创建标签按钮
- Tab 切换：列表视图 / 标签云视图
- 新增/编辑标签 Dialog
- 删除确认 Dialog
- Toast 提示（操作成功/失败）

**Pinia Store 设计：**
```typescript
// stores/tagAdmin.ts
import { defineStore } from 'pinia'
import type { Tag } from '@my-blog/database'

interface TagWithCount extends Tag {
  usageCount: number
}

interface TagAdminState {
  tags: TagWithCount[]
  loading: boolean
  dialogOpen: boolean
  editingTag: Tag | null
  pagination: {
    total: number
    current: number
    pageSize: number
  }
}

export const useTagAdminStore = defineStore('tagAdmin', {
  state: (): TagAdminState => ({
    tags: [],
    loading: false,
    dialogOpen: false,
    editingTag: null,
    pagination: { total: 0, current: 1, pageSize: 20 },
  }),
  actions: {
    async fetchTags(params: { page?: number; search?: string } = {}) {
      this.loading = true
      try {
        const query = new URLSearchParams({
          limit: this.pagination.pageSize.toString(),
          offset: ((params.page || 1) - 1) * this.pagination.pageSize,
        })
        if (params.search) query.append('search', params.search)

        const data = await $fetch(`/api/v1/tags?${query.toString()}`)
        this.tags = data.items
        this.pagination.total = data.total
        this.pagination.current = params.page || 1
      } finally {
        this.loading = false
      }
    },
    openCreateDialog() {
      this.editingTag = null
      this.dialogOpen = true
    },
    openEditDialog(tag: Tag) {
      this.editingTag = tag
      this.dialogOpen = true
    },
  },
})
```

**API 集成：**
- GET /api/v1/tags?limit=&offset=&search=
- POST /api/v1/tags
- PUT /api/v1/tags/:id
- DELETE /api/v1/tags/:id
- GET /api/v1/tags/cloud (标签云数据)

**测试：** `apps/site/tests/pages/admin/tags/index.test.ts`

---

## Must Haves

### 分类管理

- [ ] CategoryTreeTable 树形结构正确渲染
- [ ] 展开/收起子分类功能正常
- [ ] 拖拽排序功能正常（同级排序、跨层级移动）
- [ ] 拖拽后调用 reorder API 更新顺序
- [ ] CategoryForm 新增/编辑功能正常
- [ ] 别名自动生成（从名称转换）
- [ ] 别名唯一性实时验证
- [ ] 父分类选择器正确显示树形结构
- [ ] 颜色选择器工作正常
- [ ] 分类删除前校验（有子分类不可删除）
- [ ] 分类文章数量显示正确
- [ ] CategoryAdmin Store 状态管理正常

### 标签管理

- [ ] TagList 表格正确渲染
- [ ] 标签颜色 Badge 显示正确
- [ ] 使用频次统计显示正确
- [ ] 搜索功能正常（防抖处理）
- [ ] TagForm 新增/编辑功能正常
- [ ] 标签删除前校验（有文章使用不可删除）
- [ ] TagCloud 标签云可视化正确
- [ ] 标签大小根据频次动态调整
- [ ] 标签云排序切换正常
- [ ] TagAdmin Store 状态管理正常

### 通用

- [ ] Toast 提示正确显示
- [ ] 删除确认 Dialog 正常
- [ ] 空状态显示正确
- [ ] 错误处理完善
- [ ] 响应式布局正常

---

## Verification

```bash
# 运行分类管理相关测试
pnpm -C apps/site test:run tests/components/admin/categories/CategoryTreeTable.test.ts
pnpm -C apps/site test:run tests/components/admin/categories/CategoryTreeRow.test.ts
pnpm -C apps/site test:run tests/components/admin/categories/CategoryForm.test.ts
pnpm -C apps/site test:run tests/pages/admin/categories/index.test.ts

# 运行标签管理相关测试
pnpm -C apps/site test:run tests/components/admin/tags/TagList.test.ts
pnpm -C apps/site test:run tests/components/admin/tags/TagForm.test.ts
pnpm -C apps/site test:run tests/components/admin/tags/TagCloud.test.ts
pnpm -C apps/site test:run tests/pages/admin/tags/index.test.ts

# 类型检查
pnpm -C apps/site type-check

# 运行开发服务器手动测试
pnpm dev

# 手动测试检查项
# 1. 访问 /admin/categories 查看分类管理页面
# 2. 测试创建分类、编辑分类、删除分类
# 3. 测试拖拽排序功能
# 4. 测试父子分类层级关系
# 5. 访问 /admin/tags 查看标签管理页面
# 6. 测试创建标签、编辑标签、删除标签
# 7. 测试标签云可视化效果
# 8. 验证别名唯一性检查
```

---

## Output

**Created:**

### 分类管理组件

- `apps/site/components/admin/categories/CategoryTreeTable.vue` - 分类树形表格组件
- `apps/site/components/admin/categories/CategoryTreeRow.vue` - 分类行组件（递归）
- `apps/site/components/admin/categories/CategoryForm.vue` - 分类表单组件
- `apps/site/components/admin/categories/index.vue` - 分类管理页面（或放在 pages 目录）
- `apps/site/stores/categoryAdmin.ts` - 分类管理 Pinia Store
- `apps/site/composables/useCategoryAdmin.ts` - 分类管理 Composable

### 标签管理组件

- `apps/site/components/admin/tags/TagList.vue` - 标签列表组件
- `apps/site/components/admin/tags/TagForm.vue` - 标签表单组件
- `apps/site/components/admin/tags/TagCloud.vue` - 标签云可视化组件
- `apps/site/stores/tagAdmin.ts` - 标签管理 Pinia Store
- `apps/site/composables/useTagAdmin.ts` - 标签管理 Composable

### 页面

- `apps/site/pages/admin/categories/index.vue` - 分类管理页面
- `apps/site/pages/admin/tags/index.vue` - 标签管理页面

### 测试文件

- `apps/site/tests/components/admin/categories/CategoryTreeTable.test.ts`
- `apps/site/tests/components/admin/categories/CategoryTreeRow.test.ts`
- `apps/site/tests/components/admin/categories/CategoryForm.test.ts`
- `apps/site/tests/pages/admin/categories/index.test.ts`
- `apps/site/tests/components/admin/tags/TagList.test.ts`
- `apps/site/tests/components/admin/tags/TagForm.test.ts`
- `apps/site/tests/components/admin/tags/TagCloud.test.ts`
- `apps/site/tests/pages/admin/tags/index.test.ts`

### 依赖安装

```bash
pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
pnpm add vee-validate @vee-validate/zod
```

---

*Phase: 05-admin, Plan: 06/08*
*Created: 2026-03-23*
