---
phase: 00-test-arch
plan: 04
type: audit
priority: critical
wave: 4
depends_on: []
autonomous: false
---

# Phase 00 Plan 04: Phase 4 API 层已完成计划审查

## Objective

审查 Phase 4 (API 层) 已完成计划 (04-00 至 04-07) 的测试，发现并修复"测试迁就"问题。

**Purpose:** 确保 API 测试基于 API 契约 (REQUIREMENTS.md)

**Output:** 审查报告，bug 修复列表

---

## Must-Haves

### Truths

- API 测试必须基于 REQUIREMENTS.md 中的 API 规范
- 测试验证响应格式，不迁就错误实现
- 中间件测试验证预期行为

### Artifacts

| Path                                           | Provides | Exports |
| ---------------------------------------------- | -------- | ------- |
| `.planning/phases/00-test-arch/00-04-AUDIT.md` | 审查报告 | -       |

---

## Tasks

### Task 1: Audit API test files

**Behavior:**

- 审查 `apps/site/tests/server/api/auth.test.ts`
- 验证测试基于 API 契约而非实现
- 识别"测试迁就"问题

**Action:**

1. Read auth.test.ts
2. Compare assertions against REQUIREMENTS.md API specs
3. Check if tests verify response format contract
4. Document any accommodation issues

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- auth
```

---

### Task 2: Audit service layer tests

**Behavior:**

- 审查 `apps/site/tests/server/services/*.test.ts`
- 验证测试使用正确的 mock 策略
- 验证 post.service.test.ts 已修复

**Action:**

1. Read all service test files
2. Check if they use shared test db utilities
3. Verify post.service.test.ts updatePost bug is fixed in service
4. Document findings

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- services
```

---

### Task 3: Audit middleware tests

**Behavior:**

- 审查 `apps/site/tests/server/middleware/*.test.ts`
- 验证中间件测试基于预期行为

**Action:**

1. Read middleware test files
2. Verify tests check expected behavior
3. Document any issues

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- middleware
```

---

## Verification

- [ ] API tests audited
- [ ] Service tests audited
- [ ] Middleware tests audited
- [ ] Audit report created
- [ ] Bugs documented and fixed

---

## Critical Rule

> **API 测试审查标准：**
>
> 1. 测试是否验证 API 契约 (响应格式、状态码)？
> 2. 测试失败是否暴露 API bug？
> 3. 测试是否基于 REQUIREMENTS.md 中的 API 规范？

---

## Output

After completion, create `.planning/phases/00-test-arch/00-04-SUMMARY.md` with audit findings.
