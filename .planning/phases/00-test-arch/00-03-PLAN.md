---
phase: 00-test-arch
plan: 03
type: audit
priority: high
wave: 3
depends_on: []
autonomous: true
---

# Phase 00 Plan 03: Phase 3 数据库层测试审查与增强

## Objective

审查 Phase 3 (数据库层) 的测试，确保 Schema 验证的完整性。

**Purpose:** 验证数据库 Schema 和迁移脚本的正确性

**Output:** 审查报告，可能的新增测试

---

## Must-Haves

### Truths

- Schema 验证必须基于业务需求
- 迁移脚本必须可重复执行 (幂等性)
- Schema 变更必须向后兼容

### Artifacts

| Path                                           | Provides            | Exports |
| ---------------------------------------------- | ------------------- | ------- |
| `.planning/phases/00-test-arch/00-03-AUDIT.md` | 审查报告            | -       |
| `tests/migration-idempotency.test.ts`          | 迁移幂等性验证      | -       |
| `tests/schema-backward-compat.test.ts`         | Schema 向后兼容验证 | -       |

---

## Tasks

### Task 1: Audit existing schema tests

**Behavior:**

- 审查 `apps/site/tests/schema-sync.test.ts`
- 审查 `scripts/verify-schema.ts`
- 验证测试基于正式 Schema

**Action:**

1. Read existing schema test files
2. Verify they test against actual schema definitions
3. Check if tests would catch schema errors
4. Document findings

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- schema-sync
npx tsx scripts/verify-schema.ts
```

---

### Task 2: Create migration idempotency test

**Behavior:**

- 验证迁移文件可以重复执行
- 验证第二次执行不会失败

**Action:**
Create `tests/migration-idempotency.test.ts` with:

- Test: migrations can run twice without errors
- Test: rollback strategy exists for each migration

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- migration-idempotency
```

---

### Task 3: Create schema backward compat test

**Behavior:**

- 验证 Schema 变更不会破坏现有查询
- 验证新字段有默认值

**Action:**
Create `tests/schema-backward-compat.test.ts` with:

- Test: schema changes do not break existing queries
- Test: new fields have default values

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- backward-compat
```

---

## Verification

- [ ] Schema tests audited
- [ ] migration-idempotency.test.ts created
- [ ] schema-backward-compat.test.ts created
- [ ] All tests pass

---

## Critical Rule

> **Schema 验证原则：**
>
> - Schema 验证失败 → 修复正式 Schema 定义
> - 迁移失败 → 修复迁移文件
> - ❌ 绝不修改验证脚本来"通过"验证

---

## Output

After completion, create `.planning/phases/00-test-arch/00-03-SUMMARY.md`
