---
phase: 00-test-arch
plan: 05
type: enable
priority: high
wave: 5
depends_on:
  - 00-01
  - 00-02
  - 00-03
  - 00-04
autonomous: true
---

# Phase 00 Plan 05: API 契约测试框架创建

## Objective

创建可复用的 API 契约测试框架，供 Phase 4 剩余计划和未来阶段使用。

**Purpose:** 确保所有 API 端点都有统一的契约测试

**Output:** API 契约测试模板和示例

---

## Must-Haves

### Truths

- API 契约测试基于 REQUIREMENTS.md
- 所有 API 端点必须有契约测试
- 测试验证响应格式、参数验证、错误处理

### Artifacts

| Path                                              | Provides         | Exports |
| ------------------------------------------------- | ---------------- | ------- |
| `apps/site/tests/server/api-contract.template.ts` | API 契约测试模板 | -       |
| `apps/site/tests/server/helpers/api-test.ts`      | API 测试助手函数 | -       |

---

## Tasks

### Task 1: Create API contract test template

**Behavior:**

- 提供标准 API 契约测试结构
- 包含响应格式验证
- 包含参数验证测试
- 包含错误处理测试

**Action:**
Create `apps/site/tests/server/api-contract.template.ts` with:

```typescript
// Template structure:
describe('[Resource] API Contract', () => {
  describe('GET /api/[resource]', () => {
    it('returns standard response format', async () => {})
    it('returns 400 for invalid params', async () => {})
    it('filters correctly', async () => {})
  })
  describe('POST /api/[resource]', () => {
    it('creates resource', async () => {})
    it('returns 400 for invalid body', async () => {})
  })
})
```

---

### Task 2: Create API test helpers

**Behavior:**

- 提供 authenticated request 函数
- 提供标准响应验证函数
- 提供数据库清理函数

**Action:**
Create `apps/site/tests/server/helpers/api-test.ts` with:

- `makeAuthenticatedRequest(url, options)`
- `assertStandardResponse(response)`
- `assertPaginatedResponse(response)`
- `cleanupDatabase()`

---

### Task 3: Demonstrate with existing API

**Behavior:**

- 使用 auth API 演示模板用法
- 创建完整的契约测试示例

**Action:**
Create `apps/site/tests/server/api/auth-contract.test.ts` using the template

**Verify:**

```bash
pnpm -r test --filter=@my-blog/site -- auth-contract
```

---

## Verification

- [ ] api-contract.template.ts created
- [ ] api-test helpers created
- [ ] Example contract test created
- [ ] Template documented for Phase 4 remaining plans

---

## Output

After completion, create `.planning/phases/00-test-arch/00-05-SUMMARY.md`
