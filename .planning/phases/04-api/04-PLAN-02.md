---
phase: 04-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/server/api/v1/auth/login.post.ts
  - apps/site/server/api/v1/auth/logout.post.ts
  - apps/site/server/api/v1/auth/me.get.ts
  - apps/site/server/api/v1/auth/register.post.ts
  - apps/site/server/services/auth.service.ts
  - apps/site/server/schemas/auth.ts
autonomous: true
requirements:
  - API-05

must_haves:
  truths:
    - 'User can login with username and password to receive JWT token'
    - 'Invalid credentials return 401 with clear error message'
    - 'Logged in user can fetch their profile via GET /api/v1/auth/me'
    - 'Logout invalidates the token session'
    - 'Password is never returned in any response'
  artifacts:
    - path: 'apps/site/server/services/auth.service.ts'
      provides: 'Authentication business logic'
      exports: ['login', 'logout', 'getCurrentUser', 'hashPassword', 'verifyPassword']
      min_lines: 60
    - path: 'apps/site/server/api/v1/auth/login.post.ts'
      provides: 'POST /api/v1/auth/login endpoint'
      min_lines: 20
    - path: 'apps/site/server/api/v1/auth/me.get.ts'
      provides: 'GET /api/v1/auth/me endpoint'
      min_lines: 15
  key_links:
    - from: 'apps/site/server/api/v1/auth/login.post.ts'
      to: 'apps/site/server/services/auth.service.ts'
      via: 'service call'
      pattern: "login\\("
    - from: 'apps/site/server/services/auth.service.ts'
      to: 'jose.SignJWT'
      via: 'token generation'
      pattern: 'SignJWT.*sign'
    - from: 'apps/site/server/services/auth.service.ts'
      to: 'bcryptjs.compare'
      via: 'password verification'
      pattern: "bcrypt\\.compare"
---

<objective>
实现用户认证 API：登录、登出、获取当前用户信息

Purpose: 提供安全的用户认证机制，使用 JWT 30 天有效期，bcrypt 密码加密

Output: 4 个 API 端点文件、1 个 auth 服务文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
@.planning/phases/04-api/04-PLAN-01.md

# Database schema

@packages/database/src/schema/users.ts
@packages/database/src/db.ts

# Middleware from Plan 01 (for reference)

@apps/site/server/middleware/auth.ts
@apps/site/server/utils/response.ts
</context>

<interfaces>
<!-- Types from Plan 01 that this plan uses -->

From apps/site/server/utils/response.ts:

```typescript
export function createSuccessResponse<T>(data: T, message?: string): SuccessResponse<T>
export function createErrorResponse(code: string, message: string, details?): ErrorResponse
```

From apps/site/server/utils/validate.ts:

```typescript
export function validateBody<T>(body: unknown, schema: z.ZodType<T>): z.infer<T>
```

From apps/site/server/middleware/auth.ts:

```typescript
// Augmented H3 event context
declare module 'h3' {
  interface H3EventContext {
    user?: { id: string; role: string }
  }
}
```

From packages/database/src/schema/users.ts:

```typescript
export type User = typeof users.$inferSelect
export const users: SQLiteTable
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth service with login and password hashing</name>
  <files>apps/site/server/services/auth.service.ts</files>
  <behavior>
    - Test: hashPassword("mypassword") returns bcrypt hash (not plain text)
    - Test: verifyPassword("mypassword", hash) returns true
    - Test: verifyPassword("wrongpassword", hash) returns false
    - Test: login with valid credentials returns { token, user }
    - Test: login with invalid username throws 401 INVALID_CREDENTIALS
    - Test: login with wrong password throws 401 INVALID_CREDENTIALS
    - Test: JWT token contains user id in 'sub' claim and role
    - Test: JWT token has 30-day expiration
  </behavior>
  <action>
    Create auth.service.ts with:

    Constants per CONTEXT.md:
    - JWT_SECRET from process.env (default 'change-me')
    - BCRYPT_COST = 10
    - TOKEN_EXPIRY = '30d'

    Functions:
    - `hashPassword(password: string): Promise<string>` - bcrypt.hash with cost 10
    - `verifyPassword(password: string, hash: string): Promise<boolean>` - bcrypt.compare
    - `login(username: string, password: string)` - find user, verify password, generate JWT, return { token, user (without passwordHash) }
    - `getCurrentUser(userId: string)` - fetch user from DB, return without passwordHash
    - `generateToken(user: { id: string, role: string }): Promise<string>` - use jose.SignJWT

    JWT payload: { sub: userId, role, iat, exp }
    Algorithm: HS256

    IMPORTANT: Never return passwordHash in any response - explicitly exclude when returning user object

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/auth</automated>
  </verify>
  <done>Auth service with login, password hashing, JWT generation working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create login endpoint</name>
  <files>apps/site/server/api/v1/auth/login.post.ts</files>
  <behavior>
    - Test: POST /api/v1/auth/login with valid credentials returns 200 + { success: true, data: { token, user } }
    - Test: POST with invalid credentials returns 401 + { success: false, error: {...} }
    - Test: POST with missing fields returns 400 + VALIDATION_ERROR
    - Test: Response user object does NOT include passwordHash
    - Test: Token is a valid JWT string
  </behavior>
  <action>
    Create login.post.ts:
    - DefineEventHandler handler
    - Use validateBody with loginSchema (from Plan 01)
    - Call auth.login(username, password)
    - Return createSuccessResponse({ token, user }, 'Login successful')
    - HTTP method: .post.ts (Nitro convention)

    Request body: { username: string, password: string }
    Response: { success: true, data: { token: string, user: { id, username, email, role, avatar } } }

    Error cases:
    - Validation error: 400 VALIDATION_ERROR
    - Invalid credentials: 401 INVALID_CREDENTIALS

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/auth/login</automated>
  </verify>
  <done>Login endpoint accepts valid credentials and returns JWT token</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create logout and get current user endpoints</name>
  <files>apps/site/server/api/v1/auth/logout.post.ts, apps/site/server/api/v1/auth/me.get.ts</files>
  <behavior>
    - Test: GET /api/v1/auth/me without token returns 401
    - Test: GET /api/v1/auth/me with valid token returns 200 + user profile
    - Test: User profile does NOT include passwordHash
    - Test: POST /api/v1/auth/logout with valid token returns 200 + success message
    - Test: Logout response suggests client-side token removal
  </behavior>
  <action>
    Create me.get.ts:
    - Protected route (auth middleware attaches user)
    - Call auth.getCurrentUser(event.context.user.id)
    - Return createSuccessResponse(user)

    Create logout.post.ts:
    - Protected route (requires valid token)
    - Note: JWT is stateless, so "logout" is primarily client-side (remove token)
    - Optional: Could maintain token blacklist in future
    - Return createSuccessResponse({ message: 'Logout successful. Please remove the token from client storage.' })

    For JWT stateless auth: logout is a semantic operation - client must remove stored token

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/auth</automated>
  </verify>
  <done>Logout and me endpoints working correctly</done>
</task>

<task type="auto">
  <name>Task 4: Create optional register endpoint</name>
  <files>apps/site/server/api/v1/auth/register.post.ts</files>
  <action>
    Create register.post.ts (OPTIONAL - for future multi-user support):
    - Use validateBody with registerSchema
    - Check if username already exists
    - Check if email already exists
    - Hash password with bcrypt
    - Insert new user into database
    - Generate JWT token
    - Return { token, user }

    Note: For single-user blog, this may not be needed initially, but useful for multi-author scenarios.

    Alternatively, create a seed script that creates initial admin user if not exists.

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/auth/register</automated>
  </verify>
  <done>Register endpoint creates new users with hashed passwords</done>
</task>

<task type="auto">
  <name>Task 5: Create admin user seed script</name>
  <files>apps/site/server/seed-admin.ts</files>
  <action>
    Create seed-admin.ts script:
    - Check if admin user exists (username: 'admin')
    - If not, create with:
      - username: 'admin'
      - email: 'admin@example.com'
      - password: hash of 'admin123' (user should change after first login)
      - role: 'admin'
    - Log success or "admin already exists"

    This ensures there's always at least one admin user for the blog.

    Run via: `pnpm tsx apps/site/server/seed-admin.ts`

  </action>
  <verify>
    <automated>pnpm tsx apps/site/server/seed-admin.ts</automated>
  </verify>
  <done>Admin user seeded in database (idempotent)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify auth API end-to-end</name>
  <what-built>
    - POST /api/v1/auth/login - returns JWT on valid credentials
    - POST /api/v1/auth/logout - confirms logout
    - GET /api/v1/auth/me - returns current user profile
    - POST /api/v1/auth/register - creates new user (optional)
    - Admin user seeded in database
  </what-built>
  <how-to-verify>
    1. Ensure dev server running: `pnpm dev`
    2. Run seed script: `pnpm tsx apps/site/server/seed-admin.ts`
    3. Test login: `curl -X POST http://localhost:3000/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'`
       - Expected: 200 with { success: true, data: { token: "eyJ...", user: {...} } }
    4. Save token and test me endpoint: `curl http://localhost:3000/api/v1/auth/me -H "Authorization: Bearer YOUR_TOKEN"`
       - Expected: 200 with user profile (no passwordHash)
    5. Test invalid login: `curl -X POST http://localhost:3000/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"wrong"}'`
       - Expected: 401 with { success: false, error: { code: 'INVALID_CREDENTIALS', ... } }
    6. Test without token: `curl http://localhost:3000/api/v1/auth/me`
       - Expected: 401 UNAUTHORIZED
  </how-to-verify>
  <resume-signal>Type "approved" if auth flow works end-to-end, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Auth service created with login, getCurrentUser, password hashing
- [ ] Login endpoint returns JWT token on valid credentials
- [ ] Me endpoint returns user profile (without passwordHash)
- [ ] Logout endpoint provides semantic logout
- [ ] Admin user seeded in database
- [ ] All tests passing
</verification>

<success_criteria>

- Login with admin/admin123 returns valid JWT token
- Token can be used to fetch user profile from /api/v1/auth/me
- Invalid credentials return 401 with clear error
- Password hash is never exposed in any response
- JWT uses HS256 algorithm with 30-day expiry
- bcrypt cost=10 for password hashing
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-02-SUMMARY.md` with:
- Auth API endpoints summary
- Test results
- Admin user credentials (for testing only)
- Ready for Plan 03 (Posts API) execution
</output>
