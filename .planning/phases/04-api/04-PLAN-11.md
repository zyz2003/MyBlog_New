---
phase: 04-api
plan: 11
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
  - 04-PLAN-03
  - 04-PLAN-04
files_modified:
  - apps/site/server/services/category.service.ts
  - apps/site/server/api/v1/categories/index.get.ts
  - apps/site/server/api/v1/categories/index.post.ts
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - 'User can fetch list of all categories'
    - 'Authenticated user can create categories'
    - 'Category hierarchy works (parent/children relationship)'
  artifacts:
    - path: 'apps/site/server/services/category.service.ts'
      provides: 'Category CRUD with hierarchy support'
      exports:
        ['listCategories', 'createCategory', 'updateCategory', 'deleteCategory', 'getCategoryById']
      min_lines: 50
  key_links:
    - from: 'apps/site/server/services/category.service.ts'
      to: 'db.transaction'
      via: 'category hierarchy operations'
      pattern: 'db\\.transaction'
---

<objective>
实现分类 API：服务层和查询端点

Purpose: 提供分类管理能力，支持多级分类

Output: 1 个服务文件，2 个 API 端点
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create category service and list endpoint</name>
  <files>apps/site/server/services/category.service.ts, apps/site/server/api/v1/categories/index.get.ts, apps/site/server/api/v1/categories/index.post.ts</files>
  <behavior>
    - Test: listCategories returns all categories with hierarchy
    - Test: createCategory creates category with parent reference
    - Test: Creating category with invalid parent returns 400
  </behavior>
  <action>
    Create category.service.ts with:
    - listCategories(): returns tree structure with children
    - createCategory(data): creates category, validates parent
    - updateCategory(id, data): updates category
    - deleteCategory(id): deletes, sets children to top-level
    - getCategoryById(id): single category

    Create index.get.ts:
    - Call listCategories()
    - Return { success: true, data: categories }

    Create index.post.ts:
    - Require auth middleware
    - Parse body with validateBody(createCategorySchema)
    - Call createCategory()
    - Return 201 with created category

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- 'services/category|api/v1/categories'</automated>
  </verify>
  <done>Category service and endpoints created and tested</done>
</task>

</tasks>

<verification>
- [ ] category.service.ts created
- [ ] index.get.ts, index.post.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-11-SUMMARY.md`
</output>
