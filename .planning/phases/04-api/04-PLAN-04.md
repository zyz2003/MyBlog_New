---
phase: 04-api
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/server/middleware/error.ts
  - apps/site/server/middleware/logger.ts
  - apps/site/server/middleware/rate-limit.ts
autonomous: true
requirements:
  - API-01
  - API-08

must_haves:
  truths:
    - 'All errors are caught and return standardized JSON response'
    - 'Unknown errors return 500 SERVER_ERROR with sanitized message'
    - 'Every request logs: [HTTP] GET /api/v1/posts 200 15ms'
    - 'Rate limiting blocks IPs exceeding 100 req/min'
  artifacts:
    - path: 'apps/site/server/middleware/error.ts'
      provides: 'Global error handler middleware'
      min_lines: 30
    - path: 'apps/site/server/middleware/logger.ts'
      provides: 'Request logging middleware'
      min_lines: 20
    - path: 'apps/site/server/middleware/rate-limit.ts'
      provides: 'Rate limiting middleware (100 req/min per IP)'
      min_lines: 30
  key_links:
    - from: 'apps/site/server/middleware/error.ts'
      to: 'createErrorResponse'
      via: 'error formatting'
      pattern: 'createErrorResponse'
---

<objective>
创建错误处理、日志、限流中间件

Purpose: 提供全局错误捕获、请求日志、基础限流能力

Output: 3 个中间件文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create error handler middleware</name>
  <files>apps/site/server/middleware/error.ts</files>
  <behavior>
    - Test: HTTPError thrown in route handler returns proper JSON response with status code
    - Test: Unknown errors return 500 SERVER_ERROR with sanitized message
    - Test: Error responses include error code and message in response body
  </behavior>
  <action>
    Create error.ts middleware:
    - Use defineEventHandler to wrap all route handlers
    - Catch HTTPError: return createErrorResponse with matching status code
    - Catch unknown errors: log full error, return 500 SERVER_ERROR
    - Set proper Content-Type: application/json header

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- middleware/error</automated>
  </verify>
  <done>Error handler middleware created and tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create logger and rate-limit middleware</name>
  <files>apps/site/server/middleware/logger.ts, apps/site/server/middleware/rate-limit.ts</files>
  <behavior>
    - Test: Every request logs method, path, status, duration
    - Test: Rate limit allows 100 requests per minute per IP
    - Test: 101st request returns 429 TOO_MANY_REQUESTS
  </behavior>
  <action>
    Create logger.ts middleware:
    - Use defineEventHandler with onRequest hook
    - Record start time with performance.now()
    - Log: method, path, status code, duration in ms
    - Format: [HTTP] ${method} ${path} ${status} ${duration}ms

    Create rate-limit.ts middleware:
    - Track requests per IP using in-memory Map
    - Per CONTEXT.md: 100 requests per minute per IP
    - If count > 100: return 429 with error response
    - Set X-RateLimit-Limit and X-RateLimit-Remaining headers

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- 'middleware/(logger|rate-limit)'</automated>
  </verify>
  <done>Logger and rate-limit middleware created and tested</done>
</task>

</tasks>

<verification>
- [ ] error.ts, logger.ts, rate-limit.ts created
- [ ] Middleware registered in nuxt.config.ts
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-04-SUMMARY.md`
</output>
