---
phase: 04-api
plan: 04
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
files_modified:
  - apps/site/server/api/v1/categories/index.get.ts
  - apps/site/server/api/v1/categories/index.post.ts
  - apps/site/server/api/v1/categories/[id].get.ts
  - apps/site/server/api/v1/categories/[id].put.ts
  - apps/site/server/api/v1/categories/[id].delete.ts
  - apps/site/server/api/v1/tags/index.get.ts
  - apps/site/server/api/v1/tags/index.post.ts
  - apps/site/server/api/v1/tags/[id].get.ts
  - apps/site/server/api/v1/tags/[id].put.ts
  - apps/site/server/api/v1/tags/[id].delete.ts
  - apps/site/server/services/category.service.ts
  - apps/site/server/services/tag.service.ts
  - apps/site/server/schemas/category.ts
  - apps/site/server/schemas/tag.ts
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - 'User can fetch list of all categories'
    - 'User can fetch list of all tags'
    - 'Authenticated user can create/update/delete categories'
    - 'Authenticated user can create/update/delete tags'
    - 'Category hierarchy works (parent/children relationship)'
    - 'Deleting category sets children to top-level (onDelete: set null)'
  artifacts:
    - path: 'apps/site/server/services/category.service.ts'
      provides: 'Category CRUD with hierarchy support'
      exports:
        ['listCategories', 'createCategory', 'updateCategory', 'deleteCategory', 'getCategoryById']
      min_lines: 50
    - path: 'apps/site/server/services/tag.service.ts'
      provides: 'Tag CRUD operations'
      exports: ['listTags', 'createTag', 'updateTag', 'deleteTag', 'getTagById']
      min_lines: 40
  key_links:
    - from: 'apps/site/server/services/category.service.ts'
      to: 'db.transaction'
      via: 'category hierarchy operations'
      pattern: "db\\.transaction"
    - from: 'apps/site/server/utils/slug.ts'
      to: 'category/tag slug generation'
      via: 'reusable slug utility'
      pattern: 'generateSlug'
---

<objective>
实现分类和标签 API：完整的 CRUD 操作，支持分类层级结构

Purpose: 提供文章分类和标签管理能力，支持多级分类和标签管理

Output: 10 个 API 端点文件、2 个服务文件、2 个 schema 文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
@.planning/phases/04-api/04-PLAN-01.md
@.planning/phases/04-api/04-PLAN-02.md
@.planning/phases/04-api/04-PLAN-03.md

# Database schemas

@packages/database/src/schema/categories.ts
@packages/database/src/schema/tags.ts
@packages/database/src/db.ts
</context>

<interfaces>
<!-- Types from Plan 01/02/03 -->

From apps/site/server/utils/response.ts:

```typescript
export function createSuccessResponse<T>(data: T): SuccessResponse<T>
export function createPaginationResponse<T>(
  data: T[],
  meta: { total; limit; offset }
): PaginationResponse<T>
```

From apps/site/server/utils/validate.ts:

```typescript
export function validateBody<T>(body: unknown, schema: z.ZodType<T>): z.infer<T>
```

From apps/site/server/utils/slug.ts (Plan 03):

```typescript
export function generateSlug(title: string): Promise<string>
```

From packages/database/src/schema/categories.ts:

```typescript
export type Category = typeof categories.$inferSelect
// Category has: id, name, slug, description, parentId, createdAt, updatedAt
```

From packages/database/src/schema/tags.ts:

```typescript
export type Tag = typeof tags.$inferSelect
// Tag has: id, name, slug, description, color, createdAt, updatedAt
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create category and tag schemas</name>
  <files>apps/site/server/schemas/category.ts, apps/site/server/schemas/tag.ts</files>
  <behavior>
    - Test: createCategorySchema accepts { name: "技术" }
    - Test: createCategorySchema requires name (1-100 chars)
    - Test: createCategorySchema has optional: description, parentId, color (for tags)
    - Test: createTagSchema accepts { name: "JavaScript" }
    - Test: createTagSchema has optional: description, color (hex format validation)
    - Test: Invalid color format rejected (not #RRGGBB or valid color name)
  </behavior>
  <action>
    Create category.ts schema:
    - `createCategorySchema`: { name (1-100), description? (max 500), parentId? (string | null), slug? (auto-generated if not provided) }
    - `updateCategorySchema`: createCategorySchema.partial()
    - `categoryParamsSchema`: { id: string }

    Create tag.ts schema:
    - `createTagSchema`: { name (1-50), description? (max 500), color? (hex color validation), slug? (auto-generated) }
    - `updateTagSchema`: createTagSchema.partial()
    - `tagParamsSchema`: { id: string }

    Slug is optional in schema - will be auto-generated from name if not provided

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- server/schemas/category</automated>
  </verify>
  <done>Category and tag schemas created with validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create category service</name>
  <files>apps/site/server/services/category.service.ts</files>
  <behavior>
    - Test: listCategories returns all categories with tree structure
    - Test: createCategory creates category with auto-generated slug
    - Test: createCategory with parentId sets up hierarchy
    - Test: updateCategory updates fields and regenerates slug if name changed
    - Test: deleteCategory sets children's parentId to null (not cascade delete)
    - Test: getCategoryById returns category with post count
    - Test: Circular parent reference detected and rejected
  </behavior>
  <action>
    Create category.service.ts:

    - `listCategories()`:
      - Return flat list OR tree structure (nested children)
      - Include post count for each category

    - `createCategory(data)`:
      - Generate slug from name
      - Validate parentId exists (if provided)
      - Check for circular reference (parentId cannot be self)
      - Insert and return with relations

    - `updateCategory(id, data)`:
      - Regenerate slug if name changed
      - Validate new parentId
      - Check circular reference
      - Update and return

    - `deleteCategory(id)`:
      - Check existence
      - Set children's parentId to NULL (per schema onDelete: set null)
      - Delete category

    - `getCategoryById(id)`:
      - Query with post count
      - Include parent info if exists

    Use transactions for operations involving parent-child relationships

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/category</automated>
  </verify>
  <done>Category service with hierarchy support working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tag service</name>
  <files>apps/site/server/services/tag.service.ts</files>
  <behavior>
    - Test: listTags returns all tags
    - Test: createTag creates tag with auto-generated slug
    - Test: updateTag updates fields
    - Test: deleteTag removes tag (cascades post_tags)
    - Test: getTagById returns tag with post count
  </behavior>
  <action>
    Create tag.service.ts:

    - `listTags()`:
      - Return all tags
      - Include post count for each tag

    - `createTag(data)`:
      - Generate slug from name
      - Insert and return

    - `updateTag(id, data)`:
      - Regenerate slug if name changed
      - Update and return

    - `deleteTag(id)`:
      - Check existence
      - Delete (cascade handles post_tags)

    - `getTagById(id)`:
      - Query with post count

    Tags are simpler than categories - no hierarchy

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/tag</automated>
  </verify>
  <done>Tag service working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create category endpoints</name>
  <files>apps/site/server/api/v1/categories/index.get.ts, apps/site/server/api/v1/categories/index.post.ts, apps/site/server/api/v1/categories/[id].get.ts, apps/site/server/api/v1/categories/[id].put.ts, apps/site/server/api/v1/categories/[id].delete.ts</files>
  <behavior>
    - Test: GET /api/v1/categories returns list of categories
    - Test: GET /api/v1/categories/:id returns single category
    - Test: POST /api/v1/categories without auth returns 401
    - Test: POST with valid data creates category with 201
    - Test: PUT /api/v1/categories/:id updates category
    - Test: DELETE /api/v1/categories/:id removes category, sets children to null
    - Test: Invalid ID returns 404
  </behavior>
  <action>
    Create 5 endpoints following the established pattern:

    index.get.ts:
    - Public endpoint
    - Return createSuccessResponse(categories)

    index.post.ts:
    - Protected endpoint
    - Validate body with createCategorySchema
    - Call categoryService.createCategory(data)
    - Return 201 with created category

    [id].get.ts:
    - Public endpoint
    - Call categoryService.getCategoryById(id)
    - Return category or 404

    [id].put.ts:
    - Protected endpoint
    - Validate body and params
    - Call categoryService.updateCategory(id, data)
    - Return updated category

    [id].delete.ts:
    - Protected endpoint
    - Call categoryService.deleteCategory(id)
    - Return success message

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/categories</automated>
  </verify>
  <done>Category CRUD endpoints working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create tag endpoints</name>
  <files>apps/site/server/api/v1/tags/index.get.ts, apps/site/server/api/v1/tags/index.post.ts, apps/site/server/api/v1/tags/[id].get.ts, apps/site/server/api/v1/tags/[id].put.ts, apps/site/server/api/v1/tags/[id].delete.ts</files>
  <behavior>
    - Test: GET /api/v1/tags returns list of tags
    - Test: GET /api/v1/tags/:id returns single tag
    - Test: POST /api/v1/tags without auth returns 401
    - Test: POST with valid data creates tag with 201
    - Test: PUT /api/v1/tags/:id updates tag
    - Test: DELETE /api/v1/tags/:id removes tag
    - Test: Invalid ID returns 404
  </behavior>
  <action>
    Create 5 endpoints following the same pattern as categories:

    index.get.ts:
    - Public endpoint
    - Return all tags with post count

    index.post.ts:
    - Protected endpoint
    - Validate and create

    [id].get.ts:
    - Public endpoint
    - Return tag or 404

    [id].put.ts:
    - Protected endpoint
    - Validate and update

    [id].delete.ts:
    - Protected endpoint
    - Delete tag

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/tags</automated>
  </verify>
  <done>Tag CRUD endpoints working</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify categories and tags API</name>
  <what-built>
    - GET/POST /api/v1/categories - list and create categories
    - GET/PUT/DELETE /api/v1/categories/:id - single category operations
    - GET/POST /api/v1/tags - list and create tags
    - GET/PUT/DELETE /api/v1/tags/:id - single tag operations
    - Category hierarchy support
    - Auto slug generation
  </what-built>
  <how-to-verify>
    1. Ensure dev server running and admin logged in
    2. Test category list (empty): `curl http://localhost:3000/api/v1/categories`
       - Expected: 200 with empty array
    3. Create category: `curl -X POST http://localhost:3000/api/v1/categories -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"name":"技术文章","description":"技术类文章"}'`
       - Expected: 201 with category including auto-generated slug
    4. Create child category: `curl -X POST ... -d '{"name":"前端","parentId":"PARENT_ID"}'`
       - Expected: 201 with parentId set
    5. Create tag: `curl -X POST http://localhost:3000/api/v1/tags -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"name":"JavaScript","color":"#F7DF1E"}'`
       - Expected: 201 with tag
    6. Test category deletion: `curl -X DELETE http://localhost:3000/api/v1/categories/PARENT_ID -H "Authorization: Bearer $TOKEN"`
       - Expected: 200, child category now has parentId=null
  </how-to-verify>
  <resume-signal>Type "approved" if categories and tags work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Category and tag schemas created
- [ ] Category service with hierarchy support
- [ ] Tag service working
- [ ] All 10 endpoints created
- [ ] Slug generation working
- [ ] Category deletion sets children to null
- [ ] Tests passing
</verification>

<success_criteria>

- Categories can be created, read, updated, deleted
- Tags can be created, read, updated, deleted
- Category hierarchy (parent/child) works correctly
- Slugs auto-generated from names
- Deleting parent category sets children to top-level
- Post counts included in list responses
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-04-SUMMARY.md` with:
- Categories and Tags API summary
- Test results
- Ready for Plan 05 (Media API) execution
</output>
