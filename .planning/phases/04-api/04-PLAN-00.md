---
phase: 04-api
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - apps/site/vitest.config.ts
  - apps/site/tests/setup.ts
  - apps/site/tests/helpers.ts
  - apps/site/tests/fixtures.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - Vitest configured and running for server code
    - Test database helper resets state between tests
    - Test helpers provide createUser, createPost, login utilities
  artifacts:
    - path: apps/site/vitest.config.ts
      provides: Vitest configuration for Nitro server testing
    - path: apps/site/tests/helpers.ts
      provides: Test helper functions
      exports: [createUser, createPost, login, getTestHeaders]
    - path: apps/site/tests/fixtures.ts
      provides: Test fixtures and database reset utilities
  key_links:
    - from: apps/site/tests/helpers.ts
      to: packages/database/src/db.ts
      via: test database access
---

<objective>
建立测试基础设施：Vitest 配置、测试数据库、测试助手函数
Purpose: 为 API 开发提供 TDD 能力，确保代码质量和回归测试
Output: Vitest 配置文件、测试助手、测试夹具、示例测试
</objective>

<tasks>
<task type="auto"><name>Task 1: Configure Vitest</name><files>vitest.config.ts, apps/site/vitest.config.ts</files><action>Create Vitest config for workspace and apps/site</action><verify><automated>pnpm test --run --dry</automated></verify><done>Vitest configured</done></task>
<task type="auto" tdd="true"><name>Task 2: Test database helpers</name><files>apps/site/tests/setup.ts, apps/site/tests/db.ts</files><action>Create database reset and isolation utilities</action><verify><automated>pnpm -r test --filter=@my-blog/site -- tests/db</automated></verify><done>Test DB working</done></task>
<task type="auto" tdd="true"><name>Task 3: Test helpers</name><files>apps/site/tests/helpers.ts, apps/site/tests/fixtures.ts</files><action>Create createUser, createPost, login, getTestHeaders helpers</action><verify><automated>pnpm -r test --filter=@my-blog/site -- tests/helpers</automated></verify><done>Helpers working</done></task>
<task type="auto" tdd="true"><name>Task 4: Example tests</name><files>apps/site/tests/unit/example.test.ts</files><action>Create example tests to verify setup</action><verify><automated>pnpm -r test --filter=@my-blog/site -- tests/unit/example</automated></verify><done>Example tests pass</done></task>
<task type="auto"><name>Task 5: Package.json scripts</name><files>apps/site/package.json</files><action>Add test, test:run, test:coverage scripts</action><verify><automated>pnpm -C apps/site test --run</automated></verify><done>Test scripts working</done></task>
<task type="checkpoint:human-verify" gate="blocking"><name>Task 6: Verify integration</name><what-built>Vitest, test DB, helpers, example tests</what-built><how-to-verify>Run: pnpm -C apps/site test --run</how-to-verify><resume-signal>Type approved or describe issues</resume-signal></task>
</tasks>

<verification>
- [ ] vitest.config.ts created
- [ ] Test database isolation working
- [ ] Test helpers working
- [ ] Example tests pass
- [ ] Test coverage works
</verification>

<output>After completion, create .planning/phases/04-api/04-00-SUMMARY.md</output>
