---
phase: 04-api
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/vitest.config.ts
  - apps/site/tests/helpers.ts
  - apps/site/tests/setup.ts
autonomous: true
requirements:
  - API-01
  - API-08
  - API-09

must_haves:
  truths:
    - 'Tests can be run with pnpm test command'
    - 'Test helpers provide mock event objects for middleware testing'
    - 'Test database is isolated from development database'
  artifacts:
    - path: 'apps/site/vitest.config.ts'
      provides: 'Vitest configuration for server tests'
      min_lines: 20
    - path: 'apps/site/tests/helpers.ts'
      provides: 'Test utilities for API testing'
      exports: ['createMockEvent', 'createAuthEvent', 'testDb']
      min_lines: 40
  key_links:
    - from: 'apps/site/tests/helpers.ts'
      to: '@my-blog/database'
      via: 'test database connection'
      pattern: 'getDatabase|createDatabase'
---

<objective>
建立 Phase 4 API 测试基础设施：Vitest 配置、测试工具、测试数据库

Purpose: 为 API 测试提供隔离的测试环境和便捷的测试工具

Output: Vitest 配置文件、测试助手函数、测试数据库设置
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-RESEARCH.md

# Database connection reference

@packages/database/src/db.ts
</context>

<interfaces>
<!-- Database types that tests will use -->

From packages/database/src/db.ts:

```typescript
export function getDatabase(dbPath?: string): ReturnType<typeof createDatabase>
export const db = {
  get: () => {
    /* returns Drizzle database instance */
  },
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Vitest configuration</name>
  <files>apps/site/vitest.config.ts</files>
  <action>
    Create vitest.config.ts:
    - Configure test matching pattern: **/*.test.ts
    - Set up test environment (node)
    - Configure path aliases for @my-blog/database
    - Enable global test functions (describe, it, expect)
    - Configure test timeout (10000ms for database tests)
    - Set up coverage reporter if needed

    Reference existing Nuxt 3 test configuration if present in nuxt.config.ts

  </action>
  <verify>
    <automated>cd apps/site && pnpm vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>Vitest configuration created and test runner works</done>
</task>

<task type="auto">
  <name>Task 2: Create test database setup</name>
  <files>apps/site/tests/setup.ts</files>
  <action>
    Create tests/setup.ts:
    - Create test database connection (in-memory or temp file)
    - Run schema setup before tests
    - Clean up database after tests
    - Export testDb instance for use in tests

    Use SQLite in-memory mode or temp file at apps/site/data/test.db
    Ensure test database is isolated from development database

  </action>
  <verify>
    <automated>cd apps/site && pnpm vitest run tests/setup.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>Test database setup and teardown working</done>
</task>

<task type="auto">
  <name>Task 3: Create test helpers</name>
  <files>apps/site/tests/helpers.ts</files>
  <action>
    Create helpers.ts with:

    - `createMockEvent(options)`: Create mock H3 event object for middleware testing
      - Mock event.path, event.method, event.headers
      - Mock event.context for auth testing
      - Mock getQuery, getBody, getHeader helpers

    - `createAuthEvent(user?)`: Create event with authenticated user context
      - For testing protected routes

    - `testDb`: Export test database instance

    - `resetDatabase()`: Clear all tables between tests

    - `createTestUser(overrides)`: Create test user in database, return without passwordHash

    - `createTestPost(overrides)`: Create test post in database

    - `mockJwtToken(payload)`: Generate valid JWT token for testing auth middleware

  </action>
  <verify>
    <automated>cd apps/site && pnpm vitest run tests/helpers.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>Test helpers created with mock event and database utilities</done>
</task>

<task type="auto">
  <name>Task 4: Create test directory structure and sample test</name>
  <files>apps/site/tests/server/utils/response.test.ts, apps/site/tests/server/middleware/auth.test.ts</files>
  <action>
    Create directory structure:
    - apps/site/tests/server/utils/
    - apps/site/tests/server/middleware/
    - apps/site/tests/server/services/
    - apps/site/tests/server/api/

    Create sample test (response.test.ts):
    - Import response utilities (will be created in Plan 01)
    - Test createSuccessResponse
    - Test createErrorResponse
    - Test createPaginationResponse

    Create placeholder auth.test.ts:
    - Import auth middleware (will be created in Plan 01)
    - Test JWT verification
    - Test protected route rejection

  </action>
  <verify>
    <automated>cd apps/site && pnpm vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>Test directory structure created with sample tests</done>
</task>

<task type="auto">
  <name>Task 5: Install required dependencies</name>
  <files>apps/site/package.json</files>
  <action>
    Install dependencies needed for Phase 4:
    - zod (request validation)
    - jose (JWT)
    - bcryptjs (password hashing)
    - @types/bcryptjs (TypeScript types)
    - pinyin-pro (Chinese to pinyin)
    - sharp (image processing)
    - shiki (code highlighting)
    - nanoid (unique ID generation)

    Run: cd apps/site && pnpm add zod jose bcryptjs pinyin-pro sharp shiki nanoid
    Run: cd apps/site && pnpm add -D @types/bcryptjs

  </action>
  <verify>
    <automated>cd apps/site && pnpm list zod jose bcryptjs pinyin-pro sharp shiki nanoid</automated>
  </verify>
  <done>Phase 4 dependencies installed</done>
</task>

</tasks>

<verification>
- [ ] Vitest configuration created
- [ ] Test database setup working
- [ ] Test helpers created with mock utilities
- [ ] Test directory structure in place
- [ ] Sample tests passing
- [ ] Phase 4 dependencies installed
</verification>

<success_criteria>

- `pnpm vitest run` executes without errors
- Test helpers provide mock event objects
- Test database is isolated (temp file or in-memory)
- Sample tests demonstrate test pattern for subsequent plans
- All required packages installed in apps/site
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-00-SUMMARY.md` with:
- Test infrastructure summary
- Installed dependencies list
- Test helper utilities documented
- Ready for Plan 01 execution
</output>
