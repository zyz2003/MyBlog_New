---
phase: 04-api
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/server/services/auth.service.ts
  - apps/site/server/api/v1/auth/login.post.ts
  - apps/site/server/api/v1/auth/logout.post.ts
autonomous: true
requirements:
  - API-05

must_haves:
  truths:
    - 'User can login with username and password to receive JWT token'
    - 'Invalid credentials return 401 with clear error message'
    - 'Logout invalidates the token session'
    - 'Password is never returned in any response'
  artifacts:
    - path: 'apps/site/server/services/auth.service.ts'
      provides: 'Authentication business logic'
      exports: ['login', 'logout', 'hashPassword', 'verifyPassword']
      min_lines: 50
    - path: 'apps/site/server/api/v1/auth/login.post.ts'
      provides: 'POST /api/v1/auth/login endpoint'
      min_lines: 20
  key_links:
    - from: 'apps/site/server/services/auth.service.ts'
      to: 'jose.SignJWT'
      via: 'token generation'
      pattern: 'SignJWT.*sign'
    - from: 'apps/site/server/services/auth.service.ts'
      to: 'bcryptjs.compare'
      via: 'password verification'
      pattern: 'bcrypt\\.compare'
---

<objective>
实现认证 API：登录、登出

Purpose: 提供用户认证功能

Output: 1 个服务文件，2 个 API 端点
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth service</name>
  <files>apps/site/server/services/auth.service.ts</files>
  <behavior>
    - Test: hashPassword returns bcrypt hash (not plaintext)
    - Test: verifyPassword returns true for correct password
    - Test: login returns { token, user } for valid credentials
    - Test: login throws 401 for invalid credentials
  </behavior>
  <action>
    Create auth.service.ts with:
    - hashPassword(password: string): Promise<string> - uses bcrypt with cost=10
    - verifyPassword(password: string, hash: string): Promise<boolean>
    - login(username: string, password: string): Promise<{ token: string, user: User }>
    - logout(token: string): Promise<void> - blacklists token
    - Use jose.SignJWT for token generation with 30-day expiry

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/auth</automated>
  </verify>
  <done>Auth service created with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create login/logout endpoints</name>
  <files>apps/site/server/api/v1/auth/login.post.ts, apps/site/server/api/v1/auth/logout.post.ts</files>
  <behavior>
    - Test: POST /api/v1/auth/login with valid credentials returns 200 + token
    - Test: POST /api/v1/auth/login with invalid credentials returns 401
    - Test: POST /api/v1/auth/logout with valid token returns 200
  </behavior>
  <action>
    Create login.post.ts:
    - Parse body with validateBody(loginSchema)
    - Call auth.service.login()
    - Return { success: true, data: { token, user } }

    Create logout.post.ts:
    - Get token from Authorization header
    - Call auth.service.logout()
    - Return { success: true, message: 'Logged out' }

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/auth</automated>
  </verify>
  <done>Login and logout endpoints created and tested</done>
</task>

</tasks>

<verification>
- [ ] auth.service.ts created
- [ ] login.post.ts, logout.post.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-06-SUMMARY.md`
</output>
