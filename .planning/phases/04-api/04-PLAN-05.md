---
phase: 04-api
plan: 05
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
files_modified:
  - apps/site/server/api/v1/media/index.get.ts
  - apps/site/server/api/v1/media/index.post.ts
  - apps/site/server/api/v1/media/[id].get.ts
  - apps/site/server/api/v1/media/[id].delete.ts
  - apps/site/server/services/media.service.ts
  - apps/site/server/services/storage.service.ts
  - apps/site/server/schemas/media.ts
  - apps/site/server/utils/upload.ts
autonomous: true
requirements:
  - API-04
  - API-06
  - API-07

must_haves:
  truths:
    - 'User can fetch list of media files'
    - 'Authenticated user can upload files via multipart/form-data'
    - 'Upload generates thumbnail for images'
    - 'User can delete media files'
    - 'Storage provider abstraction allows switching between local and object storage'
    - 'Plugin and theme config endpoints exist for management'
  artifacts:
    - path: 'apps/site/server/services/media.service.ts'
      provides: 'Media CRUD operations'
      exports: ['listMedia', 'uploadMedia', 'deleteMedia', 'getMediaById']
      min_lines: 50
    - path: 'apps/site/server/services/storage.service.ts'
      provides: 'Storage provider abstraction (local + S3)'
      exports:
        ['StorageProvider', 'LocalStorageProvider', 'S3StorageProvider', 'getStorageProvider']
      min_lines: 80
    - path: 'apps/site/server/api/v1/media/index.post.ts'
      provides: 'POST /api/v1/media - file upload endpoint'
      min_lines: 30
  key_links:
    - from: 'apps/site/server/services/storage.service.ts'
      to: 'sharp'
      via: 'image thumbnail generation'
      pattern: "sharp\\("
    - from: 'apps/site/server/api/v1/media/index.post.ts'
      to: 'readMultipartFormData'
      via: 'h3 file upload parsing'
      pattern: 'readMultipartFormData'
---

<objective>
实现媒体管理 API 和存储抽象层，插件/主题管理端点

Purpose: 提供文件上传管理能力，支持本地和对象存储切换；提供插件和主题切换的基础端点

Output: 4 个媒体 API 端点、2 个服务文件、schema 和上传工具
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
@.planning/phases/04-api/04-PLAN-01.md
@.planning/phases/04-api/04-PLAN-02.md

# Database schema

@packages/database/src/schema/media.ts
@packages/database/src/db.ts
</context>

<interfaces>
<!-- Types from Plan 01/02 -->

From apps/site/server/utils/response.ts:

```typescript
export function createSuccessResponse<T>(data: T): SuccessResponse<T>
export function createPaginationResponse<T>(
  data: T[],
  meta: { total; limit; offset }
): PaginationResponse<T>
```

From apps/site/server/middleware/auth.ts:

```typescript
event.context.user: { id: string; role: string }
```

From packages/database/src/schema/media.ts:

```typescript
export type Media = typeof media.$inferSelect
// Media has: id, filename, originalName, path, url, mimeType, size, width, height, altText, thumbnailPath, folderId, uploadedBy, uploadedAt
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create storage service with provider abstraction</name>
  <files>apps/site/server/services/storage.service.ts</files>
  <behavior>
    - Test: LocalStorageProvider.save() writes file to disk and returns URL
    - Test: LocalStorageProvider.delete() removes file from disk
    - Test: LocalStorageProvider.generateThumbnail() creates resized image
    - Test: S3StorageProvider.save() uploads to S3-compatible storage and returns URL
    - Test: S3StorageProvider.delete() removes from S3
    - Test: getStorageProvider() returns configured provider based on env var
    - Test: Storage provider interface is consistent (same methods for local and S3)
  </behavior>
  <action>
    Create storage.service.ts with:

    StorageProvider interface:
    - `save(file: { name: string, data: Buffer, mimeType: string }): Promise<{ url: string, path: string }>`
    - `delete(path: string): Promise<void>`
    - `exists(path: string): Promise<boolean>`
    - `generateThumbnail(path: string, options: { width: number, height: number }): Promise<string>`

    LocalStorageProvider (implements StorageProvider):
    - Save to apps/site/uploads/original/ and apps/site/uploads/thumbnails/
    - Return relative URLs like /uploads/original/filename

    S3StorageProvider (implements StorageProvider):
    - Use @aws-sdk/client-s3 for S3-compatible storage
    - Support Alibaba OSS, Tencent COS via S3-compatible endpoints
    - Configure via env: S3_ENDPOINT, S3_BUCKET, S3_ACCESS_KEY, S3_SECRET_KEY, S3_REGION

    getStorageProvider():
    - Read STORAGE_PROVIDER from env ('local' | 's3')
    - Return configured provider instance

    Use sharp for thumbnail generation (resize, format conversion)

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/storage</automated>
  </verify>
  <done>Storage service with local and S3 providers working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create media service</name>
  <files>apps/site/server/services/media.service.ts, apps/site/server/utils/upload.ts</files>
  <behavior>
    - Test: uploadMedia() saves file metadata to database
    - Test: uploadMedia() generates thumbnail for images
    - Test: uploadMedia() extracts width/height for images
    - Test: listMedia() returns paginated list
    - Test: deleteMedia() removes file from storage and database
    - Test: getMediaById() returns media metadata
    - Test: Non-image files skip thumbnail generation
  </behavior>
  <action>
    Create media.service.ts:

    - `uploadMedia(file: { name: string, data: Buffer, mimeType: string }, userId: string)`:
      - Use storage provider to save file
      - Generate thumbnail for images (image/* mime types)
      - Extract dimensions using sharp.metadata()
      - Insert into database with all metadata
      - Return media object

    - `listMedia(options)`:
      - Paginated list with filters (mimeType, folderId)
      - Include uploader info

    - `deleteMedia(id)`:
      - Get media from DB
      - Delete from storage provider
      - Delete from database

    - `getMediaById(id)`:
      - Query with uploader relation

    Create upload.ts utility:
    - `parseMultipart(event): Promise<UploadedFile[]>` - wrapper around h3 readMultipartFormData
    - File size validation (max 10MB for images, 100MB for videos)
    - MIME type validation

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/media</automated>
  </verify>
  <done>Media service with upload and thumbnail generation working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create media endpoints</name>
  <files>apps/site/server/api/v1/media/index.get.ts, apps/site/server/api/v1/media/index.post.ts, apps/site/server/api/v1/media/[id].get.ts, apps/site/server/api/v1/media/[id].delete.ts</files>
  <behavior>
    - Test: GET /api/v1/media returns paginated list
    - Test: POST /api/v1/media without auth returns 401
    - Test: POST with multipart file returns 201 with media metadata
    - Test: POST handles batch upload (multiple files)
    - Test: GET /api/v1/media/:id returns media metadata
    - Test: DELETE /api/v1/media/:id removes file and record
    - Test: Invalid ID returns 404
  </behavior>
  <action>
    Create index.get.ts:
    - Public endpoint (anyone can view media library)
    - Validate query with pagination schema
    - Call mediaService.listMedia(options)
    - Return createPaginationResponse(media, meta)

    Create index.post.ts:
    - Protected endpoint (require auth)
    - Parse multipart/form-data
    - Validate each file (size, type)
    - Call mediaService.uploadMedia(file, user.id) for each
    - Return createSuccessResponse(uploadedFiles, 'Upload successful')
    - Support batch upload (array of files)

    Create [id].get.ts:
    - Public endpoint
    - Return media metadata or 404

    Create [id].delete.ts:
    - Protected endpoint
    - Call mediaService.deleteMedia(id)
    - Return success message

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/media</automated>
  </verify>
  <done>Media CRUD endpoints working with file upload</done>
</task>

<task type="auto">
  <name>Task 4: Create plugin and theme management endpoints</name>
  <files>apps/site/server/api/v1/admin/plugins.get.ts, apps/site/server/api/v1/admin/plugins.put.ts, apps/site/server/api/v1/admin/theme.get.ts, apps/site/server/api/v1/admin/theme.put.ts</files>
  <behavior>
    - Test: GET /api/v1/admin/plugins returns list of installed plugins
    - Test: PUT /api/v1/admin/plugins enables/disables plugin
    - Test: GET /api/v1/admin/theme returns current theme config
    - Test: PUT /api/v1/admin/theme updates theme settings
    - Test: All endpoints require authentication
  </behavior>
  <action>
    Create basic management endpoints (stub implementation for Phase 4, full implementation in Phase 8):

    plugins.get.ts:
    - Protected endpoint
    - List available plugins from packages/plugins/ directory
    - Read plugin status from database (plugins table)
    - Return list with { name, version, enabled, config }

    plugins.put.ts:
    - Protected endpoint
    - Accept { pluginId, enabled, config? }
    - Update plugin status in database
    - Return updated plugin info

    theme.get.ts:
    - Protected endpoint
    - Get current theme from database (themes table)
    - Return theme config

    theme.put.ts:
    - Protected endpoint
    - Accept theme configuration
    - Update in database
    - Return updated config

    Note: Full plugin/theme management with hot-reload will be implemented in Phase 8

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/admin</automated>
  </verify>
  <done>Plugin and theme management stub endpoints created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify media and management API</name>
  <what-built>
    - GET/POST /api/v1/media - list and upload media
    - GET/DELETE /api/v1/media/:id - single media operations
    - Storage provider abstraction (local + S3)
    - Thumbnail generation for images
    - Plugin and theme management stub endpoints
  </what-built>
  <how-to-verify>
    1. Ensure dev server running and admin logged in
    2. Test media list (empty): `curl http://localhost:3000/api/v1/media`
       - Expected: 200 with empty array
    3. Upload image: `curl -X POST http://localhost:3000/api/v1/media -H "Authorization: Bearer $TOKEN" -F "file=@test-image.jpg"`
       - Expected: 201 with media metadata including thumbnail URL
    4. Check file exists: `ls apps/site/uploads/original/`
    5. Check thumbnail exists: `ls apps/site/uploads/thumbnails/`
    6. Test plugin list: `curl http://localhost:3000/api/v1/admin/plugins -H "Authorization: Bearer $TOKEN"`
       - Expected: 200 with plugin list (may be empty)
    7. Test theme config: `curl http://localhost:3000/api/v1/admin/theme -H "Authorization: Bearer $TOKEN"`
       - Expected: 200 with current theme settings
  </how-to-verify>
  <resume-signal>Type "approved" if media upload and management work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Storage service with local and S3 providers
- [ ] Media service with upload and thumbnail generation
- [ ] All 4 media endpoints created
- [ ] Plugin and theme management stub endpoints
- [ ] File upload handles multipart/form-data
- [ ] Thumbnails generated for images
- [ ] Tests passing
</verification>

<success_criteria>

- File upload accepts multipart/form-data
- Images get thumbnails generated automatically
- Storage provider abstraction allows switching via env var
- Media metadata stored in database
- Plugin and theme endpoints return/accept configuration
- Delete removes both file and database record
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-05-SUMMARY.md` with:
- Media API summary
- Storage provider configuration
- Test results
- Phase 4 completion status
</output>
