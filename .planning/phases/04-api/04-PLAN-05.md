---
phase: 04-api
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/server/plugins/swagger.ts
  - apps/site/server/openapi/schemas.ts
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - 'Swagger UI accessible at /docs (dev only)'
    - 'OpenAPI spec generated from Zod schemas'
    - 'All API endpoints documented with tags'
  artifacts:
    - path: 'apps/site/server/plugins/swagger.ts'
      provides: 'Swagger UI plugin configuration'
      min_lines: 40
    - path: 'apps/site/server/openapi/schemas.ts'
      provides: 'OpenAPI schema definitions with metadata'
      exports: ['openApiSchemas', 'generateOpenAPISpec']
  key_links:
    - from: 'apps/site/server/plugins/swagger.ts'
      to: '@asteasolutions/zod-to-openapi'
      via: 'OpenAPI generation'
      pattern: 'extendApi'
---

<objective>
创建 Swagger/OpenAPI 文档

Purpose: 提供 API 文档和测试界面

Output: 1 个插件文件，1 个 OpenAPI schema 文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Swagger/OpenAPI documentation</name>
  <files>apps/site/server/plugins/swagger.ts, apps/site/server/openapi/schemas.ts</files>
  <action>
    Create swagger.ts plugin with:
    - Use @asteasolutions/zod-to-openapi to generate OpenAPI spec from Zod schemas
    - Mount Swagger UI at /docs endpoint (dev only)
    - Generate OpenAPI JSON at /api/docs/json endpoint
    - Document all API endpoints with tags: Auth, Posts, Categories, Tags, Media
    - Include security schemes for JWT Bearer token

    Create openapi/schemas.ts with:
    - Re-export Zod schemas with OpenAPI metadata (.openapi() method)
    - Define common response schemas (SuccessResponse, ErrorResponse, PaginationResponse)
    - Define parameter schemas (PaginationParams, ListQueryParams)

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- server/openapi</automated>
  </verify>
  <done>Swagger UI accessible at /docs, OpenAPI spec generated from Zod schemas</done>
</task>

</tasks>

<verification>
- [ ] swagger.ts plugin created
- [ ] openapi/schemas.ts created
- [ ] Swagger UI renders at /docs
</verification>

<output>
After completion, create `.planning/phases/04-api/04-05-SUMMARY.md`
</output>
