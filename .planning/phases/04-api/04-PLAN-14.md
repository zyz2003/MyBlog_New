---
phase: 04-api
plan: 14
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
  - 04-PLAN-03
  - 04-PLAN-04
files_modified:
  - apps/site/server/services/storage.service.ts
  - apps/site/server/services/media.service.ts
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - 'Storage provider abstraction allows switching between local and S3'
    - 'Image uploads generate thumbnails using sharp'
    - 'Media service stores file metadata in database'
  artifacts:
    - path: 'apps/site/server/services/storage.service.ts'
      provides: 'Storage provider abstraction (local + S3)'
      exports:
        ['StorageProvider', 'LocalStorageProvider', 'S3StorageProvider', 'getStorageProvider']
      min_lines: 80
    - path: 'apps/site/server/services/media.service.ts'
      provides: 'Media CRUD operations'
      exports: ['listMedia', 'uploadMedia', 'deleteMedia', 'getMediaById']
      min_lines: 50
  key_links:
    - from: 'apps/site/server/services/storage.service.ts'
      to: 'sharp'
      via: 'image thumbnail generation'
      pattern: 'sharp\\('
---

<objective>
创建存储服务和媒体服务层

Purpose: 提供文件存储抽象和媒体资源管理

Output: 2 个服务文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create storage and media services</name>
  <files>apps/site/server/services/storage.service.ts, apps/site/server/services/media.service.ts</files>
  <behavior>
    - Test: LocalStorageProvider saves file to disk
    - Test: getStorageProvider returns configured provider
    - Test: uploadMedia saves file and creates media record
    - Test: deleteMedia removes file and database record
  </behavior>
  <action>
    Create storage.service.ts with:
    - StorageProvider interface: save(file), delete(path), getUrl(path)
    - LocalStorageProvider: saves to uploads/ directory
    - S3StorageProvider: saves to S3-compatible storage
    - getStorageProvider(): returns configured provider based on config

    Create media.service.ts with:
    - uploadMedia(file, metadata): saves file, creates media record with thumbnail
    - listMedia(query): paginated list
    - getMediaById(id): single media with metadata
    - deleteMedia(id): deletes file and record

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- 'services/(storage|media)'</automated>
  </verify>
  <done>Storage and media services created and tested</done>
</task>

</tasks>

<verification>
- [ ] storage.service.ts created
- [ ] media.service.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-14-SUMMARY.md`
</output>
