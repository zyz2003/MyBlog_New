---
phase: 04-api
plan: 03
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
files_modified:
  - apps/site/server/api/v1/posts/index.get.ts
  - apps/site/server/api/v1/posts/index.post.ts
  - apps/site/server/api/v1/posts/[id].get.ts
  - apps/site/server/api/v1/posts/[id].put.ts
  - apps/site/server/api/v1/posts/[id].delete.ts
  - apps/site/server/api/v1/posts/bulk.delete.ts
  - apps/site/server/services/post.service.ts
  - apps/site/server/schemas/post.ts
  - apps/site/server/utils/slug.ts
  - apps/site/server/utils/markdown.ts
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - 'User can fetch list of published posts with pagination'
    - 'User can fetch a single post by ID or slug'
    - 'Authenticated user can create a new post'
    - 'Authenticated user can update an existing post'
    - 'Authenticated user can delete a post'
    - 'Created post has auto-generated slug from title'
    - 'Post list supports filtering by category, tag, status, search'
  artifacts:
    - path: 'apps/site/server/services/post.service.ts'
      provides: 'Post business logic with transactions'
      exports: ['createPost', 'updatePost', 'deletePost', 'getPostById', 'listPosts']
      min_lines: 80
    - path: 'apps/site/server/api/v1/posts/index.get.ts'
      provides: 'GET /api/v1/posts - list with pagination and filters'
      min_lines: 30
    - path: 'apps/site/server/api/v1/posts/[id].get.ts'
      provides: 'GET /api/v1/posts/:id - single post with relations'
      min_lines: 20
  key_links:
    - from: 'apps/site/server/services/post.service.ts'
      to: 'db.transaction'
      via: 'transactional post+tag operations'
      pattern: "db\\.transaction"
    - from: 'apps/site/server/utils/slug.ts'
      to: 'pinyin-pro'
      via: 'Chinese to pinyin conversion'
      pattern: "pinyin\\("
    - from: 'apps/site/server/api/v1/posts/index.get.ts'
      to: 'db.query.posts.findMany'
      via: 'Drizzle query with relations'
      pattern: 'findMany.*with:'
---

<objective>
实现文章 CRUD API：创建、读取、更新、删除、列表查询

Purpose: 提供完整的文章管理接口，支持草稿/发布状态、分类标签关联、SEO 字段、分页筛选

Output: 6 个 API 端点文件、1 个 post 服务文件、schema 和工具函数
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
@.planning/phases/04-api/04-PLAN-01.md
@.planning/phases/04-api/04-PLAN-02.md

# Database schemas

@packages/database/src/schema/posts.ts
@packages/database/src/schema/post_tags.ts
@packages/database/src/schema/tags.ts
@packages/database/src/schema/categories.ts
@packages/database/src/db.ts
</context>

<interfaces>
<!-- Types and utilities from Plan 01/02 -->

From apps/site/server/utils/response.ts:

```typescript
export function createSuccessResponse<T>(data: T): SuccessResponse<T>
export function createPaginationResponse<T>(
  data: T[],
  meta: { total; limit; offset }
): PaginationResponse<T>
```

From apps/site/server/utils/validate.ts:

```typescript
export function validateBody<T>(body: unknown, schema: z.ZodType<T>): z.infer<T>
export function validateQuery<T>(query: unknown, schema: z.ZodType<T>): z.infer<T>
export function validateParams<T>(params: unknown, schema: z.ZodType<T>): z.infer<T>
```

From apps/site/server/middleware/auth.ts:

```typescript
// event.context.user available on protected routes
event.context.user: { id: string; role: string }
```

From packages/database/src/schema/posts.ts:

```typescript
export type Post = typeof posts.$inferSelect
export const posts: SQLiteTable
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create post schemas and utilities</name>
  <files>apps/site/server/schemas/post.ts, apps/site/server/utils/slug.ts, apps/site/server/utils/markdown.ts</files>
  <behavior>
    - Test: createPostSchema accepts valid post data with title, content
    - Test: createPostSchema requires title (min 1, max 200) and content (min 1)
    - Test: createPostSchema has optional fields: excerpt, coverImage, categoryId, tagIds, status, seoTitle, seoDescription, isPinned
    - Test: generateSlug("中文标题") returns "zhong-wen-biao-ti" or similar pinyin
    - Test: generateSlug("Hello World") returns "hello-world"
    - Test: generateSlug checks uniqueness and appends suffix if slug exists
    - Test: renderMarkdown("**bold**") returns HTML with <strong>bold</strong>
    - Test: renderMarkdown includes Shiki code highlighting for ```code blocks
  </behavior>
  <action>
    Create post.ts schema with:
    - `createPostSchema`: { title (1-200), content (min 1), excerpt? (max 500), coverImage?, categoryId?, tagIds (default []), status (draft|published, default draft), seoTitle? (max 60), seoDescription? (max 160), isPinned (default false) }
    - `updatePostSchema`: createPostSchema.partial()
    - `postParamsSchema`: { id: string }
    - `listPostsQuerySchema`: extends pagination with category?, tag?, status?, search?, sort?, order?

    Create slug.ts utility:
    - `generateSlug(title: string): Promise<string>` - convert Chinese to pinyin using pinyin-pro
    - Check uniqueness in database, append -1, -2 if exists
    - Use nanoid for uniqueness if needed

    Create markdown.ts utility:
    - `renderMarkdown(content: string): string` - render Markdown to HTML
    - Use shiki for code block highlighting
    - Support extended syntax: tables, task lists, etc.

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- server/schemas/post</automated>
  </verify>
  <done>Post schemas and utilities created with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create post service</name>
  <files>apps/site/server/services/post.service.ts</files>
  <behavior>
    - Test: createPost creates post with auto-generated slug
    - Test: createPost with tagIds creates post_tags relations in transaction
    - Test: createPost calculates wordCount from content
    - Test: updatePost updates fields and maintains tag relations
    - Test: updatePost with version field checks for concurrent modifications
    - Test: deletePost removes post and cascades post_tags
    - Test: getPostById returns post with author, category, tags relations
    - Test: listPosts returns paginated list with optional filters
    - Test: listPosts with category filter only returns posts in that category
    - Test: listPosts with search filters by title and content
  </behavior>
  <action>
    Create post.service.ts with:

    - `createPost(data, authorId)`:
      - Generate slug from title
      - Calculate wordCount
      - Use db.transaction for post + post_tags insertion
      - Return post with relations

    - `updatePost(id, data)`:
      - Get existing post for version check
      - Generate new slug if title changed
      - Update post with version check (optimistic locking)
      - Update post_tags in transaction (delete old, insert new)
      - Return updated post with relations

    - `deletePost(id)`:
      - Check existence
      - Delete (cascade handles post_tags)

    - `getPostById(id)`:
      - Query with relations: author (id, username, avatar), category (id, name, slug), postTags.tag (id, name, slug)

    - `listPosts(options)`:
      - Build dynamic conditions for category, tag, status, search
      - Use Drizzle relations to include author, category, tags
      - Return { items, total, limit, offset }

    IMPORTANT: Use transactions for all multi-table operations

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/post</automated>
  </verify>
  <done>Post service with CRUD operations and transactions working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create list and get single post endpoints</name>
  <files>apps/site/server/api/v1/posts/index.get.ts, apps/site/server/api/v1/posts/[id].get.ts</files>
  <behavior>
    - Test: GET /api/v1/posts returns paginated list with { success: true, data: [], meta: { total, limit, offset } }
    - Test: GET /api/v1/posts?limit=5&offset=10 returns correct pagination
    - Test: GET /api/v1/posts?category=xxx filters by category
    - Test: GET /api/v1/posts?search=keyword searches title and content
    - Test: GET /api/v1/posts?status=draft requires auth (protected)
    - Test: GET /api/v1/posts/:id returns single post with author, category, tags
    - Test: GET /api/v1/posts/:id with invalid ID returns 404 POST_NOT_FOUND
    - Test: Public posts (status=published) accessible without auth
  </behavior>
  <action>
    Create index.get.ts:
    - Public endpoint (optional auth)
    - Use validateQuery with listPostsQuerySchema
    - Call postService.listPosts(options)
    - Return createPaginationResponse(posts, meta)

    Create [id].get.ts:
    - Public endpoint (anyone can read published posts)
    - Use validateParams with postParamsSchema
    - Call postService.getPostById(id)
    - Return createSuccessResponse(post)
    - Handle 404 if not found

    For list: include author info, category, tags in response
    Filter out draft posts for unauthenticated users

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/posts/index.get</automated>
  </verify>
  <done>List and get single post endpoints working with pagination and filters</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create create and update post endpoints</name>
  <files>apps/site/server/api/v1/posts/index.post.ts, apps/site/server/api/v1/posts/[id].put.ts</files>
  <behavior>
    - Test: POST /api/v1/posts without auth returns 401
    - Test: POST /api/v1/posts with valid data creates post and returns 201
    - Test: POST auto-generates slug from title
    - Test: POST with tagIds creates post_tags relations
    - Test: PUT /api/v1/posts/:id updates post
    - Test: PUT with new tagIds updates relations
    - Test: PUT with concurrent modification returns 409 CONFLICT
    - Test: PUT with invalid ID returns 404
  </behavior>
  <action>
    Create index.post.ts:
    - Protected endpoint (require auth)
    - Use validateBody with createPostSchema
    - Call postService.createPost(data, event.context.user.id)
    - Return createSuccessResponse(post, 'Post created') with status 201

    Create [id].put.ts:
    - Protected endpoint (require auth)
    - Use validateBody with updatePostSchema
    - Use validateParams with postParamsSchema
    - Call postService.updatePost(id, data)
    - Return createSuccessResponse(post, 'Post updated')
    - Handle 409 CONFLICT for version mismatch

    Author can only edit their own posts (check authorId === user.id)

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/posts/index.post</automated>
  </verify>
  <done>Create and update post endpoints working with validation</done>
</task>

<task type="auto">
  <name>Task 5: Create delete and bulk delete endpoints</name>
  <files>apps/site/server/api/v1/posts/[id].delete.ts, apps/site/server/api/v1/posts/bulk.delete.ts</files>
  <action>
    Create [id].delete.ts:
    - Protected endpoint
    - Validate ID param
    - Call postService.deletePost(id)
    - Return createSuccessResponse({ message: 'Post deleted' })
    - Handle 404

    Create bulk.delete.ts:
    - Protected endpoint
    - Accept { ids: string[] } in body
    - Delete all posts in transaction
    - Return createSuccessResponse({ deletedCount: number })
    - Handle partial failures

    Author can only delete their own posts

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/posts/[id].delete</automated>
  </verify>
  <done>Delete endpoints working for single and bulk operations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify posts API end-to-end</name>
  <what-built>
    - GET /api/v1/posts - list with pagination, filters
    - GET /api/v1/posts/:id - single post with relations
    - POST /api/v1/posts - create new post
    - PUT /api/v1/posts/:id - update post
    - DELETE /api/v1/posts/:id - delete post
    - DELETE /api/v1/posts/bulk - bulk delete
    - Auto slug generation, word count, transaction handling
  </what-built>
  <how-to-verify>
    1. Ensure dev server running and admin user exists
    2. Login to get token: `curl -X POST http://localhost:3000/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'`
    3. Save token: TOKEN="eyJ..."
    4. Test list (empty): `curl http://localhost:3000/api/v1/posts`
       - Expected: 200 with { success: true, data: [], meta: {...} }
    5. Create post: `curl -X POST http://localhost:3000/api/v1/posts -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"title":"测试文章","content":"# Hello\n\nContent here","status":"published"}'`
       - Expected: 201 with post object including auto-generated slug
    6. Get single post: `curl http://localhost:3000/api/v1/posts/{id}`
       - Expected: 200 with post including author, category, tags
    7. Update post: `curl -X PUT http://localhost:3000/api/v1/posts/{id} -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"title":"Updated Title"}'`
       - Expected: 200 with updated post
    8. Delete post: `curl -X DELETE http://localhost:3000/api/v1/posts/{id} -H "Authorization: Bearer $TOKEN"`
       - Expected: 200 with success message
  </how-to-verify>
  <resume-signal>Type "approved" if all CRUD operations work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Post service with CRUD operations created
- [ ] All 6 API endpoints created
- [ ] Slug generation working for Chinese and English
- - [ ] Transactions used for post+tag operations
- [ ] Pagination and filters working
- [ ] Optimistic locking with version field
- [ ] Tests passing
</verification>

<success_criteria>

- GET /api/v1/posts returns paginated list with filters
- POST creates post with auto-generated slug and word count
- PUT updates post with optimistic locking
- DELETE removes post and cascades relations
- All operations use transactions where needed
- Draft posts only visible to authenticated users
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-03-SUMMARY.md` with:
- Posts API endpoints summary
- Test results
- Any issues with slug generation or transactions
- Ready for Plan 04 (Categories/Tags API) execution
</output>
