---
phase: 04-api
plan: 07
type: execute
wave: 2
depends_on:
  - 04-PLAN-06
files_modified:
  - apps/site/server/api/v1/auth/me.get.ts
  - apps/site/server/api/v1/auth/register.post.ts
autonomous: true
requirements:
  - API-05

must_haves:
  truths:
    - 'Logged in user can fetch their profile via GET /api/v1/auth/me'
    - 'User can register with username, password, email'
    - 'Duplicate username returns 409 CONFLICT'
  artifacts:
    - path: 'apps/site/server/api/v1/auth/me.get.ts'
      provides: 'GET /api/v1/auth/me endpoint'
      min_lines: 15
    - path: 'apps/site/server/api/v1/auth/register.post.ts'
      provides: 'POST /api/v1/auth/register endpoint'
      min_lines: 25
  key_links:
    - from: 'apps/site/server/api/v1/auth/me.get.ts'
      to: 'event.context.user'
      via: 'auth middleware'
      pattern: 'event\\.context\\.user'
---

<objective>
实现认证 API：获取当前用户、用户注册

Purpose: 提供用户信息查询和注册功能

Output: 2 个 API 端点
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create me and register endpoints</name>
  <files>apps/site/server/api/v1/auth/me.get.ts, apps/site/server/api/v1/auth/register.post.ts</files>
  <behavior>
    - Test: GET /api/v1/auth/me with valid token returns current user
    - Test: GET /api/v1/auth/me without token returns 401
    - Test: POST /api/v1/auth/register creates user and returns 201
    - Test: POST /api/v1/auth/register with duplicate username returns 409
  </behavior>
  <action>
    Create me.get.ts:
    - Require auth middleware (protected route)
    - Return user from event.context.user
    - Return { success: true, data: { user } }

    Create register.post.ts:
    - Parse body with validateBody(registerSchema)
    - Check if username exists
    - Hash password with bcrypt
    - Create user in database
    - Return { success: true, data: { user }, message: 'Registered' }

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/auth</automated>
  </verify>
  <done>me and register endpoints created and tested</done>
</task>

</tasks>

<verification>
- [ ] me.get.ts created
- [ ] register.post.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-07-SUMMARY.md`
</output>
