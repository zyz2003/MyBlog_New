---
phase: 04-api
plan: 09
type: execute
wave: 2
depends_on:
  - 04-PLAN-08
files_modified:
  - apps/site/server/api/v1/posts/index.get.ts
  - apps/site/server/api/v1/posts/[id].get.ts
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - 'User can fetch list of published posts with pagination'
    - 'User can fetch a single post by ID or slug'
    - 'Post list supports filtering by category, tag, status, search'
  artifacts:
    - path: 'apps/site/server/api/v1/posts/index.get.ts'
      provides: 'GET /api/v1/posts - list with pagination and filters'
      min_lines: 30
    - path: 'apps/site/server/api/v1/posts/[id].get.ts'
      provides: 'GET /api/v1/posts/:id - single post with relations'
      min_lines: 20
  key_links:
    - from: 'apps/site/server/api/v1/posts/index.get.ts'
      to: 'post.service.listPosts'
      via: 'service call'
      pattern: 'listPosts\\('
---

<objective>
实现文章查询 API：列表、详情

Purpose: 提供文章读取功能

Output: 2 个 API 端点
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create list and get endpoints</name>
  <files>apps/site/server/api/v1/posts/index.get.ts, apps/site/server/api/v1/posts/[id].get.ts</files>
  <behavior>
    - Test: GET /api/v1/posts returns paginated list
    - Test: GET /api/v1/posts?category=x filters by category
    - Test: GET /api/v1/posts/:id returns post with category and tags
    - Test: GET /api/v1/posts/:slug returns post by slug
    - Test: GET /api/v1/posts/:not-found returns 404
  </behavior>
  <action>
    Create index.get.ts:
    - Parse query with validateQuery(listQuerySchema)
    - Call post.service.listPosts()
    - Return { success: true, data: posts, meta: { total, limit, offset } }

    Create [id].get.ts:
    - Get id/slug from params
    - Call post.service.getPostById()
    - Return { success: true, data: post }

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- api/v1/posts</automated>
  </verify>
  <done>List and get endpoints created and tested</done>
</task>

</tasks>

<verification>
- [ ] index.get.ts created
- [ ] [id].get.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-09-SUMMARY.md`
</output>
