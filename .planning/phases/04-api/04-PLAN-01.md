---
phase: 04-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/server/middleware/auth.ts
  - apps/site/server/middleware/error.ts
  - apps/site/server/middleware/logger.ts
  - apps/site/server/middleware/rate-limit.ts
  - apps/site/server/utils/response.ts
  - apps/site/server/utils/error.ts
  - apps/site/server/utils/validate.ts
  - apps/site/server/schemas/auth.ts
  - apps/site/server/schemas/common.ts
autonomous: true
requirements:
  - API-01
  - API-08
  - API-09

must_haves:
  truths:
    - 'Protected API routes reject requests without valid JWT token'
    - 'Invalid requests return standardized error response with Zod validation errors'
    - 'All responses follow unified format: { success, data/error, message }'
    - 'Request logs show method, path, status, duration for every request'
    - 'Rate limiting blocks IPs exceeding 100 req/min'
  artifacts:
    - path: 'apps/site/server/middleware/auth.ts'
      provides: 'JWT authentication middleware'
      min_lines: 40
    - path: 'apps/site/server/middleware/error.ts'
      provides: 'Global error handler'
      min_lines: 25
    - path: 'apps/site/server/utils/response.ts'
      provides: 'Unified response formatting utilities'
      exports: ['createSuccessResponse', 'createErrorResponse', 'createPaginationResponse']
    - path: 'apps/site/server/schemas/auth.ts'
      provides: 'Auth request validation schemas'
      exports: ['loginSchema', 'registerSchema']
  key_links:
    - from: 'apps/site/server/middleware/auth.ts'
      to: 'jose.jwtVerify'
      via: 'token verification'
      pattern: 'jwtVerify.*JWT_SECRET'
    - from: 'apps/site/server/utils/validate.ts'
      to: 'zod.parse'
      via: 'schema validation'
      pattern: "schema\\.parse"
---

<objective>
建立 API 核心基础设施：认证中间件、错误处理、请求验证、统一响应格式

Purpose: 为所有 API 端点提供一致的安全保障、错误处理和响应格式，避免重复代码

Output: 4 个中间件文件、3 个工具文件、2 个 schema 文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md

# Database schema for type references

@packages/database/src/schema/users.ts
@packages/database/src/schema/posts.ts
@packages/database/src/db.ts
</context>

<interfaces>
<!-- Database types the API middleware will use -->

From packages/database/src/schema/users.ts:

```typescript
export type User = typeof users.$inferSelect
export type NewUser = typeof users.$inferInsert
```

From packages/database/src/db.ts:

```typescript
export const db = {
  get: () => {
    /* returns Drizzle database instance */
  },
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unified response and error utilities</name>
  <files>apps/site/server/utils/response.ts, apps/site/server/utils/error.ts</files>
  <behavior>
    - Test: createSuccessResponse({ id: 1 }) returns { success: true, data: { id: 1 } }
    - Test: createErrorResponse('NOT_FOUND', 'Resource not found') returns { success: false, error: { code: 'NOT_FOUND', message: 'Resource not found' } }
    - Test: createPaginationResponse([...items], { total: 100, limit: 10, offset: 0 }) includes meta object
    - Test: HTTPError class can be thrown and caught with statusCode, code, message, details
  </behavior>
  <action>
    Create response.ts with:
    - `createSuccessResponse<T>(data: T, message?: string)` → SuccessResponse<T>
    - `createErrorResponse(code: string, message: string, details?)` → ErrorResponse
    - `createPaginationResponse<T>(data: T[], meta: { total, limit, offset })` → PaginationResponse<T>

    Create error.ts with:
    - `HTTPError` class extending Error with: statusCode (number), code (string), message (string), details (optional array)
    - Predefined error codes as constants: UNAUTHORIZED, INVALID_TOKEN, VALIDATION_ERROR, NOT_FOUND, SERVER_ERROR

    These utilities will be used by all subsequent API routes.

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- server/utils</automated>
  </verify>
  <done>Response and error utilities created with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zod validation middleware</name>
  <files>apps/site/server/utils/validate.ts, apps/site/server/schemas/auth.ts, apps/site/server/schemas/common.ts</files>
  <behavior>
    - Test: validateBody with valid data returns parsed data
    - Test: validateBody with invalid data throws HTTPError(400, 'VALIDATION_ERROR', ...)
    - Test: loginSchema accepts { username: "user", password: "pass123" }
    - Test: loginSchema rejects { username: "" } or missing password
    - Test: paginationSchema with limit=200 rejects (max 100)
  </behavior>
  <action>
    Create validate.ts with:
    - `validateBody<T>(body: unknown, schema: z.ZodType<T>): T` - validates request body
    - `validateQuery<T>(query: unknown, schema: z.ZodType<T>): T` - validates query params
    - `validateParams<T>(params: unknown, schema: z.ZodType<T>): T` - validates route params
    - Each throws HTTPError(400, 'VALIDATION_ERROR') with field-level details on failure

    Create auth.ts schema with:
    - `loginSchema`: { username: string (1-50), password: string (6-100) }
    - `registerSchema`: { username, password, email (zod email validation) }

    Create common.ts with:
    - `paginationSchema`: { limit: 1-100 default 10, offset: 0+ default 0 }
    - `listQuerySchema`: extends pagination with category?, tag?, status?, search?, sort?, order?

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- server/schemas</automated>
  </verify>
  <done>Validation utilities and schemas created with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create authentication middleware</name>
  <files>apps/site/server/middleware/auth.ts</files>
  <behavior>
    - Test: Request without Authorization header to protected route throws 401 UNAUTHORIZED
    - Test: Request with invalid token throws 401 INVALID_TOKEN
    - Test: Request with valid token attaches user to event.context.user
    - Test: Public routes (/api/v1/auth/login) skip authentication
    - Test: Optional auth attaches user if token valid, ignores if invalid (for public routes)
  </behavior>
  <action>
    Create auth.ts middleware:
    - Define JWT_SECRET from process.env.JWT_SECRET (default 'change-me' for dev)
    - Define public paths: ['/api/v1/auth/login', '/api/v1/auth/register']
    - Define public prefixes for read: ['/api/v1/posts', '/api/v1/categories', '/api/v1/tags']
    - For protected routes (/api/v1/admin/*, POST/PUT/DELETE): require valid Bearer token
    - Use jose.jwtVerify() to verify token signature and expiry
    - Attach { id: string, role: string } to event.context.user
    - Implement optionalAuth() helper for public routes that attaches user if token present

    JWT config per CONTEXT.md:
    - 30-day expiry
    - HS256 algorithm
    - Secret from environment variable

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- middleware/auth</automated>
  </verify>
  <done>Auth middleware correctly protects routes and attaches user context</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create error handler and logger middleware</name>
  <files>apps/site/server/middleware/error.ts, apps/site/server/middleware/logger.ts</files>
  <behavior>
    - Test: HTTPError thrown in route handler returns proper JSON response with status code
    - Test: Unknown errors return 500 SERVER_ERROR with sanitized message
    - Test: Every request logs: [HTTP] GET /api/v1/posts 200 15ms
    - Test: Error responses include error code and message in response body
  </behavior>
  <action>
    Create error.ts middleware:
    - Use defineEventHandler to wrap all route handlers
    - Catch HTTPError: return createErrorResponse with matching status code
    - Catch unknown errors: log full error, return 500 SERVER_ERROR
    - Set proper Content-Type: application/json header

    Create logger.ts middleware:
    - Use defineEventHandler with onRequest hook
    - Record start time with performance.now()
    - Log: method, path, status code, duration in ms
    - Format: `[HTTP] ${method} ${path} ${status} ${duration}ms`

    Register middleware in nuxt.config.ts nitro.plugins array

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- middleware/error</automated>
  </verify>
  <done>Error handler and logger middleware created and tested</done>
</task>

<task type="auto">
  <name>Task 5: Create rate limiting middleware</name>
  <files>apps/site/server/middleware/rate-limit.ts</files>
  <action>
    Create rate-limit.ts middleware:
    - Track requests per IP using in-memory Map: Map<string, { count: number, resetAt: number }>
    - Per CONTEXT.md: 100 requests per minute per IP
    - On each request: check IP count, increment or reset if expired
    - If count > 100: return 429 TOO_MANY_REQUESTS with { success: false, error: { code: 'RATE_LIMIT_EXCEEDED', message: 'Too many requests' } }
    - Set X-RateLimit-Limit and X-RateLimit-Remaining headers

    Note: This is a simple in-memory implementation. Production could upgrade to Redis-backed rate limiting.

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- middleware/rate-limit</automated>
  </verify>
  <done>Rate limiting middleware blocks IPs exceeding 100 req/min</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify middleware integration</name>
  <what-built>
    - Auth middleware protects routes with JWT verification
    - Error handler catches and formats all errors
    - Logger logs every request with timing
    - Rate limiter blocks excessive requests
    - Validation utilities with Zod schemas
    - Unified response format utilities
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Test public endpoint: `curl http://localhost:3000/api/v1/posts` - should return 200 (empty list)
    3. Test protected endpoint without token: `curl -X POST http://localhost:3000/api/v1/posts -H "Content-Type: application/json" -d '{}'` - should return 401
    4. Test invalid JSON: `curl -X POST http://localhost:3000/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":""}'` - should return 400 VALIDATION_ERROR
    5. Check server logs show: `[HTTP] POST /api/v1/auth/login 400 Xms`
  </how-to-verify>
  <resume-signal>Type "approved" if all endpoints behave correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] All middleware files created in apps/site/server/middleware/
- [ ] All utility files created in apps/site/server/utils/
- [ ] All schema files created in apps/site/server/schemas/
- [ ] Middleware registered in nuxt.config.ts
- [ ] Tests pass for all created utilities
- [ ] Server starts without errors
</verification>

<success_criteria>

- Unified response format used consistently
- Auth middleware correctly identifies public vs protected routes
- JWT verification uses jose library with 30-day expiry
- Zod validation provides field-level error details
- Rate limiting enforces 100 req/min per IP
- Request logging shows method, path, status, duration
  </success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-01-SUMMARY.md` with:
- List of created files
- Test results summary
- Any issues encountered
- Ready for Plan 02 (Auth API) execution
</output>
