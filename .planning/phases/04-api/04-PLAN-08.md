---
phase: 04-api
plan: 08
type: execute
wave: 2
depends_on:
  - 04-PLAN-01
  - 04-PLAN-02
  - 04-PLAN-03
  - 04-PLAN-04
files_modified:
  - apps/site/server/services/post.service.ts
  - apps/site/server/utils/slug.ts
  - apps/site/server/utils/markdown.ts
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - 'Post service uses transactions for post+tag operations'
    - 'Slug generated from Chinese title using pinyin-pro'
    - 'Markdown content can be optionally cached as HTML'
  artifacts:
    - path: 'apps/site/server/services/post.service.ts'
      provides: 'Post business logic with transactions'
      exports: ['createPost', 'updatePost', 'deletePost', 'getPostById', 'listPosts']
      min_lines: 80
    - path: 'apps/site/server/utils/slug.ts'
      provides: 'Chinese to pinyin slug generation'
      exports: ['generateSlug']
    - path: 'apps/site/server/utils/markdown.ts'
      provides: 'Markdown to HTML conversion with Shiki highlighting'
      exports: ['renderMarkdown']
  key_links:
    - from: 'apps/site/server/services/post.service.ts'
      to: 'db.transaction'
      via: 'transactional post+tag operations'
      pattern: 'db\\.transaction'
    - from: 'apps/site/server/utils/slug.ts'
      to: 'pinyin-pro'
      via: 'Chinese to pinyin conversion'
      pattern: 'pinyin\\('
---

<objective>
创建文章服务层和工具类

Purpose: 提供文章业务逻辑、slug 生成、Markdown 渲染

Output: 1 个服务文件，2 个工具文件
</objective>

<execution_context>
@C:/Users/ZhangYaZhou/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ZhangYaZhou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api/04-CONTEXT.md
@.planning/phases/04-api/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create post service</name>
  <files>apps/site/server/services/post.service.ts</files>
  <behavior>
    - Test: createPost returns created post with tags
    - Test: createPost uses transaction for post+postTags
    - Test: listPosts returns paginated list with relations
    - Test: getPostById returns post with category and tags
    - Test: updatePost updates post and tag relations
    - Test: deletePost removes post and postTags
  </behavior>
  <action>
    Create post.service.ts with:
    - createPost(data, tags): uses db.transaction, generates slug, returns post
    - updatePost(id, data, tags): transactional update
    - deletePost(id): removes post and postTags
    - getPostById(id, options): includes category, tags, author
    - listPosts(query): pagination, filters (category, tag, status, search), sorting

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- services/post</automated>
  </verify>
  <done>Post service created with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create slug and markdown utilities</name>
  <files>apps/site/server/utils/slug.ts, apps/site/server/utils/markdown.ts</files>
  <behavior>
    - Test: generateSlug('中文标题') returns 'zhong-wen-biao-ti'
    - Test: generateSlug ensures uniqueness with incremental suffix
    - Test: renderMarkdown('# Hello') returns HTML with highlighted code
  </behavior>
  <action>
    Create slug.ts with:
    - generateSlug(title, existingSlugs): uses pinyin-pro for Chinese, ensures uniqueness

    Create markdown.ts with:
    - renderMarkdown(content): uses Shiki for code highlighting
    - Support for custom component syntax [ComponentName]

  </action>
  <verify>
    <automated>pnpm -r test --filter=@my-blog/site -- 'utils/(slug|markdown)'</automated>
  </verify>
  <done>Slug and markdown utilities created and tested</done>
</task>

</tasks>

<verification>
- [ ] post.service.ts created
- [ ] slug.ts, markdown.ts created
- [ ] Tests pass
</verification>

<output>
After completion, create `.planning/phases/04-api/04-08-SUMMARY.md`
</output>
