# Plan 03-01: Database Package Setup and Connection

## Goal

创建 packages/database 包，配置 SQLite 数据库连接（WAL 模式），集成 Drizzle ORM。

## Tasks

### 1. 创建 packages/database 包结构

**Files to create:**

- `packages/database/package.json`
- `packages/database/tsconfig.json`
- `packages/database/src/index.ts`
- `packages/database/src/db.ts`
- `packages/database/drizzle.config.ts`

**package.json configuration:**

```json
{
  "name": "@my-blog/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate:sqlite",
    "db:push": "drizzle-kit push:sqlite",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "drizzle-kit": "^0.20.0",
    "tsx": "^4.0.0",
    "vitest": "^1.0.0"
  }
}
```

### 2. 创建数据库连接文件 (db.ts)

**Implementation:**

```typescript
import { Database } from 'better-sqlite3'
import { drizzle } from 'drizzle-orm/better-sqlite3'
import * as schema from './schema'

interface DatabaseConfig {
  dbPath: string
  enableSlowQueryLog?: boolean
  slowQueryThreshold?: number
}

export function createDatabase(config: DatabaseConfig) {
  const { dbPath, enableSlowQueryLog = true, slowQueryThreshold = 100 } = config

  const db = new Database(dbPath)

  // Enable WAL mode for better concurrency (CONTEXT.md decision)
  db.pragma('journal_mode = WAL')

  // Increase cache size for better performance
  db.pragma('cache_size = -64000') // 64MB cache

  // Enable foreign keys
  db.pragma('foreign_keys = ON')

  // Slow query logging
  if (enableSlowQueryLog) {
    db.pragma(`trusted_schema = ON`)
    // Log queries taking longer than threshold (ms)
    console.log(`[Database] Slow query logging enabled (threshold: ${slowQueryThreshold}ms)`)
  }

  return drizzle(db, { schema })
}

// Default database instance with retry logic
let dbInstance: ReturnType<typeof createDatabase> | null = null

export function getDatabase(): ReturnType<typeof createDatabase> {
  if (!dbInstance) {
    const dbPath = './apps/site/data/blog.db' // Fixed path per CONTEXT.md
    const maxRetries = 3
    let attempts = 0

    while (attempts < maxRetries) {
      try {
        dbInstance = createDatabase({
          dbPath,
          enableSlowQueryLog: true,
          slowQueryThreshold: 100, // 100ms threshold per CONTEXT.md
        })
        break
      } catch (error) {
        attempts++
        if (attempts < maxRetries) {
          console.error(
            `[Database] Connection failed (attempt ${attempts}/${maxRetries}), retrying in 100ms...`
          )
          // In real implementation, use proper delay
        } else {
          console.error('[Database] Failed to connect after 3 attempts:', error)
          throw error
        }
      }
    }
  }
  return dbInstance
}

export const db = getDatabase()
```

### 3. 创建 Drizzle Kit 配置文件

**drizzle.config.ts:**

```typescript
import type { Config } from 'drizzle-kit'

export default {
  schema: './src/schema/index.ts',
  out: './drizzle',
  driver: 'better-sqlite',
  dbCredentials: {
    url: './apps/site/data/blog.db', // Fixed path per CONTEXT.md
  },
  // Migration naming: timestamp format per CONTEXT.md
  migrations: {
    prefix: 'timestamp', // e.g., 20260318120000_create_users
  },
} satisfies Config
```

### 4. 创建包入口文件

**src/index.ts:**

```typescript
export { db, createDatabase } from './db'
export * from './schema'
```

### 5. 更新根 pnpm-workspace.yaml

确保 packages/database 被包含在 workspace 中。

### 6. 安装依赖

```bash
pnpm --filter @my-blog/database install
```

### 7. 创建数据库目录

```bash
mkdir -p apps/site/data
```

### 8. 验证数据库连接

**Test script:**

```typescript
// packages/database/src/__tests__/connection.test.ts
import { describe, it, expect } from 'vitest'
import { createDatabase } from '../db'

describe('Database Connection', () => {
  it('should create database with WAL mode', () => {
    const db = createDatabase(':memory:')
    // WAL mode test would require actual file DB
    expect(db).toBeDefined()
  })
})
```

## Verification

- [ ] `pnpm --filter @my-blog/database test` passes
- [ ] Database file can be created at `apps/site/data/blog.db`
- [ ] WAL mode is enabled (verify with `PRAGMA journal_mode`)
- [ ] TypeScript compiles without errors
- [ ] Drizzle Kit commands work
- [ ] Connection pool with retry logic (3 attempts)
- [ ] Slow query logging enabled (>100ms threshold)

## Dependencies

None (Phase 3 starting point)

## Nyquist Validation

- **V1**: Database connection test passes
- **V2**: WAL mode verified via PRAGMA command
