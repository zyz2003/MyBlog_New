# Plan 03-05: Database Migration and Testing

## Goal

完成数据库迁移脚本的生成和执行，运行所有测试验证数据库层的完整性。

## Tasks

### 1. 生成数据库迁移

```bash
cd packages/database
pnpm db:generate
```

**Expected output per CONTEXT.md:**

- Migration files with **timestamp naming**: `20260318120000_create_users.sql`
- Snapshot JSON: `drizzle/meta/20260318120000_snapshot.json`

**Verify migration files include all tables:**

- users (with extended fields)
- categories (with 2-level hierarchy)
- posts (with full fields + 4-state status)
- tags (with extended fields)
- post_tags (composite PK)
- media (with extended fields)

### 2. 执行数据库迁移

```bash
cd packages/database
pnpm db:migrate
```

**验证 (CONTEXT.md decisions):**

- 数据库文件创建在 `apps/site/data/blog.db`（固定路径）
- 所有表创建成功
- WAL 模式启用（检查 `.wal` 文件）
- Foreign keys enabled

### 3. 验证回滚策略

**Per CONTEXT.md: 完整回滚（每个迁移必须定义 down() 函数）**

```bash
# Test rollback (development only)
pnpm db:migrate:rollback

# Verify each migration has down() function
cat drizzle/*.sql | grep -A5 "DROP TABLE"
```

### 4. 执行数据库迁移

### 4. 执行数据库迁移

```bash
cd packages/database
pnpm db:migrate
```

### 5. 运行数据库测试

```bash
# 运行所有数据库测试
pnpm --filter @my-blog/database test

# 运行测试覆盖率
pnpm --filter @my-blog/database test:coverage
```

### 6. 验证数据库 Schema

```bash
# 使用 SQLite CLI 验证
sqlite3 apps/site/data/blog.db ".schema"

# 或者使用 Drizzle Studio
pnpm --filter @my-blog/database db:studio
```

### 7. 验证种子数据

```bash
# 运行种子脚本
pnpm --filter @my-blog/database db:seed

# 验证数据
sqlite3 apps/site/data/blog.db "SELECT * FROM users;"
sqlite3 apps/site/data/blog.db "SELECT * FROM categories;"
sqlite3 apps/site/data/blog.db "SELECT * FROM tags;"
sqlite3 apps/site/data/blog.db "SELECT * FROM posts LIMIT 5;"
```

### 8. 创建集成测试

**File: `packages/database/src/__tests__/integration.test.ts`**

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { Database } from 'better-sqlite3'
import { drizzle } from 'drizzle-orm/better-sqlite3'
import { migrate } from 'drizzle-orm/better-sqlite3/migrator'
import { users, categories, tags, posts, postTags, media } from '../schema'
import { eq, and } from 'drizzle-orm'
import * as schema from '../schema'

describe('Database Integration', () => {
  let database: Database
  let db: ReturnType<typeof drizzle>

  beforeAll(() => {
    database = new Database(':memory:')
    db = drizzle(database, { schema })
    // Run migrations in memory
    migrate(db, { migrationsFolder: './drizzle' })
  })

  afterAll(() => {
    database.close()
  })

  it('can insert and query users with extended fields', () => {
    const testUser = {
      id: 'test-1',
      username: 'testuser',
      email: 'test@example.com',
      passwordHash: 'hashed',
      role: 'admin' as const,
      status: 'active' as const,
      avatar: 'https://example.com/avatar.jpg',
      bio: 'Test bio',
      website: 'https://example.com',
    }

    db.insert(users).values(testUser).run()

    const result = db.select().from(users).where(eq(users.id, 'test-1')).get()
    expect(result).toBeDefined()
    expect(result?.username).toBe('testuser')
    expect(result?.avatar).toBe('https://example.com/avatar.jpg')
  })

  it('can handle posts with full fields and 4-state status', () => {
    const testPost = {
      id: 'post-1',
      title: 'Test Post',
      slug: 'test-post',
      content: 'Content',
      excerpt: 'Excerpt',
      coverImage: '/images/cover.jpg',
      seoTitle: 'SEO Title',
      seoDescription: 'SEO Description',
      status: 'reviewing' as const,
      authorId: 'test-1',
      categoryId: null,
      viewCount: 0,
      likeCount: 0,
    }

    db.insert(posts).values(testPost).run()

    const result = db.select().from(posts).where(eq(posts.slug, 'test-post')).get()
    expect(result).toBeDefined()
    expect(result?.status).toBe('reviewing')
    expect(result?.seoTitle).toBe('SEO Title')
  })

  it('can handle category hierarchy (2 levels max)', () => {
    const parentCat = {
      id: 'cat-parent',
      name: 'Parent Category',
      slug: 'parent',
      description: 'Parent',
    }

    const childCat = {
      id: 'cat-child',
      name: 'Child Category',
      slug: 'child',
      description: 'Child',
      parentId: 'cat-parent',
    }

    db.insert(categories).values([parentCat, childCat]).run()

    const result = db.select().from(categories).where(eq(categories.parentId, 'cat-parent')).all()
    expect(result.length).toBe(1)
    expect(result[0].name).toBe('Child Category')
  })

  it('enforces foreign key constraints with cascade delete', () => {
    // Insert user and post, then delete user
    // Post should be cascade deleted
  })

  it('can query posts with tags (many-to-many)', () => {
    // Test post_tags junction table
  })
})
```

### 9. 更新根 package.json

添加数据库相关命令：

```json
{
  "scripts": {
    "db:generate": "pnpm --filter @my-blog/database db:generate",
    "db:migrate": "pnpm --filter @my-blog/database db:migrate",
    "db:seed": "pnpm --filter @my-blog/database db:seed",
    "db:studio": "pnpm --filter @my-blog/database db:studio"
  }
}
```

## Verification

- [ ] Migration files generated with timestamp naming (e.g., 20260318120000_create_users)
- [ ] Migration runs without errors
- [ ] All tables created with correct schema
- [ ] WAL mode enabled (verify .wal file exists)
- [ ] Rollback supported (each migration has down() function)
- [ ] All tests pass (unit + integration)
- [ ] Seed data inserted correctly
- [ ] Admin user: username=admin, email=admin@example.com
- [ ] 3 default categories: 技术，生活，随笔
- [ ] 5-8 sample tags with colors
- [ ] 3-5 sample posts
- [ ] Foreign key constraints working (CASCADE/SET NULL as designed)

## Dependencies

- Plan 03-01 (Database Package Setup)
- Plan 03-02 (User and Category Schema)
- Plan 03-03 (Posts and Tags Schema)
- Plan 03-04 (Media Schema and Seed Data)

## Nyquist Validation

- **V1**: All migrations run successfully with timestamp naming
- **V2**: All tests pass (including extended fields and 4-state status)
- **V3**: Database is usable from the application with seed data
