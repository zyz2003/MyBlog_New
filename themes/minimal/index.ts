import type { ThemeConfig } from '../../packages/core/src/theme/types'
import themeJson from './theme.json'

export const minimalTheme: ThemeConfig = themeJson as ThemeConfig

export default minimalTheme
