# Docker 环境下的 pnpm 配置
# 覆盖根目录的 .pnpmrc（Windows 路径在 Docker 中无效）
store-dir=/root/.local/share/pnpm/store
cache-dir=/root/.cache/pnpm
shamefully-hoist=true
enable-pre-post-scripts=true
node-linker=hoisted
onlyBuiltDependencies=better-sqlite3
