{
  "name": "my-blog-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "个人博客系统 - Nuxt 3 + Vue 3 + TypeScript",
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r preview",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm --filter @my-blog/site db:migrate"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
