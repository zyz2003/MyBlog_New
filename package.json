{
  "name": "my-blog-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "个人博客系统 - Nuxt 3 + Vue 3 + TypeScript",
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
