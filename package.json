{
  "name": "my-blog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm -r --filter=@my-blog/site dev",
    "build": "pnpm -r build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit --project tsconfig.json",
    "prepare": "husky",
    "db:generate": "pnpm --filter @my-blog/database db:generate",
    "db:migrate": "pnpm --filter @my-blog/database db:migrate",
    "db:seed": "pnpm --filter @my-blog/database db:seed",
    "db:studio": "pnpm --filter @my-blog/database db:studio",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.57.1",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-vue": "^10.8.0",
    "globals": "^17.4.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prebuild-install": "^7.1.3",
    "prettier": "^3.2.0",
    "sqlite3": "^6.0.1",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=10.0.0"
  }
}
