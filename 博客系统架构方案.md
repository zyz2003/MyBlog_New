# 个人博客系统架构方案

**版本**: v1.0
**更新日期**: 2026-03-13
**状态**: 架构设计完成，待实现

---

## 一、项目概述

### 1.1 项目背景

现有主流博客系统的痛点：

| 类型 | 代表 | 痛点 |
|------|------|------|
| **静态博客** | Hexo/Hugo/Jekyll | 增删改文章需命令 + 重建，样式修改困难，无后台管理 |
| **动态 CMS** | WordPress/Ghost | 需服务器 + 数据库，运维成本高，安全风险，更新频繁 |
| **SaaS 平台** | Medium/Substack | 无法自定义样式，数据锁定，国内访问困难，平台依赖 |

### 1.2 核心需求

| 需求 | 描述 | 优先级 |
|------|------|--------|
| **方便的内容管理** | 可视化编辑 + Markdown 双支持，无需命令/重建 | P0 |
| **灵活的样式系统** | 可轻松替换、修改、增加主题风格 | P0 |
| **极度解耦** | 功能模块可热插拔，不影响其他功能 | P0 |
| **强扩展性** | 通用插件适配系统，兼容第三方及自研模块 | P0 |
| **后台统一管理** | 所有内容、样式、模块通过后台管理 | P0 |
| **首屏性能** | SSR 渲染，解决首屏加载慢的问题 | P1 |
| **自部署** | 部署到个人服务器，完全可控 | P1 |

### 1.3 目标用户

| 用户类型 | 需求 | 本方案满足 |
|----------|------|-----------|
| **博客所有者** | 有 Vue3/TS/Node.js 技术背景，需要灵活定制 | 技术栈匹配，高度可定制 |
| **内容读者** | 快速加载、良好阅读体验 | SSR 首屏快，SEO 优化 |
| **内容创作者** | 简单写作、方便管理 | 可视化编辑器，后台统一管理 |
| **开发者** | 简单易懂的主题/模块开发体验 | 清晰接口，完整文档 |

---

## 二、技术架构

### 2.1 完整整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                              个人服务器                                                   │
│                                    (VPS / 云服务器 / 本地服务器)                                          │
├─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                          │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                               Nginx / Caddy (反向代理)                             │   │
│  │  ┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────────────────────────┐   │   │
│  │  │  HTTPS 终端              │  │  静态资源缓存            │  │  请求路由转发                    │   │   │
│  │  │  - SSL/TLS              │  │  - 图片/JS/CSS          │  │  - /api/* → Nitro              │   │   │
│  │  │  - 证书自动续期          │  │  - 浏览器缓存策略        │  │  - /* → Nuxt SSR               │   │   │
│  │  └─────────────────────────┘  └─────────────────────────┘  └─────────────────────────────────┘   │   │
│  └───────────────────────────────────────────────────────────────────────────────────────────────────┘   │
│                                                    │                                                       │
│                    ┌───────────────────────────────┴───────────────────────────────┐                       │
│                    ▼                                                               ▼                       │
│  ┌───────────────────────────────────────────┐                   ┌───────────────────────────────────────┐ │
│  │           前台博客站点 (Site)              │                   │           后台管理系统 (Admin)         │ │
│  │              Nuxt 3 SSR                    │                   │              Nuxt 3 SPA               │ │
│  ├───────────────────────────────────────────┤                   ├───────────────────────────────────────┤ │
│  │  ┌─────────────────────────────────────┐  │                   │  ┌─────────────────────────────────┐  │ │
│  │  │          页面路由层                   │  │                   │  │          页面路由层              │  │ │
│  │  │  - / (首页)                          │  │                   │  │  - /admin/dashboard (仪表盘)   │  │ │
│  │  │  - /articles (文章列表)              │  │                   │  │  - /admin/articles (文章管理)  │  │ │
│  │  │  - /articles/:slug (文章详情)        │  │                   │  │  - /admin/categories (分类)    │  │ │
│  │  │  - /categories/:slug (分类)          │  │                   │  │  - /admin/tags (标签)          │  │ │
│  │  │  - /tags/:slug (标签)                │  │                   │  │  - /admin/media (媒体库)       │  │ │
│  │  │  - /pages/:slug (自定义页面)         │  │                   │  │  - /admin/themes (主题管理)    │  │ │
│  │  │  - /search (搜索)                    │  │                   │  │  - /admin/styles (样式配置)    │  │ │
│  │  │  - /about (关于)                     │  │                   │  │  - /admin/plugins (插件管理)   │  │ │
│  │  │  - /feed.xml (RSS)                   │  │                   │  │  - /admin/settings (系统设置)  │  │ │
│  │  └─────────────────────────────────────┘  │                   │  └─────────────────────────────────┘  │ │
│  │                                           │                   │                                         │ │
│  │  ┌─────────────────────────────────────┐  │                   │  ┌─────────────────────────────────┐  │ │
│  │  │          组件层                      │  │                   │  │          组件层                  │  │ │
│  │  │  ┌───────────────────────────────┐  │  │                   │  │  ┌─────────────────────────────┐ │  │ │
│  │  │  │  布局组件 (Layouts)           │  │  │                   │  │  │  表单组件 (Form Components) │ │  │ │
│  │  │  │  - BaseLayout (基础布局)      │  │  │                   │  │  │  - TextInput               │ │  │ │
│  │  │  │  - MinimalLayout (极简)       │  │  │                   │  │  │  - RichTextEditor        │ │  │ │
│  │  │  │  - MagazineLayout (杂志)      │  │  │                   │  │  │  - MarkdownEditor        │ │  │ │
│  │  │  │  - PortfolioLayout (作品集)   │  │  │                   │  │  │  - ImageUploader         │ │  │ │
│  │  │  │  ┌─ 可插拔布局 ─┐             │  │  │                   │  │  │  - ColorPicker           │ │  │ │
│  │  │  │  │ 自定义布局   │             │  │  │                   │  │  │  - ToggleSwitch          │ │  │ │
│  │  │  │  └─────────────┘             │  │  │                   │  │  │  - SelectDropdown        │ │  │ │
│  │  │  └───────────────────────────────┘  │  │                   │  │  └─────────────────────────────┘ │  │ │
│  │  │  ┌───────────────────────────────┐  │  │                   │  │  ┌─────────────────────────────┐ │  │ │
│  │  │  │  功能模块组件 (Module)        │  │  │                   │  │  │  管理组件 (Admin Specific)  │ │  │ │
│  │  │  │  - Header (导航头)            │  │  │                   │  │  │  - ArticleTable            │ │  │ │
│  │  │  │  - Footer (页脚)              │  │  │                   │  │  │  - PluginCard              │ │  │ │
│  │  │  │  - Sidebar (侧边栏)           │  │  │                   │  │  │  - ThemePreview            │ │  │ │
│  │  │  │  - PostCard (文章卡片)        │  │  │                   │  │  │  - StyleConfigPanel      │ │  │ │
│  │  │  │  - PostList (文章列表)        │  │  │                   │  │  │  - MediaGallery            │ │  │ │
│  │  │  │  - Pagination (分页)          │  │  │                   │  │  └─────────────────────────────┘ │  │ │
│  │  │  │  ┌─ 插件注入组件 ─┐           │  │  │                   │  │                                    │  │ │
│  │  │  │  │ 评论容器      │           │  │  │                   │  │                                    │  │ │
│  │  │  │  │ 统计代码      │           │  │  │                   │  │                                    │  │ │
│  │  │  │  │ 分享按钮      │           │  │  │                   │  │                                    │  │ │
│  │  │  │  │ 广告位        │           │  │  │                   │  │                                    │  │ │
│  │  │  │  └──────────────┘           │  │  │                   │  │                                    │  │ │
│  │  │  └───────────────────────────────┘  │  │                   │  └─────────────────────────────────┘  │ │
│  │  │  ┌───────────────────────────────┐  │  │                   │                                         │ │
│  │  │  │  基础 UI 组件 (UI Kit)          │  │  │                   │  ┌─────────────────────────────────┐  │ │
│  │  │  │  - Button, Input, Modal       │  │  │                   │  │          状态管理 (Pinia)        │  │ │
│  │  │  │  - Table, Card, Dropdown      │  │  │                   │  │  - articleStore (文章状态)      │  │ │
│  │  │  │  - Toast, Tooltip, Badge      │  │  │                   │  │  - themeStore (主题状态)        │  │ │
│  │  │  │  - Avatar, Spinner, Empty     │  │  │                   │  │  - pluginStore (插件状态)       │  │ │
│  │  │  │  - Tabs, Accordion, Breadcrumb│  │  │                   │  │  - styleStore (样式状态)        │  │ │
│  │  │  └───────────────────────────────┘  │  │                   │  │  - systemStore (系统状态)       │  │ │
│  │  └─────────────────────────────────────┘  │  │                   │  └─────────────────────────────────┘  │ │
│  │                                           │  │                   └───────────────────────────────────────┘ │
│  │  ┌─────────────────────────────────────┐  │  │                              │                              │
│  │  │        组合式函数 (Composables)     │  │  │                              │                              │
│  │  │  - useTheme() - 主题加载/切换       │  │  │                              │                              │
│  │  │  - usePlugins() - 插件注册/管理     │◄─┼──────────────────────────────────────┘                              │
│  │  │  - useStyle() - 样式配置/应用       │  │  │
│  │  │  - useSEO() - SEO 元数据生成         │  │  │
│  │  │  - useFetch() - API 请求封装         │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  │                                           │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │         插件挂载系统 (核心!)         │  │  │
│  │  │  ┌───────────────────────────────┐  │  │  │
│  │  │  │  挂载点注册中心                │  │  │  │
│  │  │  │  - head-script                │  │  │  │
│  │  │  │  - body-script                │  │  │  │
│  │  │  │  - header-start/end           │  │  │  │
│  │  │  │  - nav-start/end              │  │  │  │
│  │  │  │  - sidebar-start/end          │  │  │  │
│  │  │  │  - post-start/end             │  │  │  │
│  │  │  │  - comment-before/after       │  │  │  │
│  │  │  │  - footer-start/end           │  │  │  │
│  │  │  │  - global-floating            │  │  │  │
│  │  │  └───────────────────────────────┘  │  │  │
│  │  │  ┌───────────────────────────────┐  │  │  │
│  │  │  │  插件渲染器                    │  │  │  │
│  │  │  │  - 动态加载插件 SDK           │  │  │  │
│  │  │  │  - 挂载插件组件到指定位置      │  │  │  │
│  │  │  │  - 插件生命周期管理            │  │  │  │
│  │  │  │  - 插件配置热更新              │  │  │  │
│  │  │  └───────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                   │
│                                ┌──────────────────┴──────────────────┐
│                                │                                     │
│                                ▼                                     ▼
│  ┌───────────────────────────────────────────┐   ┌───────────────────────────────────────────────┐
│  │           统一 API 层 (Nitro Server)        │   │              核心框架包 (packages/core)       │
│  ├───────────────────────────────────────────┤   ├───────────────────────────────────────────────┤
│  │  ┌─────────────────────────────────────┐  │   │  ┌─────────────────────────────────────────┐  │
│  │  │           API 路由层                  │  │   │  │           插件系统核心                   │  │
│  │  │  /api/articles        - 文章 CRUD   │  │   │  │  - PluginRegistry (插件注册中心)       │  │
│  │  │  /api/categories      - 分类管理    │  │   │  │  - PluginAdapter (适配器基类)          │  │
│  │  │  /api/tags            - 标签管理    │  │   │  │  - MountPointSystem (挂载点系统)       │  │
│  │  │  /api/media           - 媒体管理    │  │   │  │  - LifecycleHook (生命周期钩子)        │  │
│  │  │  /api/themes          - 主题管理    │  │   │  └─────────────────────────────────────────┘  │
│  │  │  /api/styles          - 样式配置    │  │   │  ┌─────────────────────────────────────────┐  │
│  │  │  /api/plugins         - 插件管理    │  │   │  │           主题系统核心                   │  │
│  │  │  /api/settings        - 系统设置    │  │   │  │  - ThemeRegistry (主题注册中心)        │  │
│  │  │  /api/comments        - 评论管理    │  │   │  │  - ThemeLoader (主题加载器)            │  │
│  │  │  /api/auth            - 认证授权    │  │   │  │  - StyleCompiler (样式编译器)          │  │
│  │  │  /api/search          - 搜索接口    │  │   │  │  - CSSVariableManager (变量管理)       │  │
│  │  │  ┌─────────────────────────────────┐│  │   │  └─────────────────────────────────────────┘  │
│  │  │  │  插件自定义路由 (动态注册)     ││  │   │  ┌─────────────────────────────────────────┐  │
│  │  │  │  /api/plugins/:name/*         ││  │   │  │           钩子系统                      │  │
│  │  │  └─────────────────────────────────┘│  │   │  │  - HookRegistry (钩子注册中心)         │  │
│  │  └─────────────────────────────────────┘  │   │  │  - EventDispatcher (事件分发器)        │  │
│  │                                            │   │  └─────────────────────────────────────────┘  │
│  │  ┌─────────────────────────────────────┐  │   └───────────────────────────────────────────────┘
│  │  │           中间件层                  │  │                              │
│  │  │  - authMiddleware (认证中间件)      │  │                              │
│  │  │  - corsMiddleware (跨域中间件)      │  │                              │
│  │  │  - rateLimitMiddleware (限流)       │  │                              │
│  │  │  - errorMiddleware (错误处理)       │  │                              │
│  │  │  - logMiddleware (日志记录)         │  │                              │
│  │  └─────────────────────────────────────┘  │                              │
│  │                                            │                              │
│  │  ┌─────────────────────────────────────┐  │                              │
│  │  │           服务层 (Service)          │  │                              │
│  │  │  - ArticleService                   │  │                              │
│  │  │  - CategoryService                  │◄─┘                              │
│  │  │  - TagService                       │                                  │
│  │  │  - ThemeService                     │                                  │
│  │  │  - PluginService                    │                                  │
│  │  │  - MediaService                     │                                  │
│  │  │  - CommentService                   │                                  │
│  │  └─────────────────────────────────────┘  │
│  └───────────────────────────────────────────┘  │
│                                                   │
│                                ┌──────────────────┴──────────────────┐
│                                │                                     │
│                                ▼                                     ▼
│  ┌───────────────────────────────────────────┐   ┌───────────────────────────────────────────────┐
│  │           数据访问层 (Drizzle ORM)         │   │              缓存层 (可选)                     │
│  ├───────────────────────────────────────────┤   ├───────────────────────────────────────────────┤
│  │  ┌─────────────────────────────────────┐  │   │  ┌─────────────────────────────────────────┐  │
│  │  │        Schema 定义                   │  │   │  │  Redis / Memory Cache                   │  │
│  │  │  - posts (文章表)                   │  │   │  │  - 文章列表缓存                         │  │
│  │  │  - categories (分类表)              │  │   │  │  - 文章详情缓存                         │  │
│  │  │  - tags (标签表)                    │  │   │  │  - 主题配置缓存                         │  │
│  │  │  - post_tags (文章 - 标签关联)       │  │   │  │  - 插件配置缓存                         │  │
│  │  │  - comments (评论表)                │  │   │  │  - SEO 元数据缓存                         │  │
│  │  │  - theme_settings (主题配置)        │  │   │  └─────────────────────────────────────────┘  │
│  │  │  - plugin_settings (插件配置)       │  │   └───────────────────────────────────────────────┘
│  │  │  - users (用户表)                   │  │
│  │  │  - media (媒体资源表)               │  │
│  │  │  - system_settings (系统设置)       │  │
│  │  └─────────────────────────────────────┘  │
│  │                                            │
│  │  ┌─────────────────────────────────────┐  │
│  │  │        Query Builder                │  │
│  │  │  - 类型安全的 SQL 构建                │  │
│  │  │  - 事务支持                          │  │
│  │  │  - 迁移管理                          │  │
│  │  └─────────────────────────────────────┘  │
│  └───────────────────────────────────────────┘  │
│                                                   │
│                                ┌──────────────────┴──────────────────┐
│                                │                                     │
│                                ▼                                     ▼
│  ┌───────────────────────────────────────────┐   ┌───────────────────────────────────────────────┐
│  │              SQLite 数据库                 │   │              文件系统                         │
│  ├───────────────────────────────────────────┤   ├───────────────────────────────────────────────┤
│  │  database/                                 │   │  themes/                                     │
│  │  └── blog.sqlite  (单文件数据库)          │   │  ├── minimal/                                 │
│  │                                            │   │  │   ├── layout.vue                         │
│  │  特点：                                     │   │  │   ├── config.json                        │
│  │  - 单文件，易备份                          │   │  │   └── styles.css                         │
│  │  - 零配置，开箱即用                        │   │  ├── magazine/                                │
│  │  - 支持 ACID 事务                           │   │  └── portfolio/                               │
│  │  - 后续可迁移 MySQL/PG                     │   │                                              │
│  │                                            │   │  uploads/                                    │
│  │  性能优化：                                 │   │  ├── images/                                │
│  │  - WAL 模式 (Write-Ahead Logging)          │   │  │   ├── 2024/                              │
│  │  - 索引优化                                │   │  │   └── 2025/                              │
│  │  - 连接池管理                              │   │  ├── documents/                             │
│  └───────────────────────────────────────────┘   │  └── avatars/                                 │
│                                                  └───────────────────────────────────────────────┘
│
└─────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流架构图

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                               数据流架构                                                 │
├─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                          │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐       │
│  │   用户请求   │────▶│  Nginx/Caddy│────▶│  Nuxt SSR   │────▶│  Nitro API  │────▶│  Drizzle    │       │
│  │  (浏览器)    │     │  (反向代理)  │     │  (应用层)    │     │  (服务层)    │     │  (ORM 层)     │       │
│  └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘       │
│        │                   │                   │                   │                   │                │
│        │                   │                   │                   │                   ▼                │
│        │                   │                   │                   │         ┌─────────────┐           │
│        │                   │                   │                   │         │   SQLite    │           │
│        │                   │                   │                   │         │  (数据库)    │           │
│        │                   │                   │                   │         └─────────────┘           │
│        │                   │                   │                   │                                    │
│        │                   │                   │                   ▼                                    │
│        │                   │                   │         ┌─────────────────┐                           │
│        │                   │                   │         │   Plugin Hooks  │                           │
│        │                   │                   │         │  (插件钩子拦截)  │                           │
│        │                   │                   │         └─────────────────┘                           │
│        │                   │                   │                                                        │
│        │                   │                   ▼                                                        │
│        │                   │         ┌─────────────────────────────────┐                               │
│        │                   │         │      响应数据处理流程            │                               │
│        │                   │         │  1. API 返回原始数据              │                               │
│        │                   │         │  2. Service 层业务逻辑处理        │                               │
│        │                   │         │  3. 插件钩子拦截/修改数据         │                               │
│        │                   │         │  4. SSR 渲染为 HTML               │                               │
│        │                   │         │  5. 注入插件内容 (评论/统计等)     │                               │
│        │                   │         │  6. 返回完整 HTML 给浏览器          │                               │
│        │                   │         └─────────────────────────────────┘                               │
│        │                   │                                                                            │
│        │                   ▼                                                                            │
│        │         ┌─────────────────────────────────┐                                                   │
│        │         │      静态资源处理                │                                                   │
│        │         │  - 图片压缩/CDN                 │                                                   │
│        │         │  - JS/CSS 打包/压缩              │                                                   │
│        │         │  - 浏览器缓存策略               │                                                   │
│        │         └─────────────────────────────────┘                                                   │
│        │                                                                                               │
│        ▼                                                                                               │
│  ┌─────────────────────────────────┐                                                                  │
│  │      浏览器渲染                  │                                                                  │
│  │  1. 接收 HTML (首屏直出)         │                                                                  │
│  │  2. 解析并显示内容              │                                                                  │
│  │  3. 加载 JS 进行水合 (Hydration)  │                                                                  │
│  │  4. 插件 SDK 异步加载             │                                                                  │
│  │  5. 插件组件挂载到指定位置        │                                                                  │
│  │  6. 交互式页面完成              │                                                                  │
│  └─────────────────────────────────┘                                                                  │
│                                                                                                        │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 插件系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                             插件系统架构                                                │
├─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                          │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                          插件注册中心 (PluginRegistry)                             │   │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────────┐  │   │
│  │  │  插件类型分类管理：                                                                          │  │   │
│  │  │  ├── 评论类 (Comment)    - Twikoo, Waline, Disqus, utterances, 内置评论                     │  │   │
│  │  │  ├── 统计类 (Analytics)  - Google Analytics, 百度统计，Umami, 自定义统计                    │  │   │
│  │  │  ├── 搜索类 (Search)     - Algolia, MeiliSearch, 本地搜索，FlexSearch                      │  │   │
│  │  │  ├── 社交类 (Social)     - 分享按钮 (Share.js), Follow 按钮，订阅框                          │  │   │
│  │  │  ├── 广告类 (Ad)         - Google AdSense, 自定义广告位，联盟广告                           │  │   │
│  │  │  ├── 功能类 (Feature)    - 打赏，暗黑模式，阅读进度，返回顶部                               │  │   │
│  │  │  └── 自定义 (Custom)     - 任意 JS 代码/组件，第三方服务集成                                   │  │   │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────────┘  │   │
│  └───────────────────────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                                            │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                          插件适配器接口 (PluginAdapter)                           │   │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────────┐  │   │
│  │  │  interface PluginAdapter {                                                                   │  │   │
│  │  │    // ============ 元信息 ============                                                      │  │   │
│  │  │    name: string;              // 唯一标识，如 'twikoo', 'google-analytics'                  │  │   │
│  │  │    label: string;             // 显示名称，如 'Twikoo 评论'                                 │  │   │
│  │  │    type: PluginType;          // 插件类型，用于分类管理                                     │  │   │
│  │  │    version: string;           // 版本号，语义化版本                                        │  │   │
│  │  │    author?: string;           // 作者信息                                                  │  │   │
│  │  │    description?: string;      // 插件描述                                                  │  │   │
│  │  │    website?: string;          // 插件官网                                                  │  │   │
│  │  │    icon?: string;             // 插件图标 (SVG/Base64)                                     │  │   │
│  │  │                                                                                             │  │   │
│  │  │    // ============ 配置 Schema (用于后台自动生成表单) ============                         │  │   │
│  │  │    configSchema: ConfigSchema;  // 配置项定义，决定后台表单结构                            │  │   │
│  │  │                                                                                             │  │   │
│  │  │    // ============ 挂载点定义 ============                                                  │  │   │
│  │  │    mountPoints: MountPoint[];   // 插件可以挂载到页面的哪些位置                            │  │   │
│  │  │                                                                                             │  │   │
│  │  │    // ============ 生命周期钩子 ============                                                │  │   │
│  │  │    onInit?(config: any): Promise<void>;                    // 插件初始化 (加载 SDK 等)       │  │   │
│  │  │    onMount?(container: HTMLElement, config: any): void;    // 挂载到页面                    │  │   │
│  │  │    onUnmount?(): void;                                     // 卸载/清理资源                 │  │   │
│  │  │    onConfigChange?(newConfig: any): void;                  // 配置变更时的回调               │  │   │
│  │  │                                                                                             │  │   │
│  │  │    // ============ 服务端路由 (可选) ============                                          │  │   │
│  │  │    serverRoutes?: ServerRoute[];  // 如果需要后端 API 支持                                    │  │   │
│  │  │  }                                                                                          │  │   │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────────┘  │   │
│  └───────────────────────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                                            │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                          挂载点系统 (MountPointSystem)                           │   │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────────┐  │   │
│  │  │  页面挂载点完整列表 (16 个标准位置):                                                           │  │   │
│  │  │                                                                                             │  │   │
│  │  │  ┌─────────────────────────────────────────────────────────────────────────────────────┐   │  │   │
│  │  │  │  <html>                                                                             │   │  │   │
│  │  │  │    <head>                                                                           │   │  │   │
│  │  │  │      ├─ head-start         // <head> 开始位置 (插入 meta 等)                         │   │  │   │
│  │  │  │      ├─ head-end           // </head>前 (插入 script/link 等)                       │   │  │   │
│  │  │  │    </head>                                                                          │   │  │   │
│  │  │  │    <body>                                                                           │   │  │   │
│  │  │  │      ├─ body-start         // <body> 开始位置                                        │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ header-start       // Header 组件开始                                        │   │  │   │
│  │  │  │      ├─ header-end         // Header 组件结束                                        │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ nav-start          // 导航栏开始                                             │   │  │   │
│  │  │  │      ├─ nav-end            // 导航栏结束                                             │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ main-start         // 主内容区开始                                           │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ sidebar-start      // 侧边栏开始                                             │   │  │   │
│  │  │  │      ├─ sidebar-end        // 侧边栏结束                                             │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ post-start         // 文章正文前 (插入目录/提示等)                           │   │  │   │
│  │  │  │      ├─ post-end           // 文章正文后 (插入评论/相关文章/打赏等)                  │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ comment-before     // 评论区前                                               │   │  │   │
│  │  │  │      ├─ comment-after      // 评论区后                                               │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ footer-start       // Footer 组件开始                                        │   │  │   │
│  │  │  │      ├─ footer-end         // Footer 组件结束                                        │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ global-floating    // 全局悬浮组件 (返回顶部/客服/广告等)                    │   │  │   │
│  │  │  │      │                                                                              │   │  │   │
│  │  │  │      ├─ body-end           // </body>前 (插入统计代码等)                            │   │  │   │
│  │  │  │    </body>                                                                          │   │  │   │
│  │  │  └─────────────────────────────────────────────────────────────────────────────────────┘   │  │   │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────────┘  │   │
│  └───────────────────────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                                            │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                          插件开发方式                                              │   │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────────┐  │   │
│  │  │  方式一：在线开发 (后台可视化)                                                                │  │   │
│  │  │  后台 → 插件中心 → 开发插件 → 填写配置 → 编写代码 → 保存测试                                 │  │   │
│  │  │  适用：简单插件，如自定义脚本、广告代码等                                                    │  │   │
│  │  │                                                                                             │  │   │
│  │  │  方式二：本地开发后上传                                                                       │  │   │
│  │  │  npx create-myblog-plugin my-plugin  →  本地开发  →  npm run build  →  后台上传.zip        │  │   │
│  │  │  适用：复杂插件，需要完整开发环境                                                            │  │   │
│  │  │                                                                                             │  │   │
│  │  │  方式三：npm 包发布 (未来)                                                                     │  │   │
│  │  │  发布到 npm/pnpm registry  →  后台搜索安装                                                   │  │   │
│  │  │  适用：公开插件，供多人使用                                                                  │  │   │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────────┘  │   │
│  └───────────────────────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                                            │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 2.4 技术栈选型详解

| 层次 | 技术选型 | 版本 | 理由 | 备选方案 |
|------|---------|------|------|---------|
| **前端框架** | Nuxt 3 | 3.x | SSR 首屏性能、Vue3 生态、约定式路由 | Next.js 14, Astro |
| **UI 框架** | Vue 3 | 3.4+ | Composition API、响应式系统、性能好 | React 18, Svelte |
| **语言** | TypeScript | 5.x | 类型安全、智能提示、维护友好 | JavaScript (不推荐) |
| **样式** | Tailwind CSS | 3.x | 原子化 CSS、易定制、社区活跃 | UnoCSS, Bootstrap |
| **CSS 扩展** | CSS Variables | Native | 动态换肤、运行时修改 | SCSS Variables |
| **后端框架** | Nitro | 2.x | Nuxt 3 内置、零配置、轻量高效 | NestJS, Express, Koa |
| **ORM** | Drizzle ORM | 0.x | 轻量、类型安全、SQL-like | Prisma, TypeORM |
| **数据库** | SQLite | 3.x | 单文件、易备份、零配置 | MySQL, PostgreSQL |
| **缓存** | Redis (可选) | 7.x | 高性能、数据结构丰富 | Memory Cache |
| **可视化编辑** | TipTap | 2.x | Vue3 友好、可扩展、富文本 | Quill, Slate |
| **Markdown 编辑** | Vditor | 3.x | 国产开源、即时渲染、模式切换 | bytemd, MD Editor |
| **部署** | Docker + PM2 | Latest | 容器化、进程管理、自动保活 | Systemd, Supervisor |
| **反向代理** | Nginx / Caddy | Latest | 静态资源、HTTPS、负载均衡 | - |
| **构建工具** | Vite | 5.x | 快速开发、热更新、按需编译 | Webpack (不推荐) |
| **包管理** | pnpm | 8.x | 快速、节省空间、依赖提升 | npm, yarn |
| **Monorepo** | pnpm workspace | - | 多包管理、代码共享 | Turborepo, Nx |

---

## 三、项目目录结构 (完整版)

```
my-blog/
│
├── apps/
│   ├── site/                           # 前台博客应用 (Nuxt 3 SSR)
│   │   ├── app.vue                     # 应用入口
│   │   ├── nuxt.config.ts              # Nuxt 配置
│   │   ├── tsconfig.json               # TypeScript 配置
│   │   ├── tailwind.config.js          # Tailwind 配置
│   │   │
│   │   ├── components/
│   │   │   ├── layouts/                # 布局组件
│   │   │   │   ├── BaseLayout.vue      # 基础布局
│   │   │   │   ├── MinimalLayout.vue   # 极简布局
│   │   │   │   └── MagazineLayout.vue  # 杂志布局
│   │   │   │
│   │   │   ├── modules/                # 功能模块组件 (由插件系统注册)
│   │   │   │   ├── Header.vue          # 导航头
│   │   │   │   ├── Footer.vue          # 页脚
│   │   │   │   ├── Sidebar.vue         # 侧边栏
│   │   │   │   ├── PostCard.vue        # 文章卡片
│   │   │   │   ├── PostList.vue        # 文章列表
│   │   │   │   ├── Pagination.vue      # 分页
│   │   │   │   ├── TableOfContents.vue # 文章目录
│   │   │   │   └── PluginMount.vue     # 插件挂载容器
│   │   │   │
│   │   │   ├── ui/                     # 基础 UI 组件
│   │   │   │   ├── Button.vue
│   │   │   │   ├── Input.vue
│   │   │   │   ├── Modal.vue
│   │   │   │   ├── Card.vue
│   │   │   │   ├── Dropdown.vue
│   │   │   │   ├── Toast.vue
│   │   │   │   └── index.ts            # 统一导出
│   │   │   │
│   │   │   └── plugins/                # 第三方插件渲染组件
│   │   │       ├── CommentRenderer.vue # 评论渲染器
│   │   │       ├── AnalyticsInjector.vue # 统计注入
│   │   │       └── ScriptInjector.vue  # 脚本注入
│   │   │
│   │   ├── composables/                # 组合式函数
│   │   │   ├── useTheme.ts             # 主题加载/切换
│   │   │   ├── usePlugins.ts           # 插件注册/管理
│   │   │   ├── useStyle.ts             # 样式配置/应用
│   │   │   ├── useSEO.ts               # SEO 元数据生成
│   │   │   ├── useFetch.ts             # API 请求封装
│   │   │   ├── useMountPoints.ts       # 挂载点管理
│   │   │   └── index.ts
│   │   │
│   │   ├── pages/                      # 页面路由 (约定式)
│   │   │   ├── index.vue               # 首页
│   │   │   ├── articles/
│   │   │   │   ├── index.vue           # 文章列表
│   │   │   │   └── [slug].vue          # 文章详情
│   │   │   ├── categories/
│   │   │   │   └── [slug].vue          # 分类页
│   │   │   ├── tags/
│   │   │   │   └── [slug].vue          # 标签页
│   │   │   ├── pages/
│   │   │   │   └── [slug].vue          # 自定义页面
│   │   │   ├── search.vue              # 搜索页
│   │   │   ├── about.vue               # 关于页
│   │   │   └── feed.xml.ts             # RSS Feed
│   │   │
│   │   ├── stores/                     # Pinia 状态管理
│   │   │   ├── theme.ts                # 主题状态
│   │   │   ├── plugin.ts               # 插件状态
│   │   │   ├── article.ts              # 文章状态
│   │   │   └── site.ts                 # 站点配置
│   │   │
│   │   ├── utils/                      # 工具函数
│   │   │   ├── seo.ts                  # SEO 工具
│   │   │   ├── format.ts               # 格式化工具
│   │   │   └── cache.ts                # 缓存工具
│   │   │
│   │   └── public/                     # 静态资源
│   │       ├── robots.txt
│   │       └── sitemap.xml
│   │
│   └── admin/                          # 后台管理应用 (Nuxt 3 SPA)
│       ├── app.vue
│       ├── nuxt.config.ts
│       ├── tsconfig.json
│       │
│       ├── components/
│       │   ├── forms/                  # 表单组件
│       │   │   ├── TextInput.vue
│       │   │   ├── RichTextEditor.vue
│       │   │   ├── MarkdownEditor.vue
│       │   │   ├── ImageUploader.vue
│       │   │   ├── ColorPicker.vue
│       │   │   ├── ToggleSwitch.vue
│       │   │   └── SelectDropdown.vue
│       │   │
│       │   ├── articles/               # 文章管理组件
│       │   │   ├── ArticleTable.vue
│       │   │   ├── ArticleEditor.vue
│       │   │   ├── CategorySelector.vue
│       │   │   └── TagInput.vue
│       │   │
│       │   ├── themes/                 # 主题管理组件
│       │   │   ├── ThemeCard.vue
│       │   │   ├── ThemePreview.vue
│       │   │   └── ThemeConfigPanel.vue
│       │   │
│       │   ├── plugins/                # 插件管理组件
│       │   │   ├── PluginCard.vue
│       │   │   ├── PluginConfigForm.vue
│       │   │   └── PluginMarket.vue
│       │   │
│       │   ├── styles/                 # 样式配置组件
│       │   │   ├── StyleConfigPanel.vue
│       │   │   ├── ColorPalettePicker.vue
│       │   │   └── FontSelector.vue
│       │   │
│       │   └── common/                 # 通用组件
│       │       ├── Sidebar.vue
│       │       ├── Navbar.vue
│       │       ├── MediaGallery.vue
│       │       └── Dashboard.vue
│       │
│       ├── pages/
│       │   ├── index.vue               # 后台首页 (重定向)
│       │   ├── dashboard.vue           # 仪表盘
│       │   ├── articles/
│       │   │   ├── index.vue           # 文章列表
│       │   │   ├── create.vue          # 新建文章
│       │   │   └── edit/
│       │   │       └── [id].vue        # 编辑文章
│       │   ├── categories/
│       │   │   └── index.vue           # 分类管理
│       │   ├── tags/
│       │   │   └── index.vue           # 标签管理
│       │   ├── media/
│       │   │   └── index.vue           # 媒体库
│       │   ├── themes/
│       │   │   ├── index.vue           # 主题列表
│       │   │   ├── config/
│       │   │   │   └── [name].vue      # 主题配置
│       │   │   └── editor/
│       │   │       └── [name].vue      # 主题编辑器
│       │   ├── styles/
│       │   │   └── index.vue           # 样式配置
│       │   ├── plugins/
│       │   │   ├── index.vue           # 插件列表
│       │   │   ├── market.vue          # 插件市场
│       │   │   └── develop/
│       │   │       └── [name].vue      # 插件开发
│       │   └── settings/
│       │       ├── index.vue           # 系统设置
│       │       ├── site.vue            # 站点设置
│       │       ├── seo.vue             # SEO 设置
│       │       └── users.vue           # 用户管理
│       │
│       ├── stores/
│       │   ├── article.ts
│       │   ├── theme.ts
│       │   ├── plugin.ts
│       │   ├── style.ts
│       │   └── system.ts
│       │
│       └── utils/
│           ├── validator.ts
│           └── uploader.ts
│
├── packages/
│   ├── core/                           # 核心框架包
│   │   ├── src/
│   │   │   ├── plugin/                 # 插件系统核心
│   │   │   │   ├── index.ts
│   │   │   │   ├── types.ts            # 类型定义
│   │   │   │   ├── registry.ts         # 插件注册中心
│   │   │   │   ├── adapter.ts          # 适配器基类
│   │   │   │   ├── mountPoints.ts      # 挂载点系统
│   │   │   │   └── lifecycle.ts        # 生命周期管理
│   │   │   │
│   │   │   ├── theme/                  # 主题系统核心
│   │   │   │   ├── index.ts
│   │   │   │   ├── registry.ts         # 主题注册中心
│   │   │   │   ├── loader.ts           # 主题加载器
│   │   │   │   ├── compiler.ts         # 样式编译器
│   │   │   │   └── variables.ts        # CSS 变量管理
│   │   │   │
│   │   │   └── hook/                   # 钩子系统核心
│   │   │       ├── index.ts
│   │   │       ├── registry.ts         # 钩子注册中心
│   │   │       └── dispatcher.ts       # 事件分发器
│   │   │
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── database/                       # 数据库包
│   │   ├── src/
│   │   │   ├── schema/                 # Schema 定义
│   │   │   │   ├── posts.ts
│   │   │   │   ├── categories.ts
│   │   │   │   ├── tags.ts
│   │   │   │   ├── comments.ts
│   │   │   │   ├── themeSettings.ts
│   │   │   │   ├── pluginSettings.ts
│   │   │   │   ├── users.ts
│   │   │   │   ├── media.ts
│   │   │   │   └── index.ts
│   │   │   │
│   │   │   ├── migrations/             # 数据库迁移
│   │   │   │   └── meta/
│   │   │   │       └── _journal.json
│   │   │   │
│   │   │   └── index.ts                # 统一导出
│   │   │
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── plugins/                        # 官方插件包
│       ├── twikoo/
│       │   ├── src/
│       │   │   └── index.ts            # Twikoo 适配器
│       │   ├── package.json
│       │   └── README.md
│       │
│       ├── google-analytics/
│       ├── algolia-search/
│       ├── custom-script/
│       └── built-in-comment/           # 内置评论系统
│
├── themes/                             # 主题仓库
│   ├── minimal/                        # 极简主题
│   │   ├── layout.vue                  # 布局定义
│   │   ├── config.json                 # 主题配置
│   │   │   {
│   │   │     "name": "Minimal",
│   │   │     "version": "1.0.0",
│   │   │     "colors": { ... },
│   │   │     "fonts": { ... },
│   │   │     "layout": { ... }
│   │   │   }
│   │   ├── styles.css                  # 主题样式
│   │   ├── components/                 # 主题独有组件 (可选)
│   │   └── screenshot.png              # 主题预览图
│   │
│   ├── magazine/                       # 杂志主题
│   │   ├── layout.vue
│   │   ├── config.json
│   │   ├── styles.css
│   │   └── screenshot.png
│   │
│   └── portfolio/                      # 作品集主题
│       ├── layout.vue
│       ├── config.json
│       ├── styles.css
│       └── screenshot.png
│
├── database/                           # SQLite 数据库 (运行时生成)
│   └── blog.sqlite
│
├── uploads/                            # 上传文件 (运行时生成)
│   ├── images/
│   ├── documents/
│   └── avatars/
│
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
│
├── scripts/                            # 脚本工具
│   ├── init-db.ts
│   ├── migrate.ts
│   └── seed.ts
│
├── docs/                               # 项目文档
│   ├── architecture.md
│   ├── plugin-dev-guide.md
│   ├── theme-dev-guide.md
│   └── deployment.md
│
├── .env.example                        # 环境变量示例
├── .gitignore
├── package.json                        # Monorepo 根配置
├── pnpm-workspace.yaml                 # pnpm workspace 配置
├── tsconfig.json                       # TypeScript 根配置
└── README.md                           # 项目说明
```

---

## 四、核心系统设计详解

### 4.1 主题系统

**设计理念**: 主题 = 布局 (Layout) + 样式 (CSS) + 配置 (JSON)

#### 4.1.1 主题文件结构

```
themes/minimal/
├── layout.vue              # 布局定义 (Vue 组件)
├── config.json             # 主题配置
├── styles.css              # 主题样式
├── components/             # 主题独有组件 (可选)
│   ├── Hero.vue
│   └── FeaturedPosts.vue
├── screenshot.png          # 主题预览图
└── manifest.json           # 主题元信息
```

#### 4.1.2 主题配置 Schema

```json
{
  "name": "Minimal",
  "version": "1.0.0",
  "author": "Your Name",
  "description": "极简主义博客主题",
  "colors": {
    "primary": "#3B82F6",
    "secondary": "#64748B",
    "accent": "#8B5CF6",
    "background": "#FFFFFF",
    "surface": "#F8FAFC",
    "text": "#0F172A",
    "textMuted": "#64748B"
  },
  "fonts": {
    "heading": "system-ui",
    "body": "Inter, system-ui, sans-serif",
    "mono": "JetBrains Mono, monospace"
  },
  "spacing": {
    "unit": "8px",
    "containerMax": "720px",
    "contentPadding": "24px"
  },
  "borderRadius": {
    "small": "4px",
    "medium": "8px",
    "large": "16px"
  },
  "layout": {
    "headerPosition": "top",
    "sidebarPosition": "right",
    "footerStyle": "simple"
  }
}
```

#### 4.1.3 主题切换流程

```
1. 用户在后台选择主题
        ↓
2. 后台调用 API: POST /api/themes/activate { name: 'minimal' }
        ↓
3. Nitro API 更新数据库 themeSettings 表
        ↓
4. 触发主题变更事件 (Hook: theme:changed)
        ↓
5. 前台 Nuxt 监听到事件，加载新主题配置
        ↓
6. 应用新的 CSS Variables 到 :root
        ↓
7. 切换 Layout 组件
        ↓
8. 页面即时生效 (无需刷新)
```

#### 4.1.4 CSS Variables 系统

```css
/* themes/minimal/styles.css */
[data-theme="minimal"] {
  /* 颜色变量 */
  --color-primary: #3B82F6;
  --color-secondary: #64748B;
  --color-accent: #8B5CF6;
  --color-background: #FFFFFF;
  --color-surface: #F8FAFC;
  --color-text: #0F172A;
  --color-text-muted: #64748B;

  /* 字体变量 */
  --font-heading: system-ui;
  --font-body: Inter, system-ui, sans-serif;
  --font-mono: JetBrains Mono, monospace;

  /* 间距变量 */
  --spacing-unit: 8px;
  --spacing-sm: calc(var(--spacing-unit) * 1);
  --spacing-md: calc(var(--spacing-unit) * 2);
  --spacing-lg: calc(var(--spacing-unit) * 4);

  /* 圆角变量 */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 16px;

  /* 布局变量 */
  --container-max: 720px;
  --content-padding: 24px;
}

/* 使用变量 */
.article-content {
  max-width: var(--container-max);
  padding: var(--content-padding);
  color: var(--color-text);
  font-family: var(--font-body);
}

.btn-primary {
  background-color: var(--color-primary);
  border-radius: var(--radius-md);
  padding: var(--spacing-sm) var(--spacing-md);
}
```

---

### 4.2 样式配置系统

#### 4.2.1 配置层级

```
┌─────────────────────────────────────────────────────────────┐
│                    样式配置金字塔                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│                       ┌─────────┐                           │
│                      │ 自定义   │  开发者：编辑 CSS/SCSS    │
│                     │   CSS    │  完整控制，需要技术能力    │
│                    └─────────┘                              │
│                   ┌─────────────┐                           │
│                  │   主题配置   │  高级用户：修改主题 JSON   │
│                 │  (JSON 文件)   │  颜色/字体/间距配置       │
│                └─────────────┘                              │
│               ┌─────────────────┐                           │
│              │   可视化配置器   │  普通用户：后台配置面板    │
│             │  (颜色/字体/滑块)  │  所见即所得，实时预览     │
│            └─────────────────┘                              │
│           ┌─────────────────────┐                           │
│          │      预设主题库      │  一键切换，零配置         │
│         └─────────────────────┘                             │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 4.2.2 后台样式配置 UI

```
┌─────────────────────────────────────────────────────────────────────────┐
│  后台管理系统 - 样式配置                              [重置] [保存] [预览]│
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─ 颜色配置 ────────────────────────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  主色调                                                             │  │
│  │  ┌─────┐  #3B82F6  [选择器]                                        │  │
│  │  │ ███ │  用于：按钮、链接、高亮元素                                │  │
│  │  └─────┘                                                           │  │
│  │                                                                    │  │
│  │  辅色调                                                             │  │
│  │  ┌─────┐  #64748B  [选择器]                                        │  │
│  │  │ ███ │  用于：次要按钮、边框、分隔线                              │  │
│  │  └─────┘                                                           │  │
│  │                                                                    │  │
│  │  强调色                                                             │  │
│  │  ┌─────┐  #8B5CF6  [选择器]                                        │  │
│  │  │ ███ │  用于：徽章、标签、特殊提示                                │  │
│  │  └─────┘                                                           │  │
│  │                                                                    │  │
│  │  背景色                                                             │  │
│  │  ┌─────┐  #FFFFFF  [选择器]                                        │  │
│  │  │ ███ │                                                           │  │
│  │  └─────┘                                                           │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ 字体配置 ────────────────────────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  标题字体                                                           │  │
│  │  [system-ui ▼]  [思源黑体 ▼]  [自定义_______]                      │  │
│  │                                                                    │  │
│  │  正文字体                                                           │  │
│  │  [Inter ▼]  [思源宋体 ▼]  [自定义_______]                          │  │
│  │                                                                    │  │
│  │  等宽字体                                                           │  │
│  │  [JetBrains Mono ▼]  [Fira Code ▼]  [自定义_______]               │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ 间距配置 ────────────────────────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  基础间距单位                                                       │  │
│  │  [━━●━━━━━━]  8px (4px - 16px)                                   │  │
│  │                                                                    │  │
│  │  内容最大宽度                                                       │  │
│  │  [━━━━━●━━━━━]  720px (600px - 1200px)                          │  │
│  │                                                                    │  │
│  │  内容内边距                                                         │  │
│  │  [━━━━●━━━━━━]  24px (16px - 48px)                              │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ 圆角配置 ────────────────────────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  小圆角 (按钮/徽章)                                                   │  │
│  │  [━━●━━━━━━]  4px (0px - 12px)                                   │  │
│  │                                                                    │  │
│  │  中圆角 (卡片/模块)                                                   │  │
│  │  [━━━●━━━━━]  8px (0px - 24px)                                   │  │
│  │                                                                    │  │
│  │  大圆角 (弹窗/模态框)                                                 │  │
│  │  [━━━━●━━━━]  16px (0px - 32px)                                  │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ 高级 CSS ────────────────────────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  [编辑自定义 CSS]  →  打开代码编辑器                               │  │
│  │                                                                    │  │
│  │  :root {                                                           │  │
│  │    --custom-bg: #fafafa;                                          │  │
│  │    .article-content p { line-height: 1.8; }                       │  │
│  │  }                                                                 │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ 实时预览 ───────────────────────────────────────────────────────┐   │
│  │  ┌────────────────────────────────────────────────────────────┐   │  │
│  │  │  你的博客 - 实时预览 (配置变更后自动刷新)                    │   │  │
│  │  │  ────────────────────────────────────────────────────────   │   │  │
│  │  │  [这是一个按钮示例]  [这是次要按钮]                          │   │  │
│  │  │                                                             │   │  │
│  │  │  文章标题示例                                                │   │  │
│  │  │  这里是正文内容预览... (应用当前所有配置)                     │   │  │
│  │  └────────────────────────────────────────────────────────────┘   │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 4.3 插件适配系统详解

#### 4.3.1 插件接口定义 (TypeScript)

```typescript
// packages/core/src/plugin/types.ts

// 插件类型枚举
export type PluginType =
  | 'comment'      // 评论
  | 'analytics'    // 统计
  | 'search'       // 搜索
  | 'social'       // 社交
  | 'ad'           // 广告
  | 'feature'      // 功能
  | 'custom';      // 自定义

// 挂载点枚举
export type MountPoint =
  | 'head-start'
  | 'head-end'
  | 'body-start'
  | 'header-start'
  | 'header-end'
  | 'nav-start'
  | 'nav-end'
  | 'sidebar-start'
  | 'sidebar-end'
  | 'main-start'
  | 'main-end'
  | 'post-start'
  | 'post-end'
  | 'comment-before'
  | 'comment-after'
  | 'footer-start'
  | 'footer-end'
  | 'global-floating'
  | 'body-end';

// 配置字段类型
export type ConfigFieldType =
  | 'string'
  | 'number'
  | 'boolean'
  | 'select'
  | 'multi-select'
  | 'code'
  | 'textarea'
  | 'color'
  | 'image';

// 配置字段定义
export interface ConfigField {
  type: ConfigFieldType;
  label: string;
  description?: string;
  required?: boolean;
  default?: any;
  options?: Array<{ label: string; value: any }>; // select 类型使用
  placeholder?: string;
  language?: string; // code 类型使用
  validator?: (value: any) => boolean;
}

// 配置 Schema
export type ConfigSchema = Record<string, ConfigField>;

// 插件元信息
export interface PluginMeta {
  name: string;
  label: string;
  type: PluginType;
  version: string;
  author?: string;
  description?: string;
  website?: string;
  icon?: string;
}

// 服务端路由定义
export interface ServerRoute {
  path: string;
  method: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';
  handler: (request: Request) => Promise<Response>;
  middleware?: Array<(request: Request) => Promise<void>>;
}

// 插件适配器接口
export interface PluginAdapter {
  // 元信息
  meta: PluginMeta;

  // 配置 Schema (决定后台表单结构)
  configSchema: ConfigSchema;

  // 可挂载位置
  mountPoints: MountPoint[];

  // 生命周期钩子
  onInit?(config: any): Promise<void>;
  onMount?(container: HTMLElement, config: any): void;
  onUnmount?(): void;
  onConfigChange?(newConfig: any, oldConfig: any): void;

  // 服务端路由 (可选)
  serverRoutes?: ServerRoute[];

  // 依赖声明 (可选)
  dependencies?: string[]; // 依赖的其他插件
  conflicts?: string[]; // 冲突的插件
}
```

#### 4.3.2 插件注册中心实现

```typescript
// packages/core/src/plugin/registry.ts

import type { PluginAdapter, MountPoint, PluginType } from './types';

export class PluginRegistry {
  // 已注册的插件定义
  private plugins: Map<string, PluginAdapter> = new Map();

  // 已启用的插件名称
  private enabledPlugins: Set<string> = new Set();

  // 插件配置存储
  private configs: Map<string, any> = new Map();

  // 挂载点容器映射
  private mountContainers: Map<MountPoint, HTMLElement[]> = new Map();

  /**
   * 注册插件定义
   */
  register(plugin: PluginAdapter): void {
    if (this.plugins.has(plugin.meta.name)) {
      console.warn(`Plugin "${plugin.meta.name}" is already registered.`);
      return;
    }
    this.plugins.set(plugin.meta.name, plugin);
  }

  /**
   * 启用插件
   */
  async enable(pluginName: string, config: any): Promise<void> {
    const plugin = this.plugins.get(pluginName);
    if (!plugin) {
      throw new Error(`Plugin "${pluginName}" not found.`);
    }

    // 检查依赖
    if (plugin.dependencies) {
      for (const dep of plugin.dependencies) {
        if (!this.enabledPlugins.has(dep)) {
          throw new Error(`Plugin "${pluginName}" requires "${dep}" to be enabled first.`);
        }
      }
    }

    // 检查冲突
    if (plugin.conflicts) {
      for (const conflict of plugin.conflicts) {
        if (this.enabledPlugins.has(conflict)) {
          throw new Error(`Plugin "${pluginName}" conflicts with "${conflict}".`);
        }
      }
    }

    // 验证配置
    this.validateConfig(plugin.configSchema, config);

    // 初始化插件
    if (plugin.onInit) {
      await plugin.onInit(config);
    }

    // 记录启用状态
    this.enabledPlugins.add(pluginName);
    this.configs.set(pluginName, config);

    // 挂载到所有声明的挂载点
    this.mountPlugin(plugin, config);

    // 保存到数据库
    await this.savePluginConfig(pluginName, config);
  }

  /**
   * 禁用插件
   */
  async disable(pluginName: string): Promise<void> {
    const plugin = this.plugins.get(pluginName);
    if (!plugin) {
      throw new Error(`Plugin "${pluginName}" not found.`);
    }

    // 调用卸载钩子
    if (plugin.onUnmount) {
      plugin.onUnmount();
    }

    // 从 DOM 移除
    this.unmountPlugin(plugin);

    // 移除启用状态
    this.enabledPlugins.delete(pluginName);

    // 从数据库移除
    await this.deletePluginConfig(pluginName);
  }

  /**
   * 获取已启用的插件
   */
  getEnabledPlugins(type?: PluginType): PluginAdapter[] {
    let plugins = Array.from(this.enabledPlugins)
      .map((name) => this.plugins.get(name)!)
      .filter(Boolean);

    if (type) {
      plugins = plugins.filter((p) => p.meta.type === type);
    }

    return plugins;
  }

  /**
   * 渲染挂载点的所有插件
   */
  renderMountPoint(mountPoint: MountPoint, container: HTMLElement): void {
    // 记录容器
    if (!this.mountContainers.has(mountPoint)) {
      this.mountContainers.set(mountPoint, []);
    }
    this.mountContainers.get(mountPoint)!.push(container);

    // 渲染所有已启用且支持此挂载点的插件
    const plugins = this.getEnabledPlugins().filter((p) =>
      p.mountPoints.includes(mountPoint)
    );

    for (const plugin of plugins) {
      const pluginContainer = document.createElement('div');
      pluginContainer.className = `plugin-${plugin.meta.name}`;
      pluginContainer.dataset.plugin = plugin.meta.name;
      container.appendChild(pluginContainer);

      const config = this.configs.get(plugin.meta.name);
      if (plugin.onMount) {
        plugin.onMount(pluginContainer, config);
      }
    }
  }

  /**
   * 更新插件配置
   */
  async updateConfig(pluginName: string, newConfig: any): Promise<void> {
    const plugin = this.plugins.get(pluginName);
    if (!plugin) {
      throw new Error(`Plugin "${pluginName}" not found.`);
    }

    const oldConfig = this.configs.get(pluginName);
    this.validateConfig(plugin.configSchema, newConfig);

    // 调用配置变更钩子
    if (plugin.onConfigChange) {
      plugin.onConfigChange(newConfig, oldConfig);
    }

    // 重新渲染 (如果需要)
    if (plugin.onMount) {
      this.reloadPlugin(plugin, newConfig);
    }

    // 保存新配置
    this.configs.set(pluginName, newConfig);
    await this.savePluginConfig(pluginName, newConfig);
  }

  /**
   * 验证配置
   */
  private validateConfig(schema: ConfigSchema, config: any): void {
    for (const [key, field] of Object.entries(schema)) {
      if (field.required && !(key in config)) {
        throw new Error(`Missing required config field: ${key}`);
      }
      if (field.validator && key in config && !field.validator(config[key])) {
        throw new Error(`Invalid config value for field: ${key}`);
      }
    }
  }

  /**
   * 挂载插件到所有声明的位置
   */
  private mountPlugin(plugin: PluginAdapter, config: any): void {
    for (const mountPoint of plugin.mountPoints) {
      const containers = this.mountContainers.get(mountPoint) || [];
      for (const container of containers) {
        const pluginContainer = document.createElement('div');
        pluginContainer.className = `plugin-${plugin.meta.name}`;
        pluginContainer.dataset.plugin = plugin.meta.name;
        container.appendChild(pluginContainer);

        if (plugin.onMount) {
          plugin.onMount(pluginContainer, config);
        }
      }
    }
  }

  /**
   * 卸载插件从所有挂载点
   */
  private unmountPlugin(plugin: PluginAdapter): void {
    document.querySelectorAll(`[data-plugin="${plugin.meta.name}"]`).forEach((el) => {
      el.remove();
    });
  }

  /**
   * 重新加载插件
   */
  private reloadPlugin(plugin: PluginAdapter, config: any): void {
    this.unmountPlugin(plugin);
    this.mountPlugin(plugin, config);
  }

  /**
   * 保存配置到数据库
   */
  private async savePluginConfig(pluginName: string, config: any): Promise<void> {
    // 调用 Nitro API
    await fetch('/api/plugins/config', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ pluginName, config }),
    });
  }

  /**
   * 从数据库删除配置
   */
  private async deletePluginConfig(pluginName: string): Promise<void> {
    await fetch(`/api/plugins/config/${pluginName}`, {
      method: 'DELETE',
    });
  }
}

// 全局单例
export const pluginRegistry = new PluginRegistry();
```

#### 4.3.3 插件开发示例

```typescript
// packages/plugins/twikoo/src/index.ts
import { definePlugin, type PluginAdapter, type ConfigSchema } from '@my-blog/core';

export default definePlugin({
  meta: {
    name: 'twikoo',
    label: 'Twikoo 评论',
    type: 'comment',
    version: '1.6.0',
    author: 'imaegoo',
    description: '轻量、简洁、优雅的评论系统',
    website: 'https://twikoo.js.org/',
  },

  configSchema: {
    envId: {
      type: 'string',
      label: '环境 ID',
      description: 'Twikoo 云开发环境 ID 或自建服务地址',
      required: true,
      placeholder: 'https://your-twikoo-url.com',
    },
    region: {
      type: 'string',
      label: '区域',
      description: '腾讯云区域，自建服务可不填',
      required: false,
      default: '',
    },
    theme: {
      type: 'select',
      label: '主题',
      options: [
        { label: '跟随博客', value: 'auto' },
        { label: '浅色', value: 'light' },
        { label: '暗黑', value: 'dark' },
      ],
      default: 'auto',
    },
    emoji: {
      type: 'select',
      label: '表情库',
      options: [
        { label: '默认', value: '默认' },
        { label: 'Bilibis', value: 'Bilibis' },
        { label: 'Twitter', value: 'Twitter' },
        { label: 'QQ', value: 'QQ' },
      ],
      default: '默认',
    },
    pathPrefix: {
      type: 'string',
      label: '路径前缀',
      description: '评论路径前缀，用于区分不同页面',
      required: false,
      default: '',
    },
  },

  mountPoints: ['post-end', 'comment-before'],

  async onInit(config) {
    // 动态加载 Twikoo SDK
    if (!(window as any).twikoo) {
      await this.loadScript('https://cdn.jsdelivr.net/npm/twikoo/dist/twikoo.all.min.js');
    }
  },

  onMount(container, config) {
    container.innerHTML = '<div id="twikoo-container"></div>';

    (window as any).twikoo.init({
      envId: config.envId,
      el: '#twikoo-container',
      region: config.region,
      path: config.pathPrefix + window.location.pathname,
      theme: config.theme === 'auto' ? 'auto' : config.theme,
      emoji: config.emoji,
    });
  },

  onUnmount() {
    // 清理 Twikoo 实例
    if ((window as any).twikoo) {
      (window as any).twikoo.destroy && (window as any).twikoo.destroy();
    }
  },

  onConfigChange(newConfig) {
    // 配置变更时重新初始化
    this.onMount && this.onMount(document.querySelector('#twikoo-container')!, newConfig);
  },

  // 工具方法
  private loadScript(src: string): Promise<void> {
    return new Promise((resolve, reject) => {
      const script = document.createElement('script');
      script.src = src;
      script.onload = () => resolve();
      script.onerror = () => reject(new Error(`Failed to load script: ${src}`));
      document.head.appendChild(script);
    });
  },
}) as PluginAdapter;
```

---

### 4.4 文章管理系统

#### 4.4.1 双编辑器支持

**可视化编辑器 (TipTap)**:
- 所见即所得
- 支持富文本格式
- 支持图片拖拽上传
- 支持 Markdown 快捷键
- 支持自定义扩展

**Markdown 编辑器 (Vditor)**:
- 支持即时渲染
- 支持三种模式切换 (编辑/预览/源码)
- 支持大纲导航
- 支持代码高亮
- 支持流程图/时序图等

#### 4.4.2 编辑器切换

```
┌─────────────────────────────────────────────────────────────────┐
│  文章编辑器                                     [保存] [发布]    │
├─────────────────────────────────────────────────────────────────┤
│  标题：[我的第一篇博客文章___________________________]            │
│                                                                  │
│  ┌────────────┬──────────────────────────────────────────────┐  │
│  │            │  ┌────────────────────────────────────────┐  │  │
│  │  文章      │  │  B  I  U  💡 📷  🔗  ⋮   [👁] [✏️] [💻]│  │  │
│  │  分类      │  ├────────────────────────────────────────┤  │  │
│  │  标签      │  │                                        │  │  │
│  │  设置      │  │  今天开始搭建我的新博客！                │  │  │
│  │            │  │                                        │  │  │
│  │  [切换     │  │  技术栈选择：                          │  │  │
│  │   可视化]  │  │  - Nuxt 3 for SSR                      │  │  │
│  │            │  │  - Tailwind CSS for styling            │  │  │
│  │            │  │                                        │  │  │
│  │            │  │  [图片拖拽上传区域]                    │  │  │
│  │            │  │                                        │  │  │
│  │            │  └────────────────────────────────────────┘  │  │
│  └────────────┴──────────────────────────────────────────────┘  │
│                                                                  │
│  状态：草稿    最后保存：刚刚                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

### 4.5 数据库设计详解

#### 4.5.1 完整 Schema 定义

```typescript
// packages/database/src/schema/index.ts

import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core';
import { relations } from 'drizzle-orm';

// ==================== 文章相关 ====================

// 文章表
export const posts = sqliteTable('posts', {
  id: text('id').primaryKey(), // ULID 或 UUID
  title: text('title').notNull(),
  slug: text('slug').notNull().unique(), // URL 友好的标识
  content: text('content').notNull(), // HTML 或 Markdown 内容
  excerpt: text('excerpt'), // 摘要
  coverImage: text('cover_image'), // 封面图
  status: text('status', { enum: ['draft', 'published', 'scheduled'] }).default('draft'),
  publishedAt: integer('published_at', { mode: 'timestamp' }),
  scheduledAt: integer('scheduled_at', { mode: 'timestamp' }),
  viewCount: integer('view_count').default(0),
  likeCount: integer('like_count').default(0),
  commentCount: integer('comment_count').default(0),
  seoTitle: text('seo_title'),
  seoDescription: text('seo_description'),
  seoKeywords: text('seo_keywords'),
  isTop: integer('is_top', { mode: 'boolean' }).default(false), // 是否置顶
  allowComment: integer('allow_comment', { mode: 'boolean' }).default(true),
  password: text('password'), // 密码保护
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 分类表 (支持多级分类)
export const categories = sqliteTable('categories', {
  id: integer('id').primaryKey(),
  name: text('name').notNull(),
  slug: text('slug').notNull().unique(),
  description: text('description'),
  parentId: integer('parent_id').references((): any => categories.id),
  sortOrder: integer('sort_order').default(0),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 标签表
export const tags = sqliteTable('tags', {
  id: integer('id').primaryKey(),
  name: text('name').notNull(),
  slug: text('slug').notNull().unique(),
  color: text('color').default('#3B82F6'), // 标签颜色
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 文章 - 分类关联 (支持多分类)
export const postCategories = sqliteTable('post_categories', {
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  categoryId: integer('category_id')
    .notNull()
    .references(() => categories.id, { onDelete: 'cascade' }),
  isPrimary: integer('is_primary', { mode: 'boolean' }).default(false), // 是否为主分类
}, (t) => ({
  pk: primaryKey({ columns: [t.postId, t.categoryId] }),
}));

// 文章 - 标签关联
export const postTags = sqliteTable('post_tags', {
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  tagId: integer('tag_id')
    .notNull()
    .references(() => tags.id, { onDelete: 'cascade' }),
}, (t) => ({
  pk: primaryKey({ columns: [t.postId, t.tagId] }),
}));

// ==================== 评论相关 ====================

// 评论表
export const comments = sqliteTable('comments', {
  id: integer('id').primaryKey(),
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  userId: text('user_id'), // 关联用户表 (如果用户登录评论)
  authorName: text('author_name').notNull(),
  authorEmail: text('author_email').notNull(),
  authorWebsite: text('author_website'),
  authorAvatar: text('author_avatar'),
  content: text('content').notNull(),
  parentId: integer('parent_id').references((): any => comments.id), // 回复评论
  status: text('status', { enum: ['pending', 'approved', 'spam', 'trash'] }).default('pending'),
  ipAddress: text('ip_address'),
  userAgent: text('user_agent'),
  likeCount: integer('like_count').default(0),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 用户相关 ====================

// 用户表
export const users = sqliteTable('users', {
  id: text('id').primaryKey(),
  username: text('username').notNull().unique(),
  email: text('email').notNull().unique(),
  password: text('password').notNull(), // 加密后的密码
  displayName: text('display_name'),
  avatar: text('avatar'),
  bio: text('bio'),
  website: text('website'),
  role: text('role', { enum: ['admin', 'editor', 'author', 'subscriber'] }).default('subscriber'),
  status: text('status', { enum: ['active', 'inactive', 'banned'] }).default('active'),
  lastLoginAt: integer('last_login_at', { mode: 'timestamp' }),
  lastLoginIp: text('last_login_ip'),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 媒体相关 ====================
// 完整媒体资源管理方案：包含存储架构、处理队列、变体生成、CDN 加速

// 媒体资源主表
export const media = sqliteTable('media', {
  id: text('id').primaryKey(), // ULID

  // 基本信息
  userId: text('user_id').references(() => users.id), // 上传者
  filename: text('filename').notNull(), // 存储文件名 (abc123.webp)
  originalName: text('original_name'), // 原始文件名 (我的图片.png)
  title: text('title'), // 标题 (可编辑)
  alt: text('alt'), // Alt 文本 (可编辑)
  caption: text('caption'), // 说明文字
  description: text('description'), // 详细描述

  // 文件信息
  mimeType: text('mime_type').notNull(), // image/png, video/mp4
  size: integer('size').notNull(), // 字节
  extension: text('extension').notNull(), // png, mp4

  // 存储信息
  storageType: text('storage_type', {
    enum: ['local', 'oss', 'cos', 's3', 'external']
  }).default('local'),
  storageBucket: text('storage_bucket'), // OSS Bucket 名称
  storagePath: text('storage_path').notNull(), // 存储路径
  url: text('url').notNull(), // 完整 URL
  cdnUrl: text('cdn_url'), // CDN 加速 URL

  // 尺寸信息 (图片/视频)
  width: integer('width'),
  height: integer('height'),
  duration: integer('duration'), // 视频时长 (秒)

  // 缩略图/多尺寸 (JSON 存储)
  variants: text('variants', { mode: 'json' }),
  // {
  //   "thumbnail": { "url": "...", "width": 150, "height": 150, "size": 5000 },
  //   "medium": { "url": "...", "width": 600, "height": 400, "size": 50000 },
  //   "large": { "url": "...", "width": 1200, "height": 800, "size": 150000 },
  //   "webp": { "url": "...", "size": 180000 }
  // }

  // 视频特有信息
  videoCodec: text('video_codec'), // h264, h265, vp9
  audioCodec: text('audio_codec'), // aac, mp3
  bitrate: integer('bitrate'), // 比特率
  framerate: real('framerate'), // 帧率
  hasMultipleQuality: integer('has_multiple_quality', { mode: 'boolean' }).default(false),
  hlsPlaylist: text('hls_playlist'), // HLS m3u8 文件路径

  // 使用统计
  viewCount: integer('view_count').default(0), // 被引用次数
  downloadCount: integer('download_count').default(0),

  // 状态
  status: text('status', {
    enum: ['pending', 'processing', 'ready', 'failed']
  }).default('pending'),
  processingProgress: integer('processing_progress').default(0), // 处理进度 0-100
  errorMessage: text('error_message'), // 处理失败时的错误信息

  // 元数据
  metadata: text('metadata', { mode: 'json' }), // EXIF, XMP 等
  // {
  //   "exif": { "camera": "iPhone 15", "lens": "...", "iso": 100, ... },
  //   "xmp": { ... },
  //   "colors": ["#3B82F6", "#64748B", ...] // 主导色
  // }

  // 时间戳
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  uploadedAt: integer('uploaded_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 媒体 - 实体关联表 (多对多)
export const mediaRelations = sqliteTable('media_relations', {
  mediaId: text('media_id')
    .notNull()
    .references(() => media.id, { onDelete: 'cascade' }),
  entityType: text('entity_type').notNull(), // 'post', 'user', 'page'
  entityId: text('entity_id').notNull(), // 关联实体 ID
  relationType: text('relation_type', { // 关联类型
    enum: ['cover', 'thumbnail', 'inline', 'attachment', 'avatar']
  }).default('inline'),
  sortOrder: integer('sort_order').default(0),
}, (t) => ({
  pk: primaryKey({ columns: [t.mediaId, t.entityType, t.entityId] }),
  idx_entity: index('idx_media_relations_entity').on(t.entityType, t.entityId),
}));

// 媒体处理任务表
export const mediaProcessingTasks = sqliteTable('media_processing_tasks', {
  id: text('id').primaryKey(),
  mediaId: text('media_id')
    .notNull()
    .references(() => media.id, { onDelete: 'cascade' }),
  taskType: text('task_type', { // 任务类型
    enum: [
      'resize',           // 调整尺寸
      'convert_webp',     // 转 WebP
      'generate_thumbnail', // 生成缩略图
      'video_transcode',  // 视频转码
      'video_hls',        // HLS 切片
      'watermark',        // 添加水印
      'optimize',         // 优化压缩
    ]
  }).notNull(),
  status: text('status', {
    enum: ['pending', 'processing', 'completed', 'failed']
  }).default('pending'),
  priority: integer('priority').default(0), // 优先级，越高越先处理
  params: text('params', { mode: 'json' }), // 任务参数
  result: text('result', { mode: 'json' }), // 处理结果
  errorMessage: text('error_message'),
  retryCount: integer('retry_count').default(0),
  maxRetries: integer('max_retries').default(3),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  startedAt: integer('started_at', { mode: 'timestamp' }),
  completedAt: integer('completed_at', { mode: 'timestamp' }),
});

// 媒体使用统计表
export const mediaUsageStats = sqliteTable('media_usage_stats', {
  id: integer('id').primaryKey(),
  mediaId: text('media_id')
    .notNull()
    .references(() => media.id, { onDelete: 'cascade' }),
  date: text('date').notNull(), // YYYY-MM-DD
  viewType: text('view_type', { // 查看类型
    enum: ['direct', 'page', 'api', 'cdn']
  }),
  viewCount: integer('view_count').default(0),
  bandwidth: integer('bandwidth').default(0), // 流量消耗 (字节)
}, (t) => ({
  unique: uniqueIndex('idx_media_usage_unique').on(t.mediaId, t.date, t.viewType),
}));

// 存储配置表
export const storageConfigs = sqliteTable('storage_configs', {
  id: integer('id').primaryKey(),
  name: text('name').notNull().unique(), // 'local', 'aliyun-oss', 'tencent-cos'
  type: text('type', {
    enum: ['local', 'oss', 'cos', 's3']
  }).notNull(),
  config: text('config', { mode: 'json' }).notNull(),
  // {
  //   "accessKeyId": "...",
  //   "accessKeySecret": "...",
  //   "bucket": "my-blog-media",
  //   "region": "oss-cn-hangzhou",
  //   "endpoint": "oss-cn-hangzhou.aliyuncs.com",
  //   "cdnDomain": "cdn.example.com",
  //   "cdnHttps": true
  // }
  isDefault: integer('is_default', { mode: 'boolean' }).default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 主题/样式相关 ====================

// 主题配置表
export const themeSettings = sqliteTable('theme_settings', {
  id: integer('id').primaryKey(),
  themeName: text('theme_name').notNull().unique(),
  config: text('config', { mode: 'json' }).notNull(),
  isActive: integer('is_active', { mode: 'boolean' }).default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 样式配置表
export const styleSettings = sqliteTable('style_settings', {
  id: integer('id').primaryKey(),
  configKey: text('config_key').notNull().unique(),
  configValue: text('config_value').notNull(),
  category: text('category'), // colors, fonts, spacing, etc.
  description: text('description'),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 插件相关 ====================

// 插件配置表
export const pluginSettings = sqliteTable('plugin_settings', {
  id: integer('id').primaryKey(),
  pluginName: text('plugin_name').notNull().unique(),
  config: text('config', { mode: 'json' }).notNull(),
  enabled: integer('enabled', { mode: 'boolean' }).default(false),
  version: text('version'), // 插件版本
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 系统相关 ====================

// 系统设置表
export const systemSettings = sqliteTable('system_settings', {
  id: integer('id').primaryKey(),
  key: text('key').notNull().unique(),
  value: text('value', { mode: 'json' }).notNull(),
  category: text('category'), // site, seo, email, etc.
  description: text('description'),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 会话表 (用于用户登录)
export const sessions = sqliteTable('sessions', {
  id: text('id').primaryKey(),
  userId: text('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  token: text('token').notNull().unique(),
  expiresAt: integer('expires_at', { mode: 'timestamp' }).notNull(),
  ipAddress: text('ip_address'),
  userAgent: text('user_agent'),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== 关系定义 ====================

export const postsRelations = relations(posts, ({ many, one }) => ({
  categories: many(postCategories),
  tags: many(postTags),
  comments: many(comments),
  author: one(users, {
    fields: [posts.id],
    references: [users.id],
  }),
}));

export const categoriesRelations = relations(categories, ({ many, one }) => ({
  posts: many(postCategories),
  parent: one(categories, {
    fields: [categories.parentId],
    references: [categories.id],
  }),
  children: many(categories, {
    relationName: 'category_hierarchy',
  }),
}));

export const tagsRelations = relations(tags, ({ many }) => ({
  posts: many(postTags),
}));

export const commentsRelations = relations(comments, ({ one, many }) => ({
  post: one(posts, {
    fields: [comments.postId],
    references: [posts.id],
  }),
  parent: one(comments, {
    fields: [comments.parentId],
    references: [comments.id],
  }),
  replies: many(comments, {
    relationName: 'comment_replies',
  }),
}));

// ==================== 文章扩展 ====================

// 文章版本控制表
export const postRevisions = sqliteTable('post_revisions', {
  id: text('id').primaryKey(),
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  userId: text('user_id').references(() => users.id),
  title: text('title').notNull(),
  content: text('content').notNull(),
  reason: text('reason'), // 修改原因
  isAutoSave: integer('is_auto_save', { mode: 'boolean' }).default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_post: index('idx_post_revisions_post_id').on(t.postId),
  idx_created: index('idx_post_revisions_created_at').on(t.createdAt),
}));

// 文章系列表
export const series = sqliteTable('series', {
  id: integer('id').primaryKey(),
  name: text('name').notNull(),
  slug: text('slug').notNull().unique(),
  description: text('description'),
  coverImage: text('cover_image'),
  sortOrder: integer('sort_order').default(0),
  isPublished: integer('is_published', { mode: 'boolean' }).default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 文章 - 系列关联
export const postSeries = sqliteTable('post_series', {
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  seriesId: integer('series_id')
    .notNull()
    .references(() => series.id, { onDelete: 'cascade' }),
  position: integer('position').notNull(), // 在系列中的位置
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  pk: primaryKey({ columns: [t.postId, t.seriesId] }),
}));

// 文章点赞表
export const postLikes = sqliteTable('post_likes', {
  id: integer('id').primaryKey(),
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  userId: text('user_id').references(() => users.id),
  ipAddress: text('ip_address'), // 未登录用户用 IP 标识
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  unique: uniqueIndex('idx_unique_like').on(t.postId, t.userId, t.ipAddress),
}));

// 文章浏览统计表
export const postViews = sqliteTable('post_views', {
  id: integer('id').primaryKey(),
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  date: text('date').notNull(), // YYYY-MM-DD
  viewCount: integer('view_count').default(0),
  uniqueVisitors: integer('unique_visitors').default(0),
}, (t) => ({
  unique: uniqueIndex('idx_unique_views').on(t.postId, t.date),
}));

// 文章收藏表
export const postBookmarks = sqliteTable('post_bookmarks', {
  id: integer('id').primaryKey(),
  postId: text('post_id')
    .notNull()
    .references(() => posts.id, { onDelete: 'cascade' }),
  userId: text('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  folderId: integer('folder_id').references(() => bookmarkFolders.id),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  unique: uniqueIndex('idx_unique_bookmark').on(t.postId, t.userId),
}));

// 收藏文件夹表
export const bookmarkFolders = sqliteTable('bookmark_folders', {
  id: integer('id').primaryKey(),
  userId: text('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  parentId: integer('parent_id').references((): any => bookmarkFolders.id),
  sortOrder: integer('sort_order').default(0),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 自定义字段定义表
export const customFields = sqliteTable('custom_fields', {
  id: integer('id').primaryKey(),
  name: text('name').notNull(),
  label: text('label').notNull(),
  type: text('type', {
    enum: ['text', 'textarea', 'number', 'select', 'image', 'file', 'json']
  }).notNull(),
  defaultValue: text('default_value'),
  options: text('options', { mode: 'json' }), // select 选项
  entityType: text('entity_type', {
    enum: ['post', 'user', 'page', 'category']
  }).notNull(),
  sortOrder: integer('sort_order').default(0),
  isRequired: integer('is_required', { mode: 'boolean' }).default(false),
  validation: text('validation'), // 正则表达式
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 自定义字段值表
export const customFieldValues = sqliteTable('custom_field_values', {
  id: integer('id').primaryKey(),
  fieldId: integer('field_id')
    .notNull()
    .references(() => customFields.id, { onDelete: 'cascade' }),
  entityId: text('entity_id').notNull(),
  value: text('value', { mode: 'json' }),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  unique: uniqueIndex('idx_unique_field_value').on(t.fieldId, t.entityId),
}));

// ==================== 插件系统扩展 ====================

// 插件钩子注册表
export const pluginHooks = sqliteTable('plugin_hooks', {
  id: integer('id').primaryKey(),
  pluginName: text('plugin_name')
    .notNull()
    .references(() => pluginSettings.pluginName, { onDelete: 'cascade' }),
  hookName: text('hook_name').notNull(),
  priority: integer('priority').default(10),
  isEnabled: integer('is_enabled', { mode: 'boolean' }).default(true),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_hook: index('idx_plugin_hooks_name').on(t.hookName),
}));

// 插件路由注册表
export const pluginRoutes = sqliteTable('plugin_routes', {
  id: integer('id').primaryKey(),
  pluginName: text('plugin_name')
    .notNull()
    .references(() => pluginSettings.pluginName, { onDelete: 'cascade' }),
  path: text('path').notNull(),
  method: text('method', {
    enum: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH']
  }).notNull(),
  handler: text('handler').notNull(),
  middleware: text('middleware', { mode: 'json' }),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_path: index('idx_plugin_routes_path').on(t.path, t.method),
}));

// 插件运行日志表
export const pluginLogs = sqliteTable('plugin_logs', {
  id: integer('id').primaryKey(),
  pluginName: text('plugin_name').notNull(),
  level: text('level', {
    enum: ['debug', 'info', 'warn', 'error']
  }).notNull(),
  message: text('message').notNull(),
  context: text('context', { mode: 'json' }),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_plugin: index('idx_plugin_logs_plugin').on(t.pluginName, t.createdAt),
}));

// ==================== 主题系统扩展 ====================

// 主题布局表
export const themeLayouts = sqliteTable('theme_layouts', {
  id: integer('id').primaryKey(),
  themeName: text('theme_name')
    .notNull()
    .references(() => themeSettings.themeName, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  path: text('path').notNull(),
  content: text('content'),
  isActive: integer('is_active', { mode: 'boolean' }).default(false),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 主题组件配置表
export const themeComponents = sqliteTable('theme_components', {
  id: integer('id').primaryKey(),
  themeName: text('theme_name')
    .notNull()
    .references(() => themeSettings.themeName, { onDelete: 'cascade' }),
  componentName: text('component_name').notNull(),
  position: text('position'),
  config: text('config', { mode: 'json' }),
  isEnabled: integer('is_enabled', { mode: 'boolean' }).default(true),
  sortOrder: integer('sort_order').default(0),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 样式预设表
export const stylePresets = sqliteTable('style_presets', {
  id: integer('id').primaryKey(),
  name: text('name').notNull(),
  description: text('description'),
  type: text('type', {
    enum: ['color', 'font', 'spacing', 'layout', 'complete']
  }).notNull(),
  config: text('config', { mode: 'json' }).notNull(),
  previewImage: text('preview_image'),
  isBuiltIn: integer('is_built_in', { mode: 'boolean' }).default(false),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// ==================== SEO 和统计扩展 ====================

// SEO 重定向表
export const seoRedirects = sqliteTable('seo_redirects', {
  id: integer('id').primaryKey(),
  sourcePath: text('source_path').notNull().unique(),
  targetPath: text('target_path').notNull(),
  redirectType: integer('redirect_type').default(301),
  isEnabled: integer('is_enabled', { mode: 'boolean' }).default(true),
  hitCount: integer('hit_count').default(0),
  lastHitAt: integer('last_hit_at', { mode: 'timestamp' }),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 访问统计表
export const trafficStats = sqliteTable('traffic_stats', {
  id: integer('id').primaryKey(),
  date: text('date').notNull(), // YYYY-MM-DD
  hour: integer('hour'), // 小时 (0-23)，NULL 表示天级别汇总
  pageViews: integer('page_views').default(0),
  uniqueVisitors: integer('unique_visitors').default(0),
  newVisitors: integer('new_visitors').default(0),
  returningVisitors: integer('returning_visitors').default(0),
  bounceRate: real('bounce_rate'),
  avgSessionDuration: integer('avg_session_duration'),
  topReferrer: text('top_referrer'),
  topPage: text('top_page'),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  unique: uniqueIndex('idx_unique_traffic').on(t.date, t.hour),
}));

// 搜索日志表
export const searchLogs = sqliteTable('search_logs', {
  id: integer('id').primaryKey(),
  query: text('query').notNull(),
  resultCount: integer('result_count').default(0),
  clickedPostId: text('clicked_post_id'),
  userId: text('user_id').references(() => users.id),
  ipAddress: text('ip_address'),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_query: index('idx_search_logs_query').on(t.query),
  idx_created: index('idx_search_logs_created').on(t.createdAt),
}));

// ==================== 通知系统 ====================

// 通知表
export const notifications = sqliteTable('notifications', {
  id: text('id').primaryKey(),
  type: text('type').notNull(),
  title: text('title').notNull(),
  content: text('content').notNull(),
  data: text('data', { mode: 'json' }),
  imageUrl: text('image_url'),
  actionUrl: text('action_url'),
  isDismissible: integer('is_dismissible', { mode: 'boolean' }).default(true),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 用户通知关联表
export const userNotifications = sqliteTable('user_notifications', {
  id: integer('id').primaryKey(),
  notificationId: text('notification_id')
    .notNull()
    .references(() => notifications.id, { onDelete: 'cascade' }),
  userId: text('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  isRead: integer('is_read', { mode: 'boolean' }).default(false),
  readAt: integer('read_at', { mode: 'timestamp' }),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
}, (t) => ({
  idx_user: index('idx_user_notifications_user').on(t.userId, t.isRead),
}));

// ==================== 备份和迁移 ====================

// 数据库备份记录表
export const databaseBackups = sqliteTable('database_backups', {
  id: integer('id').primaryKey(),
  filename: text('filename').notNull(),
  filepath: text('filepath').notNull(),
  size: integer('size').notNull(),
  checksum: text('checksum'),
  status: text('status', {
    enum: ['pending', 'processing', 'completed', 'failed']
  }).default('pending'),
  errorMessage: text('error_message'),
  createdAt: integer('created_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
});

// 数据迁移记录表
export const dataMigrations = sqliteTable('data_migrations', {
  id: text('id').primaryKey(),
  executedAt: integer('executed_at', { mode: 'timestamp' }).$defaultFn(() => new Date()),
  executionTime: integer('execution_time'),
  status: text('status', {
    enum: ['success', 'failed', 'partial']
  }).notNull(),
  errorMessage: text('error_message'),
  rolledBack: integer('rolled_back', { mode: 'boolean' }).default(false),
});

// ==================== 扩展关系定义 ====================

// 文章扩展关系
export const postsRelationsExtended = relations(posts, ({ many, one }) => ({
  revisions: many(postRevisions),
  series: one(postSeries),
  likes: many(postLikes),
  views: many(postViews),
  bookmarks: many(postBookmarks),
  customFields: many(customFieldValues),
}));

// 系列关系
export const seriesRelations = relations(series, ({ many }) => ({
  posts: many(postSeries),
}));

// 用户扩展关系
export const usersRelationsExtended = relations(users, ({ many }) => ({
  uploadedMedia: many(media),
  bookmarks: many(postBookmarks),
  notifications: many(userNotifications),
}));

// 媒体关系
export const mediaRelationsExtended = relations(media, ({ one, many }) => ({
  uploader: one(users, {
    fields: [media.userId],
    references: [users.id],
  }),
  relations: many(mediaRelations),
  processingTasks: many(mediaProcessingTasks),
  usageStats: many(mediaUsageStats),
}));

// 插件设置关系
export const pluginSettingsRelations = relations(pluginSettings, ({ many }) => ({
  hooks: many(pluginHooks),
  routes: many(pluginRoutes),
  logs: many(pluginLogs),
}));

// 主题设置关系
export const themeSettingsRelations = relations(themeSettings, ({ many }) => ({
  layouts: many(themeLayouts),
  components: many(themeComponents),
}));
```

---

## 五、核心特性详解

### 5.1 强扩展性 (High Extensibility)

#### 5.1.1 插件系统能力

| 能力 | 说明 | 实现方式 |
|------|------|---------|
| **第三方评论集成** | Twikoo, Waline, Disqus, utterances | 插件适配器模式 |
| **统计分析** | Google Analytics, 百度统计，Umami | head-script 挂载点 |
| **搜索服务** | Algolia, MeiliSearch, 本地搜索 | 插件 API + 前端组件 |
| **社交功能** | 分享按钮，Follow 按钮 | post-end 挂载点 |
| **广告嵌入** | Google AdSense, 自定义广告 | 多位置挂载点 |
| **自定义代码** | 任意 JS 代码/组件 | custom-script 插件 |
| **自研模块** | 完全自定义功能 | 完整插件 SDK |

#### 5.1.2 扩展流程

```
1. 需求分析
   ↓
2. 选择扩展方式
   ├── 已有插件 → 后台安装 + 配置
   ├── 类似插件 → Fork 修改 + 上传
   └── 全新功能 → 从零开发
   ↓
3. 开发插件
   ├── 定义 meta/configSchema/mountPoints
   ├── 实现生命周期钩子
   └── (可选) 添加服务端路由
   ↓
4. 测试
   ├── 本地测试
   └── 后台上传测试
   ↓
5. 发布/使用
   ├── 个人使用 → 直接启用
   └── 公开分享 → 发布到插件市场
```

### 5.2 可变性 (Mutability)

#### 5.2.1 主题/样式热切换

| 变更类型 | 生效方式 | 是否需要重启 |
|---------|---------|------------|
| 切换主题 | 后台选择 → 即时生效 | ❌ 否 |
| 修改颜色 | 配置器调整 → 实时预览 | ❌ 否 |
| 修改字体 | 配置器调整 → 实时预览 | ❌ 否 |
| 修改间距 | 配置器调整 → 实时预览 | ❌ 否 |
| 自定义 CSS | 编辑器保存 → 即时生效 | ❌ 否 |
| 上传新主题 | 上传 → 启用 → 即时生效 | ❌ 否 |

#### 5.2.2 模块热插拔

```
启用模块:
后台 → 插件中心 → 找到插件 → [启用] → 填写配置 → [保存]
       ↓
       插件 SDK 异步加载
       ↓
       组件挂载到指定位置
       ↓
       前台立即可见

禁用模块:
后台 → 插件中心 → 找到插件 → [禁用]
       ↓
       调用 onUnmount 钩子
       ↓
       从 DOM 移除组件
       ↓
       前台立即消失
```

### 5.3 解耦性 (Decoupling)

#### 5.3.1 架构解耦

```
┌─────────────────────────────────────────────────────────────┐
│                        解耦层次                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  应用层解耦                                                  │
│  ┌─────────────┐     ┌─────────────┐                        │
│  │  前台 Site  │     │  后台 Admin │  ← 独立部署，共享 API   │
│  └─────────────┘     └─────────────┘                        │
│                                                              │
│  服务层解耦                                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Nitro API (模块化路由)                  │    │
│  │  /api/articles  ← 独立服务模块                       │    │
│  │  /api/themes    ← 独立服务模块                       │    │
│  │  /api/plugins   ← 独立服务模块 (+ 插件路由动态注册)    │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  数据层解耦                                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Drizzle ORM                            │    │
│  │  - 每个表独立 Schema 定义                             │    │
│  │  - 关系通过 relations 定义，非硬编码                   │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  插件层解耦                                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              插件系统                                │    │
│  │  - 插件间无直接依赖 (可选声明依赖)                   │    │
│  │  - 插件通过挂载点与主应用交互                        │    │
│  │  - 插件配置独立存储                                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  主题层解耦                                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              主题系统                                │    │
│  │  - 主题 = 独立目录 (layout + config + styles)        │    │
│  │  - 主题间完全隔离，互不影响                          │    │
│  │  - 主题可动态切换，无需重建                          │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 5.3.2 模块依赖关系

```
应用依赖图:

                    ┌─────────────┐
                    │    Nginx    │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
            ▼              ▼              ▼
    ┌───────────┐  ┌───────────┐  ┌───────────┐
    │ Site SSR  │  │ Admin SPA │  │   Static  │
    │  (Nuxt)   │  │  (Nuxt)   │  │  Assets   │
    └─────┬─────┘  └─────┬─────┘  └───────────┘
          │              │
          └───────┬──────┘
                  │
                  ▼
         ┌─────────────────┐
         │   Nitro API     │
         │  (服务层)        │
         └────────┬────────┘
                  │
         ┌────────┼────────┐
         │        │        │
         ▼        ▼        ▼
   ┌─────────┐ ┌──────┐ ┌──────────┐
   │ Core    │ │ DB   │ │  Cache   │
   │ (插件/  │ │ ORM  │ │ (Redis/  │
   │  主题)  │ │      │ │ Memory)  │
   └─────────┘ └──────┘ └──────────┘
```

### 5.4 高适配性 (High Adaptability)

#### 5.4.1 第三方服务适配

| 服务类型 | 适配方式 | 配置复杂度 |
|---------|---------|-----------|
| **评论系统** | 插件适配器 | 低 (填 envId) |
| **统计服务** | head-script 注入 | 低 (填 ID) |
| **搜索服务** | 插件 + API 对接 | 中 (需配置 API) |
| **CDN 服务** | 图片上传适配层 | 中 (需配置密钥) |
| **邮件服务** | 后端适配层 | 中 (需 SMTP 配置) |
| **对象存储** | 文件上传适配层 | 中 (需配置密钥) |
| **推送服务** | 插件适配器 | 低 (填 Webhook) |

#### 5.4.2 部署环境适配

| 环境 | 适配方案 |
|------|---------|
| **Docker** | 官方支持，docker-compose.yml |
| **PM2** | ecosystem.config.js 配置 |
| **Systemd** | 提供 service 文件模板 |
| **VPS** | 一键部署脚本 |
| **云平台** | 适配 Vercel/Railway/Render |

### 5.5 高自定义性 (High Customizability)

#### 5.5.1 自定义层级

```
自定义能力金字塔 (从简单到复杂):

Level 1: 预设主题切换
  - 操作：后台选择主题 → 启用
  - 技术要求：无
  - 自定义程度：★☆☆☆☆

Level 2: 可视化样式配置
  - 操作：后台配置面板调整颜色/字体/间距
  - 技术要求：无
  - 自定义程度：★★☆☆☆

Level 3: 自定义 CSS
  - 操作：后台 CSS 编辑器编写样式
  - 技术要求：CSS 基础
  - 自定义程度：★★★☆☆

Level 4: 主题开发
  - 操作：本地创建主题目录 → 编写 layout.vue + config.json + styles.css
  - 技术要求：Vue 基础 + CSS
  - 自定义程度：★★★★☆

Level 5: 插件开发
  - 操作：使用插件 SDK 开发完整功能
  - 技术要求：Vue + TS + Node.js
  - 自定义程度：★★★★★

Level 6: 核心定制
  - 操作：修改 packages/core 源代码
  - 技术要求：完整技术栈
  - 自定义程度：★★★★★ (完全可控)
```

### 5.6 高性能 (High Performance)

#### 5.6.1 性能优化策略

| 层级 | 优化措施 | 预期效果 |
|------|---------|---------|
| **网络层** | Nginx 静态资源缓存 + Gzip | 减少 70% 传输量 |
| **渲染层** | Nuxt 3 SSR 首屏直出 | FCP < 1s |
| **资源层** | 图片懒加载 + WebP 格式 | 减少 50% 图片体积 |
| **代码层** | Vite 按需加载 + Tree Shaking | 减少 40% JS 体积 |
| **数据层** | SQLite WAL 模式 + 索引优化 | 查询速度提升 10 倍 |
| **缓存层** | Redis/内存缓存热点数据 | 减少 80% 数据库查询 |
| **CDN 层** | 静态资源 CDN 分发 | 全球加速访问 |

#### 5.6.2 性能指标目标

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| **FCP (First Contentful Paint)** | < 1.0s | Lighthouse |
| **LCP (Largest Contentful Paint)** | < 2.5s | Lighthouse |
| **TTI (Time to Interactive)** | < 3.5s | Lighthouse |
| **CLS (Cumulative Layout Shift)** | < 0.1 | Lighthouse |
| **TTFB (Time to First Byte)** | < 200ms | WebPageTest |
| **数据库查询延迟** | < 10ms | 直接测量 |
| **API 响应时间 (P95)** | < 100ms | 监控工具 |

---

## 六、预期效果描述

### 6.1 最终成品效果

#### 6.1.1 前台博客效果

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo] 首页    文章    分类    标签    关于    [搜索🔍]        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                                                            │ │
│  │            欢迎来到我的博客                                │ │
│  │            分享技术与生活                                  │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  ┌───────────┐  │  │  ┌───────────┐  │  │  ┌───────────┐  │  │
│  │  │  文章封面  │  │  │  │  文章封面  │  │  │  │  文章封面  │  │  │
│  │  └───────────┘  │  │  └───────────┘  │  │  └───────────┘  │  │
│  │  文章标题一      │  │  文章标题二      │  │  文章标题三      │  │
│  │  2024-03-13     │  │  2024-03-12     │  │  2024-03-11     │  │
│  │  #Vue3  #Nuxt   │  │  #TypeScript    │  │  #插件系统       │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│                                                                  │
│          [← 上一页]   1 / 5   [下一页 →]                          │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│  关于博主    最新评论    标签云    [Twikoo 评论]  [Google 统计]   │
├─────────────────────────────────────────────────────────────────┤
│  © 2024 My Blog. Powered by Nuxt 3.                            │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.1.2 后台管理效果

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo] 博客管理                               [用户▼] [退出]    │
├──────────┬──────────────────────────────────────────────────────┤
│          │                                                       │
│  📊 仪表盘│  文章管理 / 所有文章                                 │
│          │                                                       │
│  📝 文章  │  [+ 新建文章]  [批量操作▼]    [搜索文章_______]     │
│    所有   │                                                       │
│    草稿   │  ┌────────────────────────────────────────────────┐ │
│    已发布 │  │ ☐ │ 标题    │ 分类  │ 标签  │ 日期      │ 状态 │ │
│          │  ├────────────────────────────────────────────────┤ │
│  📁 分类  │  │ ☐ │ 文章一   │ 技术  │ Vue   │ 03-13  │ ✅   │ │
│          │  │ ☐ │ 文章二   │ 生活  │ 随笔  │ 03-12  │ ✅   │ │
│  🏷️ 标签  │  │ ☐ │ 文章三   │ 技术  │ TS    │ 03-11  │ 📝   │ │
│          │  └────────────────────────────────────────────────┘ │
│  🖼️ 媒体库 │                                                       │
│          │  [1-3 共 3 条]  [批量删除] [编辑]                     │
│  🎨 主题   │                                                       │
│    所有   │                                                       │
│    编辑器 │                                                       │
│          │                                                       │
│  🎨 样式   │                                                       │
│          │                                                       │
│  🔌 插件   │                                                       │
│    所有   │                                                       │
│    市场   │                                                       │
│          │                                                       │
│  ⚙️ 设置   │                                                       │
│    站点   │                                                       │
│    SEO    │                                                       │
│    用户   │                                                       │
│          │                                                       │
└──────────┴──────────────────────────────────────────────────────┘
```

### 6.2 用户体验描述

#### 6.2.1 内容创作者体验

```
写文章流程:
1. 登录后台 → 2. 点击"新建文章" → 3. 输入标题和内容
   → 4. 选择分类/标签 → 5. 点击"发布" → 完成

全程可视化操作，无需命令行，无需重建，发布后立即可见。
```

#### 6.2.2 开发者体验

```
开发新主题:
1. 复制 themes/minimal 目录 → 2. 修改 layout.vue/styles.css
   → 3. 后台预览 → 4. 启用 → 完成

开发新插件:
1. npx create-myblog-plugin my-plugin → 2. 编写代码
   → 3. npm run build → 4. 后台上传.zip → 5. 启用 → 完成
```

#### 6.2.3 读者体验

```
访问博客:
1. 打开链接 → 2. 首屏内容立即显示 (SSR) → 3. 快速浏览
   → 4. 点击文章 → 5. 流畅阅读 → 6. 可选评论互动

性能表现:
- 首屏加载 < 1s
- 文章切换 < 300ms
- 评论加载 < 500ms
```

---

## 七、开发计划

### 7.1 阶段划分 (详细)

| 阶段 | 名称 | 内容 | 交付物 | 预计时间 |
|------|------|------|--------|---------|
| **Phase 1** | 脚手架搭建 | Monorepo 结构、基础配置、CI/CD | 可运行的空项目 | 3-5 天 |
| **Phase 2** | 核心框架 | 插件系统、主题系统、钩子系统 | packages/core | 5-7 天 |
| **Phase 3** | 数据库层 | Schema 定义、ORM 配置、迁移脚本 | packages/database | 3-5 天 |
| **Phase 4** | API 层 | Nitro 路由、中间件、服务层 | apps/api | 5-7 天 |
| **Phase 5** | 后台管理 | 文章管理、媒体库、基础 UI | apps/admin (部分) | 7-10 天 |
| **Phase 6** | 前台博客 | 首页、列表、详情页 | apps/site (部分) | 5-7 天 |
| **Phase 7** | 主题系统 | 主题加载、样式配置、预览 | 完整主题功能 | 5-7 天 |
| **Phase 8** | 插件系统 | 插件管理、官方插件开发 | 完整插件功能 | 7-10 天 |
| **Phase 9** | 双编辑器 | TipTap + Vditor 集成 | 完整编辑体验 | 3-5 天 |
| **Phase 10** | 测试优化 | 单元测试、性能优化、Bug 修复 | 稳定版本 | 5-7 天 |
| **Phase 11** | 部署文档 | Docker 配置、部署文档、用户文档 | 可交付产品 | 3-5 天 |

**总预计时间**: 8-10 周

### 7.2 下一步行动

1. ✅ **确认架构方案** - 已完成
2. ⏳ **创建 Git 仓库** - 待进行
3. ⏳ **搭建 Monorepo 脚手架** - 待进行
4. ⏳ **实现核心插件系统** - 待进行
5. ⏳ **开发文章管理模块** - 待进行

---

## 八、风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|---------|
| **技术选型过新** | 中 | 中 | Nuxt 3 已成熟，有官方支持；备选 Next.js |
| **插件系统复杂** | 高 | 中 | 分阶段实现，先核心后扩展；充分测试 |
| **性能问题** | 低 | 高 | SSR + 缓存 + 图片优化；性能监控 |
| **单数据库瓶颈** | 低 | 中 | SQLite 够用；可迁移 MySQL/PostgreSQL |
| **开发周期长** | 中 | 中 | MVP 优先，分阶段交付；社区贡献 |
| **安全问题** | 中 | 高 | 输入验证、XSS 防护、SQL 注入防护；安全审计 |

---

## 九、总结

### 9.1 核心优势总结

| 优势 | 描述 | 竞品对比 |
|------|------|---------|
| **极度解耦** | 主题/样式/模块完全独立，可分别替换 | WordPress 主题插件耦合 |
| **通用适配** | 插件适配系统支持任意第三方/自研功能 | Hexo 插件需改代码 |
| **后台统一** | 所有内容/样式/模块通过后台管理 | 静态博客无后台 |
| **首屏性能** | SSR 渲染，FCP < 1s | WordPress 首屏慢 |
| **双编辑器** | 可视化 + Markdown 自由切换 | 单一编辑器 |
| **自部署** | 完全可控，数据自己掌握 | SaaS 平台锁定 |
| **易扩展** | 16 个挂载点，支持任意位置注入 | 固定模板结构 |
| **热切换** | 主题/样式/插件即改即生效 | 需重建部署 |

### 9.2 适用场景

| 场景 | 适合度 | 理由 |
|------|--------|------|
| **个人博客** | ⭐⭐⭐⭐⭐ | 完全匹配需求 |
| **技术博客** | ⭐⭐⭐⭐⭐ | 代码高亮、MD 支持 |
| **企业官网** | ⭐⭐⭐⭐ | 可定制，但需多语言 |
| **内容平台** | ⭐⭐⭐ | 需多用户/权限扩展 |
| **文档站点** | ⭐⭐⭐⭐ | 主题适配后可用 |

### 9.3 不适用场景

| 场景 | 原因 | 建议方案 |
|------|------|---------|
| **超大型网站** | SQLite 可能瓶颈 | WordPress + MySQL |
| **纯电商** | 缺少电商功能 | Shopify / WooCommerce |
| **论坛社区** | 缺少论坛功能 | Discourse / Flarum |
| **纯静态托管** | 需要服务器 | GitHub Pages + Hexo |

---

**文档版本**: v1.0
**最后更新**: 2026-03-13
**下一步**: 创建 Git 仓库，开始 Phase 1 脚手架搭建

---

**END**
